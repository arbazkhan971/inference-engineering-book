# Appendix F. KDP and release checklist

> **Appendices — the reference shelf.** The last gate between this manuscript and readers: what "release-ready" means, how the book meets the market, and the calendar that keeps it honest after launch. Written 2026-08-27; like every dated page in this book, it describes a snapshot — including a snapshot of what is not yet done.

## F.1 What release-ready means — the gate ledger

The editorial system behind this book defines gates, not vibes. Here is the honest ledger as of the day this appendix was written:

| Gate | Status | Evidence and what remains |
|---|---|---|
| Architecture | Pass | Chapter map, four parts, capability ladder locked at scaffold; positioning verified against the books corpus — no dedicated inference/serving title appeared in the AI or NLP bestseller nodes reviewed 2026-08-27 |
| Writer (Gate 1) | Pass, per chapter | 18/18 chapters + prologue carry logged self-reviews (structure, numbers traceability, fixes) in the project progress ledger |
| Technical edit (Gate 2) | Pass (chapters + appendices) | All 18 chapters reviewed (findings in review/); every P0/P1/P2 finding applied through the driver fix-passes (final chapter, ch18, closed 2026-08-27); verdicts were 17 MINOR and 1 MAJOR (ch04, resolved). Appendices A–F reviewed next (all MINOR, P0 = 0); appendix A's 2 P1 + 3 P2, B's 1 P1 + 6 P2, C's 3 P1 + 2 P2, D's 3 P1 + 6 P2, E's 4 P1 + 6 P2, and F's 1 P1 + 3 P2 all applied 2026-08-28 (E's P2 Splitwise finding rejected as false positive — ch03:40 carries the quote) — sweep complete |
| Code test | Pass | Companion tinyengine type-checks clean under TypeScript 5.9.3; two offline suites green across repeated runs — the smoke suite (chapter Break-it/Prove-it cases replayed as code, plus the gate-6 attack repros as regressions) and the cadence suite (the tester role's three nightly gates over fixtures: golden set, cache-hit gate, invoice reconciliation, plus the duplicate-row and formula-agreement regressions) |
| Build test | Pass | One command produces a validated EPUB (6.2 MB at last build); 34/34 mermaid diagrams render with labels; structural validator passes |
| Copyedit | Pass (2026-08-27) | Book-wide style/terminology scan + fixes: closer format normalized (ch15–18 bolded paragraphs → H3 subsections, matching ch01–14); token-rate units unified to `tokens/s`; terminology/spelling stragglers fixed; structural conventions verified uniform; full record in QUALITY_REPORT.md §3. The reflow decision is closed (see F.3 item 5): reader-facing code lines all fit 66 columns and the checker runs at budget 0 inside the build |
| Adversarial claims + xref audit (gate-6 wave 1, 2026-08-28) | P1 + P2 passes applied (wave closed) | Independent re-derivations + ~660 pointer checks (review/gate6-*.md): 0 P0s, 5 P1 + 17 P2. All 5 P1s applied 2026-08-28 — ch03's community 4-bit throughput relabeled batched aggregate with the derived 50–57 tokens/s single-stream ceiling; ch04's Anthropic tier row/prose aligned to the 4.6+ standard-price fact (older-sheet tier kept, dated); SchemaGuard de-attributed from the companion (ch13 Build-it, ch16 routing table, with the deliberate-omission note in Appendix D); ch12/ch18 event counts restated as four streaming events plus two finish-time markers (figure 12-m1 relabeled + re-rendered); the usage ledger gained its cache-write bucket (reasoning now a billed sub-count, matching the code's five fields and the four priced buckets everywhere else). The P2 pass landed 2026-08-28: of the 16 outstanding (xref P2-1 closed with the P1 ripples), 12 applied — ch03's arithmetic-intensity wording (a 2-byte weight buys two operations), H200 ridge 210→206, crossover streams 220→210, the 64×/61× convention stated instead of "exactly 61×", the cached-read example aligned to appendix C's Gemini 2.5 Flash row ($0.03 vs $0.30 + $1.00/1M/hr), appendix B's cache-loop card unified on N = reuses (w + N·r vs N + 1) with the 1-hour gloss "two cache reads, three requests total" (ch14's answer 1 too), ch14's read multiplier scoped to the US majors with DeepSeek's newest 2–3% rows named (plus the intro's "a tenth or less on current rows"), the 1-hour crossover printing both constants (1.6 ignoring reads, ≈1.74 counting them; checkpoint answer 5 moved to ~1.7), ch11's compaction turn billing the re-prefill at up to 2.5× where it books as a 1.25× write (checkpoint Q5 cued), ch06's kernel tax era-hedged twice ("the paper's 2023 measurements", "for as long as the gather runs through the block table"), ch11's Prove-it crossover restated model-scale-dependent (≈25K tokens on 8B-class, past 100K only at frontier width), the Qwen3 18 GiB unit drift fixed at both named sites plus a third (ch03) the review missed, and the companion README's node: shim count two→four; 1 already fixed by iteration 58 (session-store line count); 2 rejected as false positives with evidence — the OPT-13B "800 KB/token" is exactly the book's §4.3 formula under the paper's real config (2 × 5120 hidden × 40 layers × 2 bytes; head dim 128, not the reviewer's 64 — digest paged-attention-block-tables.md L5/L19), and vLLM's V1 max_num_seqs default is 128 (DEFAULT_MAX_NUM_SEQS in vllm/config/scheduler.py, dated digest + live-source check; the reviewer's 1024 was an early-V1 value since reverted per PR #16158); 1 closed as no-fix by judgment (the two titled Field notes — STYLE.md mandates no title form, and the titles are deliberate mini-headlines) |
| Companion attack (gate-6, 2026-08-28) | P1 + P2 passes applied (7/7 + 10/10 — queue closed) | 14 targeted attacks against the companion's money and admission paths (review/gate6-companion-attack.md + tests/attack-gate6.ts): 0 P0s, 7 P1 + 11 P2. All 7 P1s applied 2026-08-28 — `data:null`/array/string payloads skip instead of crashing the stream loop; non-numeric usage fields coerce to 0 with an `incomplete` flag instead of poisoning the meter with NaN; reservations are atomic (a TPM miss no longer burns the RPM slot); Bedrock overruns (output × burndown > booked) are debited, with the both-ways reconcile added to ch15's Build-it and Appendix D; an unknown model throws a named `UnknownModelError` before any session state mutates; `CacheLedger.hitRate` aligned to the book's cached ÷ (cached + fresh), agreeing with cache-hit-gate by construction; duplicate invoice and meter rows sum instead of last-wins (the gap-sign inversion fixed; the meter-side aggregation also resolved E2, one of the P2s, leaving 10). The 10 remaining P2s applied 2026-08-28 — first-stop-wins dedup for repeated finish chunks (with the dedup contract assigned in ch12's Build-it); Anthropic deltas keyed by block index (interleaved tool blocks attribute correctly; an orphan delta with no prior block start merges into nothing); non-object tool arguments (a string-typed `functionCall.args`) rejected with an `incomplete_call` marker, JSON-text strings parsed leniently; reservation fields clamped at the door (a negative cache-write can no longer cancel positive terms into a zero charge); the token bucket ignores a non-monotonic clock (NTP step / VM resume never drains it); usage clamps non-negative at the ledger's edge with a noted event (the meter is the trust boundary — no negative money); a read+write turn logs both a `read` and a `write` event with costs split so a turn's events always sum to its full cost; a task retired from the golden set is never reported `fixed`; and a `--floor` that does not parse as a finite number exits 2 instead of silently disabling the floor comparison. Appendix D updated to match (D.3 rules 2–3, D.4, D.5, D.8, the shipped-count table — six modules now 728 lines against the 720 estimate sum, both stated), ch18's "lands a little under the sum" reconciliation pointer made exact ("within a rounding"), README counts refreshed. Key repros wired into tests/smoke.ts (A7–A10, B3/B4, C3/C4) and tests/cadence.ts (D1/D2); attack-gate6.ts kept as regression evidence — its A8 and D2 blocks, which originally asserted the buggy behavior by construction, now assert the fixed contracts, and C1 still reports by construction on any throw (the named error it asked for), so its regression gate lives in smoke. Attack suite at the fixed tree: 10/10 P2 attacks HELD, 14 previously-held attacks still hold, C1's by-construction print the only remaining line |
| Clean-checkout build attack (gate-6, 2026-08-28) | P2 pass applied (3/3 — queue closed) | Fresh-clone protocol against commit 8999da3 (review/gate6-clean-build.md): build, companion install, verify, idempotency rebuild all PASS — 0 P0 / 0 P1 / 3 P2. All three applied 2026-08-28: the cover render gained the same mtime guard tools/render-mermaid.sh already uses (a committed PNG/JPEG that is not older than its SVG source is current — a stranger's first build no longer dirties figures/png/cover.png + cover.jpg with a different machine's rsvg/font bytes; touch the SVG to re-render), the README status table refreshed from its pre-seal wording ("fanout in progress", "autonomous driver") to the sealed release-candidate state, and the Kindle temp-file prefix in tools/prepare-kindle-epub.py de-Vol.-I'd (harness-engineering- → inference-engineering-; the series-scoped urn:harness-engineering:vol2-inference identifier in build.sh is deliberate and stays). Both guard paths verified live: untouched tree → both outputs skip; touched SVG → both re-render with deterministic bytes |
| Pedagogy wave (beginner-simulation audits, 2026-08-28) | Fix pass underway (ch01–16 applied; ch17+ queued) | Beginner-simulation audits (review/ped/*-pedagogy.md, [LOST]/[CONFUSING]/[POLISH] protocol): ch01 graded 4.58/5 with zero [LOST] findings — all 4 [CONFUSING] + 8 [POLISH] applied 2026-08-28: Token defined in the 1.1 table itself (the book's unit of account, was undefined until ch2; table now fourteen rows), the 1.2 KV-caching gloss reuses the ticket picture instead of unexplained "attention", "Pareto frontier" replaced by the plain trade-off sentence, throughput glossed at its true first use, HTTP 200 anchored in plain words, SOSP/OSDI get one-phrase venue glosses, the arXiv identifier moved out of the spine to Appendix E (STYLE's no-citation-walls rule), the 1.3 double citation slimmed to one named finding, hop 7 points at chapter 6, the Build-it closer gained a no-codebase on-ramp (the quickstart option), the 1.2 scope sentence broken into a three-beat list, and two analogy bridges added at the 1.4/1.5 transitions. Appendix A's Token pointer updated to (Ch. 1, 2). ch02 graded 4.3/5 — all 19 findings applied 2026-08-28: the single [LOST] fixed with a plain-words BPE bridge sentence ("start with single characters, glue together the pairs that show up most often… that is the shorthand being invented") placed before the formal merge sentence; 8 [CONFUSING] — bytes glossed at first use, token bucket glossed, the cache vocabulary (uncached/cache-hit/cache-miss, four chapters early) given referents at all three sites with a chapter-6 pointer, "low mutual information" kept but subordinated to the plain clause, "affine" replaced by the taxi picture it was naming, "call site" replaced at both sites, BERT/SentencePiece compressed phrasing unpacked; 10 [POLISH] — greedy→longest-match ×2, H100 SXM glossed as the data-center card, "compression codebook"→catalog, arXiv expanded at its first durable-spine use book-wide, comma alignment restated in counting words, odometer row realigned to the autocorrect picture, "two hops"→two stages, e2e spelled out in the vocab table, provenance note de-jargoned, the quadrupled-training-cost claim given its forward gesture to chapter 3's arithmetic instead of naked trust; plus the audit's restaurant↔taxi bridge clause welded at 2.6's open. ch03 graded 3.75/5 (the wave's floor chapter) — all 20 findings applied 2026-08-28: 2 [LOST] — the opening's petaFLOP/BF16/TFLOPS/dense-mode jargon stack replaced with the plain-words quadrillion framing plus an explicit deferral pointer (3.2 now carries the BF16 and TFLOPS expansions at their first use), and the FlashAttention sentence reordered picture-first ("attention's working notes — called queries, keys, and values" bridge + "running re-balancing of scores" gloss) before the tiling/online-softmax formalism; 9 [CONFUSING] — 2:4 sparsity glossed as the throws-away-half-the-numbers shortcut, the 3.1 table grown 12 → 17 rows (Weights/Parameter/Kernel/TTFT/TPOT — the machinery vocabulary the chapter's own divisions use), 8B glossed (8 billion parameters) at first use, the 0.3% derivation's division shown (1 ÷ 295 ≈ 0.003), the 13B roofline example de-e-notated with 13 × 2 = 26 shown and honestly re-anchored to chapter 2's actual 8B/208 floor (the old "re-derive chapter 2's floor" attribution was wrong — ch2's floor is the 8B one; the 13B is now a second-size generalization), the attention formula's new symbols glossed (stacked layers, d as an ignore-its-value lane width, "O(N²) is engineer-speak for grows with the square"), the 128k-really-means-131,072 convention stated once at first switch, the Gemini pricing box restructured into structure-vs-rates layers (the self-referencing archived-tier parenthetical deleted); 9 [POLISH] — tensor cores → the chip's specialized matrix-math units, a binary-vs-decimal units note at the KV crossover, service-level objective glossed as the promised worst latency, 4-bit quantization given its chapter-9 pointer, eager-mode → step-by-step execution overheads, PaLM 540B glossed as a 540-billion-parameter model, the 61× division shown ((1,000,000 ÷ 128,000)² ≈ 61), GEMM's "rows" given its tokens' antecedent (prose + table row), BERT-large/GPT-2 dated as two well-known models of the era. Appendix A reverse-index ripples: Weights (Ch. 1, 3, 4), TTFT (Ch. 1–3), TPOT (Ch. 2, 3). ch04 graded 4.5/5 — all 12 findings applied 2026-08-28 (zero [LOST]): 8 [CONFUSING] — the cache-vs-text magnitude corrected (the audit's highest-priority numeric-honesty fix: "dozens of times larger" understated a ratio the chapter's own numbers give as 32,768×; now "tens of thousands" with the derived 131,072 ÷ 4 shown and both-inputs-are-rules-of-thumb hedged), "vector" glossed at its only load-bearing use, "transformer" primed, the opening's triple parenthetical split (GPU glossed inline; the HBM reminder moved to 4.2's first-use site and anchored to chapter 3's pyramid instead of re-expanding the letters, which ch03 already expanded), needle-in-a-haystack glossed as find-one-fact-in-a-wall-of-text, multi-hop glossed (answers that need several chained lookups), B given its local gloss at the 4.6 recall moment, model card glossed (the spec sheet published with every model) and config.json glossed in Build it; 4 [POLISH] — the 4.5 ELI5's fixes numbered Fix one–four (also correcting the miscounted "Three fixes exist"), the OPT/MHA unit mix normalized to KiB with one-step-checkable ratios (800 KiB / 1.6 GiB with the ×2,048 derivation; 2,560 KiB with the full 2 × 80 × 64 × 128 × 2 formula and 20 GiB; GQA's 2,560 ÷ 320 = exactly one-eighth; 6.25× vs the 8B figure), "point your monitoring"→memory dashboard, the 400k/272k gap reframed as the developer-facing surprise. Both advisory worst-gap items applied: the MLA cluster got its worked micro-example (2 × 128 × 128 = 32,768 numbers = 64 KiB vs 576 numbers ≈ 1.1 KiB, the ~57× reduction; 576 × 2 bytes × 61 layers lands on the table's ≈68.6 KiB/token row), and the 4.6 recall moments got local glosses (the ×0.7 derate named as deliberately skipped). One completion fix beyond the audit: the 4.5 heading said "Five ways" over a ladder whose fifth row is the 1× MHA baseline — retitled "Four ways" with the ladder intro naming baseline-plus-four. Pass provenance: recovered from an uncommitted working tree left by an interrupted session that had applied all 12 findings + both advisory gaps; this iteration verified every hunk against the audit, re-derived every number, and added the completion fixes. ch05 graded 4.3/5 — all 13 findings applied 2026-08-28 (1 [LOST] + 7 [CONFUSING] + 5 [POLISH]): the [LOST] fixed at the queueing-formula wall, the chapter's climax mechanism — the formula sentence now glosses λ ("how fast requests arrive") beside ρ, and a plain-words reading follows immediately ("your wait grows with how fast requests arrive (λ); with the average of *squared* request lengths — a few very long jobs hurt more than many short ones that add up to the same total; and it explodes as utilization approaches 100%, which is the ÷(1−ρ) underneath"); 7 [CONFUSING] — "product leg" de-jargoned to "one of our products" with concurrency glossed at its load-bearing first use ("running fewer of our requests at the same time"), arithmetic intensity glossed at use ("how much useful math each byte of model-reading buys"), the unglossed "weight streaming of chapter 3" replaced by "reading the model's whole memory every step, the weight traffic chapter 3 priced" (grep showed ch03 never coins "weight streaming"; "weight traffic" is ch03's own term, reused at 5.4's second site), "their tensors stack" → "stack into one uniform block of numbers (a *tensor*)" (the book's only standalone use of the noun), attention glossed at its chapter-first use ("the lookup step in which each new token consults every earlier token of its conversation, chapter 4's machinery" — the audit's "chapter 2" pointer corrected: ch02 never uses the word, ch04's §4.2 is the teaching site), the utilization table's unexplained "M/M/1 form" replaced by "same math, simplified arrival assumptions" plus a half-line defining system time ("your wait plus your own service; the higher ρ climbs, the more of that number is pure wait"), and the ITL naming whiplash given the audit's bold mini-equation on its own line ("**ITL = TPOT** — same clock; the dashboard's name vs yours"); 5 [POLISH] — the stage-granularity abstraction concretized as "a faster door onto a stage that still moves everyone at once" (adapted off the audit's coat-check image: coat check is ch04's dedicated KV metaphor and already borrowed in this chapter's Where-the-picture-stops "coat-check overflow crisis" — one picture, one mechanism), the 5.5 carpool switch given its bus bridge ("the city bus again, or if you like, a carpool of strangers"), "shed load *multiplicatively*" → "cut your in-flight requests by half or more each time attainment drops" (the AIMD shape in plain words; ch15 owns the formal name), the ASCII block's mid-block "t=4..." note moved below the fence as a full sentence ("A finishes at iteration 4, but the batch runs until iteration 32…"), and the "promissory note" metaphor simplified to "the promise chapter 3 left you — now due" (ch03's 3.5 close makes exactly that promise). Three documented deviations, all evidenced: the attention pointer (ch04, not the audit's ch02), the coat-check→door image (collision), and the table header kept "system-time" with an immediate definition instead of the audit's "wait multiplier" — the values (2× service at ρ=0.5, where wait = 1× service) are system-time multipliers, and relabeling them "wait" would break the checking-beginner rule. Ripple greps clean: product leg / promissory / M/M/1 / weight streaming / multiplicatively / "granularity is too coarse" return zero hits in ch05, and no other manuscript file quotes the edited sentences (ch03's own field note keeps "product leg" — practitioner voice, its audit passed it). Ch05 now 5,192 words (+181, all gloss/on-ramp/plain-reading-bearing), inside the 5,500 concept cap. ch06 graded 4.4/5 — all 19 findings applied 2026-08-28 (zero [LOST]; 12 [CONFUSING] + 7 [POLISH]): the audit's worst-gap machinery cluster glossed at first use — the attention kernel ("the chip's read-the-notes routine — the piece of the engine that actually walks the memory") at its 6.2 first appearance, goodput against chapter 5's own coinage ("the requests per second that actually finish inside their latency promises"), vLLM introduced as "the open-source serving engine this chapter keeps using as its example" at first mention, the intro's "KV desk" (chapter 4's furniture colliding with chapter 5's bus in one sentence) → "the KV memory", and fragmentation ("memory stranded in pieces too small or too scattered to reuse") at its intro first use ahead of 6.2's definitions; the hash-chain formula's only unexplained symbols captioned ("H is the hashing function; ∅ means 'nothing before block 0'; the blank padding is alignment only"); the 62-of-62.5 pricing leap now shows its division (1,000 ÷ 16 = 62.5, so 62 whole blocks hit and the half-block tail does not count) per the chapter's own show-the-division method; Alpaca/ShareGPT introduced as "two public workload collections"; the mamba constraint glossed ("the different cache layout some newer architectures use") instead of name-dropped; the radix-tree triple-idea sentence split into three; "embed" glossed ("turn its words into numbers that score similarity"); the never-hired librarian replaced by the translator ("byte-identical contracts" — her own ELI5 was a 40-page contract; also repairs the dangling "her" in the paraphrase break two paragraphs later); [POLISH] — the OS expansion moved to its true first use (the intro's "operating system" line, grep-confirmed the book-wide first; step 3's duplicate "(operating system)" dropped; ch18's later expansion is noted for its own queued pass), "128k context" glossed (128,000-token conversations), the 1.6 GB sentence's double citation split into two sentences with the LLaMA-13B aside standing alone per the citation-wall rule, "refcounts them" → "bumps their reference counts" (noun), the lever table's compaction glossed (session-summarizing), Build-it gained a no-code on-ramp (playground two-send stopwatch — the usage-field check is the proof, the stopwatch is the lesson; no unsupported product claims), and the barista/translator/print-shop connective sentence ("same kitchen, three jobs: noticing, reusing, pricing") added at 6.5's open. One opportunistic ripple beyond the audit: ch06's duplicate HBM expansion dropped — ch03's table and §3.2 own the letters book-wide (iteration-65 dedup precedent). Two documented wording deviations: the fragmentation gloss uses "stranded in pieces too small or too scattered to reuse" (covering both internal and external senses) rather than the audit's "broken into pieces too broken up to reuse" (broken twice), and the goodput gloss matches ch05's actual definition ("finish inside their latency promises") rather than the audit's looser "speed promise". Ripple greps clean: librarian / KV desk / "refcounts them" / "out of 62.5" / the duplicate HBM parenthetical return zero hits; "on Alpaca and" appears nowhere else; 62.5 appears in no other manuscript file. Ch06 now 5,637 words (+226, all gloss/on-ramp-bearing), 2.5% over the 5,500 concept cap — logged rather than trimmed, consistent with the ch02 (2.9%) / ch04 (5.6%) pedagogy precedent. ch07 graded 4.70/5 — all 6 findings applied 2026-08-28 (zero [LOST]; 3 [CONFUSING] + 3 [POLISH]): the audit's worst-gap hardware-name cluster fixed — "tensor-core-shaped" dropped for chapter 3's own coinage ("built for the chip's specialized matrix-math units (chapter 3)"; grep showed ch03's iteration-64 fix had removed "tensor cores" book-wide, making ch07 the term's only surviving site), and the NVLink/InfiniBand brand-name sentence retaught as plain hops ("over the fast chip-to-chip links within one machine (NVLink), or the network links between machines (RDMA, remote direct memory access, over InfiniBand-class networks)" — the sentence's own within-vs-across structure now carries the teaching); the opener's "under tail-latency constraints" → "under strict worst-case-wait limits" (the technical term keeps its self-glossed 7.3 site — "protecting everyone's stream" — and 7.5's proper use; ch05's own instance passed its closed audit and stays); the opener's "full of retrieved documents" → "full of attached documents" (RAG stays named only where it is expanded, 7.4); Checkpoint Q2's unnamed formula now taught in 7.2 — the 512× sentence gains "arithmetic intensity climbing toward ≈ 512, chapter 3's batch dial turned up by the prompt's own length" (matching ch03's own "approximately B FLOPs per byte"); and "forward pass" — used book-wide since ch01 but never defined — glossed at its ch07 site below the timeline fence ("That long prefill bar is one forward pass over the prompt — a single full run of the model across every prompt token, the \"up-front pass\" of the 7.1 table — and it alone takes hundreds of ms") rather than the audit's in-fence parenthetical, which breaks the 66-column reader-fit budget at the note's alignment indent (iteration-66 move-it-below-the-fence precedent); ch01's own unglossed forward-pass use left untouched (its audit closed, no front-running). Ripple greps clean: tensor-core / "retrieved documents" / "NVLink within" return zero hits book-wide; "tail-latency constraints" survives only at ch05's passed site and 7.3's self-glossed one. Ch07 now 5,062 words (+73, all gloss/on-ramp-bearing), inside the 5,500 concept cap. ch08 graded 4.4/5 — all 12 findings applied 2026-08-28 (1 [LOST] + 6 [CONFUSING] + 5 [POLISH]): the [LOST] fixed at the correction-rule wall — the chapter's one theorem now carries its plain-words ladder immediately after the formula ("In plain words: each model holds a ranked preference list over the next word — that list is the distribution. Subtract the drafter's preference for each word from the target's, throw away anything that drops below zero, rescale what remains so the list sums to one again, and draw the replacement from that corrected list") rather than the audit's inline em-dash, keeping the formula sentence intact; the three borrowed load-bearing terms glossed — logits at its true 8.2 first use ("a raw preference score for every possible next token"; 8.5's −∞ additionally glossed as "a score so low the sampler can never draw them" — the audit had flagged only the 8.5 site), n-gram at 8.3's first prose use ("a run of n consecutive tokens (a 3-gram is three in a row)" — the audit's "word-pieces" replaced by the book's own token vocabulary), and the EAGLE hidden state ("the model's private intermediate notes, computed before it commits to any word", with the vocab table's "hidden layers" → "early layers" so the self-speculation row survives without the jargon); plus tree attention ("scoring every branch of the draft tree in the same pass"), the mode/greedy/argmax cluster ("its single most-favored next token, the top item of its preference list, picked with no randomness — that is what 'greedy' means here" and "the same top-scoring token"), the roofline 295 given its one-clause scale ("the chip would need roughly 295 arithmetic operations per byte moved to stay busy; decode hands it about one") with "balance point" re-coined to chapter 3's own term "ridge point" (ch03:80 "also called the machine balance" — the audit had quoted the drift), 70B expanded at the opener ("70-billion-parameter (70B)"), E[progress] glossed ("expected progress — the long-run average, not a promise about any single pass"), sub-token amortized draft cost unfolded ("spread so thin across the tokens it helps produce that it rounds below one token's worth"), "8×H100" → "eight H100 chips", and GQA pointed at "one of chapter 4's cache-shrinking designs" (ch04 §4.5 is literally "Four ways to shrink the coat"; the audit's suggested "cache-sharing ladder" would have mispointed at chapter 6's prefix sharing — sharing is a different lever than shrinking). Pass provenance: the manuscript edits were committed mid-application by an interrupted session (1547b62, halted at its turn cap with ledgers and validation outstanding, per its own commit message); this iteration verified every hunk against the audit, verified every cross-chapter reference the edits lean on (ch03's ridge-point coinage, ch04's §4.5 title and GQA row, ch13's owning logit definition), confirmed all 12 findings applied across 11 changed lines with zero numeric drift, then ran validation and wrote the ledgers. Ch08 now 5,555 words (+220, all gloss-bearing), 1.0% over the 5,500 concept cap — logged rather than trimmed, consistent with the ch06 (2.5%) / ch02 (2.9%) / ch04 (5.6%) pedagogy precedent. ch09 graded 4.2/5 — all 14 findings applied 2026-08-28 (zero [LOST]; 6 [CONFUSING] + 8 [POLISH]): the audit's worst gap (the formula-first affine paragraph in 9.2) fixed at both flagged symptoms — the zero-point `z` gained its "you may ignore it" parenthetical (a small offset so that zero lands on a grid point; it changes nothing in the arithmetic below), and "15 levels between min and max" corrected to "the span splits into 2^4 − 1 = 15 equal steps between 16 grid points" (4 bits = 16 values; the formula divides by the steps between them — the counting beginner now matches the math); the 9.4 headline (the 4-bit reasoning cliff, the chapter's must-retain fact) split from one triple-dash sentence into four plain sentences with the 0.00 datum kept ("did not move at all (0.00)"); the intro's promissory sentence split and de-dashed (also dropping ch09's duplicate "(key-value)" re-expansion); the 9.3 jargon leaks fixed — "before the matmul" → "before the matrix multiply (the giant multiplication at the heart of every layer)" (the audit's preferred option; ch03's GEMM expansion remains the formal teaching site), the Hessian parenthesis flipped plain-first ("using a mathematical map of how rounding errors ripple forward through the remaining weights (an approximate Hessian — the technical name for that error map)" — documented deviation: the audit's "for the credential-checkers" snark dropped, the credential term kept), and the e4m3/e5m2 parenthetical re-framed plain while preserving the iteration-35 gate-2 bit attribution ("two 8-bit layouts: the digits count exponent bits then mantissa bits, 4-and-3 or 5-and-2"); outlier channels glossed ("a few coordinates (single positions in the list of numbers) that grow far larger than all the others" — parens not the audit's nested dashes); "brain float 16" dropped (the book-wide outlier — every other chapter's gloss says bfloat16, and ch09's own 9.1 table already carries "The 2-byte formats models are trained and shipped in"), keeping the Meta model card citation; "the 0.7 real-kernel haircut" → "chapter 3's 0.7 kernel-efficiency discount" (ch03's own coinage is the 0.7 efficiency factor/rule of thumb) at 9.2 and, as a ripple, at Build-it's "the 0.7 haircut"; "the floor only binds what binds it" → "the floor only limits the traffic that actually flows through it"; the variance sentence's missing article restored ("cost the 7B/32B Qwen distills"); MRCR led plain ("a multi-round who-referred-to-what benchmark; the literature calls it co-reference resolution" — documented deviation keeping the gate-2-corrected official term inside the gloss; MRCR appears in no other manuscript file) with "(area under the curve)" → "(the summary score under the whole curve)"; and the lever table's Where column disambiguated with the book's own convention — header "Chapter", bare chapter numbers, "N (this chapter)" self-reference — after the audit flagged "this chapter, 3" next to "this chapter, 16" as unreadable (documented deviation: the audit read row 1 as §9.3, but the row text points at chapter 3's floor and row 2 at chapter 4's cache — under the book-wide bare-number convention all three rows were chapter references, and ch03/ch04's own tables set the fixed pattern). One ripple beyond the audit: ch10:38's "same method and haircut caveats as chapter 3" → "efficiency caveats" — the term's only surviving site after ch09's rename, and ch03 itself never says haircut. Ripple greps clean: brain float / haircut / matmul / "15 levels" / "this chapter, " / co-reference / "area under the curve" return zero hits outside the fixed sites (appendix E's "Hessian-compensated weight quantization" annotation is correct bibliography voice and untouched). Ch09 now 5,864 words (raw wc; +92 over its sealed 5,772), 6.6% over the 5,500 concept cap on a base already 4.9% over and logged content-bearing — logged rather than trimmed, consistent with the pedagogy precedent. ch10 graded 4.3/5 — all 21 findings applied 2026-08-28 (zero [LOST]; 9 [CONFUSING] + 12 [POLISH]): the audit's worst-gap cluster (undefined load-bearing terms) glossed at first use — *collectives* ("synchronized data swaps between chips") in 10.2's DP bullet, *hidden state* ("the token's working copy so far") at its true first use in the 10.2 EP bullet (covering 10.3/10.4/10.5's reuses), *rank* ("the supercomputing word for one chip's slot in the group") inline in 10.3's CP sentence, and — the audit's single worst — the *residual connection* in the silent-drop payoff sentence ("continues past that layer unchanged, carried by the transformer's always-open side path (the residual connection) — arriving without that expert's contribution"); 10.3's depth parentheticals given skip signals — the all-reduce/all-gather/reduce-scatter names now carry "(names you can skip; the point is two synchronized swaps per layer)", and "projection weights" replaced by "that layer's numbers" with heads re-anchored to chapter 4; the missing 74 GB derivation shown in the headline sentence ("37B active × 2 bytes ≈ 74 GB, derived" — the identical pattern the chapter already used for 1,342 GB, and the exact arithmetic Checkpoint Q2 examines); the 10.2 product-formula wall preceded by its worked example ("The choices multiply: 2-way TP × 4 pipeline stages × 2 copies = 16 chips"); the 10.1 table de-jargoned and completed — the TP row leads plain ("Split each layer's learned numbers (its weight matrices) into slices"), Grouped GEMM leads plain ("One combined math job per expert, covering every token sent to it"), and the missing EP row added after CP ("Spread the model's many experts across chips; each token travels to its experts | Specialists seated at different counters"), with the after-table sentence re-keyed "Three ideas carry the chapter"; plus "shard" glossed at its intro first use ("real engines split — *shard* — across GPUs"), *dense* glossed at its 10.2 first use with a 10.4 pointer, "haircut" already gone (iteration-70 ripple), the router de-jargoned ("a tiny learned scorer"), "compose multiplicatively" → "the two savings multiply when stacked", DeepEP's SMs glossed at first use ("low use of the GPU's compute units (its 'streaming multiprocessors')"), the production paragraph marked "(depth layer — skim on first read)", "ontology" → "taxonomy of skills", "granularity" → "the finest-grained — and most expensive — rhythm", and the nested mermaid given its reading instruction ("Read boxes inside boxes as chips inside groups inside copies: outermost is the duplicate (DP); innermost is the slice (TP) or the expert strip (EP)"). Appendix A ripples (the glossary mirrors the chapter table by design): TP entry re-worded to match, new EP entry added after CP, Grouped GEMM entry re-worded — its old text was the audit's "landed tokens" jargon surviving in the glossary. Pass provenance: recovered from an uncommitted working tree left by an interrupted session (all 21 findings already applied); this iteration verified every hunk against the audit, re-derived the one added number (37 × 2 = 74), confirmed the mermaid block byte-identical, fixed the three appendix-A ripples, and wrote the ledgers. Ch10 now 5,616 words (raw wc; +173 over its pre-pass 5,443, all gloss/on-ramp/row-bearing), 2.1% over the 5,500 concept cap — logged rather than trimmed, consistent with the ch06 (2.5%) / ch02 (2.9%) / ch04 (5.6%) pedagogy precedent. ch11 graded 4.2/5 — all 11 findings applied 2026-08-28 (1 [LOST] + 5 [CONFUSING] + 5 [POLISH]): the [LOST] fixed at the tier-cliff money example — the chapter's only actively-wrong-model spot — the ambiguous "100K more usable room" clause replaced by the reconciling arithmetic ("The big request carries 1.5× the tokens *and* pays double per token — 1.5 × 2 = 3× the bill") and the closer re-keyed "You pay double per token for the fog" so the doubled rate and tripled bill no longer collide (300/199 ≈ 1.5; $4/$2 = 2; $1.20/$0.40 = 3 — re-derived before applying); 5 [CONFUSING] — the 11.1 Pass-KV row de-jargoned ("Which cargo circulates: the stored notes (keys/values), or the new questions (queries)", with "tensor" removed from the Simple-meaning column and given a chapter-5 recall gloss at its true 11.3 prose home), the 11.2 quadratic decomposition given its missing middle rung (one inserted sentence — "Under the hood, every model has two moving parts: a page-by-page part … and a cross-referencing part …" — so N² and 2·W land on a ladder; documented deviation: the audit's "library part" became "page-by-page part" because "library" collides with the `long-context-attention` library named two paragraphs later), Ring Attention's query block glossed at the mechanics site ("the questions the current tokens are asking" — a spaced recall: ch03/ch04 taught Q/K/V, the audit read ch11 cold), DeepSpeed-Ulysses given its missing picture (the ELI5 extended with the question-type family, "each clerk answers one category of question — all the dates, say — for the whole file"; the heads↔question-categories mapping made at the term's true first use in the Ring bullet — one sentence before the Ulysses bullet the audit pointed at, documented deviation — and the Ulysses bullet anchored "The other family from the ELI5"), and the 11.5 layer-one arithmetic's unstated baseline named ("2× that turn (30K fresh versus the old 15K-equivalent read)"); 5 [POLISH] — the formula's c given ch03's own ignore-it gloss ("a per-model constant you never need to number"), the MagicDec sentence split rule-first/evidence-second ("speculation follows bandwidth … The measured version: …"), "contract-layer view" → "what it looks like from the API's point of view", "the trap composes with caching" → "stacks" with the same-idiom ripple to 11.6's "Compose with" → "Combine with" (ch05's "decomposes" and ch10/ch13/ch16's own compose sites belong to their closed or queued audits — no front-running), and the KVSL row's binder made self-evident ("The whole binder: the file plus every note taken since"). Appendix A ripple: the Pass-KV entry re-worded to match the fixed row (the "tensor" leak had survived in the glossary). Ripple greps clean: "usable room" / "three times cheaper" / contract-layer (ch11) / composes (ch11) / "Which tensor" return zero manuscript hits outside the fixed sites. Ch11 now 5,319 words (+169, all gloss/on-ramp/picture-bearing), inside the 5,500 concept cap. ch12 graded 4.4/5 — all 12 findings applied 2026-08-28 (zero [LOST]; 6 [CONFUSING] + 6 [POLISH]) plus both advisory worst-gap items: the audit's single worst gap — the event/delta/chunk triple at the chapter's front door, three table rows that felt 90% identical — disambiguated in one sentence after the Two-terms paragraph ("the *event* is the envelope, the *delta* is the slice inside it, and *chunk* is simply the common vendor name for an event that carries deltas (OpenAI's, most famously) — two things, three words, because providers named them separately"; documented deviation: the audit's "simply OpenAI's name" became "the common vendor name (OpenAI's, most famously)" — the chapter's own Gemini sentence uses "chunks" two sections later, and the digest attests chunks for both wire formats); the parallel-calls sentence's self-contradictory "do not interleave … but … can mix" construction rewritten digest-exactly ("Fragments belonging to the *same* call always arrive in order, never mixed with another call's fragments inside one slot; between one call's fragments and the next, a different call's fragments or plain text may appear — which is why you buffer per call slot, never in one global buffer"; documented deviation: the audit's "with nothing else in between" dropped — the digest attests same-call order and no cross-call interleaving within one index, but not that text cannot interrupt one call's fragment run); Pydantic glossed (a widely used Python library that checks data shapes) and enum glossed (a fixed list of allowed values — "stop", "length", and friends) at their 12.2 sites; p99/p50 glossed as spaced recalls at the two-clocks paragraph (the 99th-percentile first-token time, the slow end of normal; the median — typical throughput); the cache-lifetime parenthetical pointed at chapter 14 ("the two cache lifetimes chapter 14 prices"); the 12.5 stop-reason state machine gained the first-stop-wins clause ("Finish chunks can arrive more than once — first one wins, the rest are ignored" — machinery Build-it previously introduced only at exercise time, consistent with Appendix D's rule 3 and the companion's stop() dedup); the opening's double acronym expansion given its everyday handle ("the paper form your code mails to the provider and gets stamped back"); "ride along all chapter" grammar slip fixed ("for the whole chapter"); PCM glossed (uncompressed digital sound); middleware glossed (extra software layers sitting between client and engine); the dated four-grammars box opens with its recap flag ("reference recap — the same four grammars, compressed"); and both worst-gap reference-wall flags applied (12.2's provider walk and 12.5's usage bullets each tell the reader they are not meant to memorize — the normalizer exists so they never have to). Ripple greps clean: the new phrases appear nowhere else; appendix A's Chunk entry is ch07's prefill sense with the existing ch12 cross-flag (no glossary ripple). Ch12 now 5,678 words (raw wc; +227, all gloss/on-ramp/flag-bearing), 3.2% over the 5,500 concept cap — logged rather than trimmed, consistent with the ch06 (2.5%) / ch02 (2.9%) / ch04 (5.6%) pedagogy precedent. ch13 graded 4.69/5 — all 9 actionable findings applied 2026-08-28 (zero [LOST]; 7 [CONFUSING] + 2 applied [POLISH]; the third [POLISH] is the audit's explicit keep-as-is on inline checkpoint answers, no change): the audit's single worst gap — the triple-duty compile sentence in 13.2 (compile, intersect, and per-state lookup nested in one clause chain; the only place a beginner risks a full stall) — split into three plain sentences ("the engine first compiles the grammar over characters. Then it intersects that rule set with the tokenizer's entire vocabulary, using a token trie — a prefix tree over the vocabulary. The result is, for each rule-machine state, the exact set of legal token ids"), citations preserved; the #2 gap — four name-dropped sampler knobs in a sentence whose point is "they still apply" — glossed in place ("the dials that control how adventurous each word choice is"); documented deviation: the audit's "(chapter 2's tour)" pointer dropped — grep-verified that sampler-knob content exists nowhere outside ch13 in the manuscript, and iteration 39's gate-2 fix removed exactly that false pointer (the lever table's "Not owned elsewhere — glossed in §13.2" row stays true); the #3 gap — 13.5's academic armor — pass@3 glossed ("allowed three attempts", completing the notation family ch02's pass@1 definition opens) and TC⁰ inverted picture-first ("limited to circuits too shallow for some reasoning problems (the complexity class TC⁰), making certain correct outputs unreachable in principle"); "context-free" given its plain gloss ("the kind whose rules can nest brackets to any depth, which is exactly why the machine needs a stack") with the pushdown automaton's push/pop mechanics kept; `additionalProperties: false` glossed ("no fields beyond the ones you declared"); the quoted CFG acronym keyed after the quote ("CFG is the bracket-nesting grammar kind from 13.2", folded into the citation parenthetical so one parenthesis carries gloss + source); the opening's unglossed "sampler" replaced by "the engine's word-choosing machinery" (sampler's first surviving use is now 13.1's self-defining vocabulary paragraph — "the number the sampler converts into a probability before drawing" — with 13.2's full definition following); and the key-finding clause "behavioral, not a parsing artifact" bolded per the audit's consider-bolding note. Ripple greps clean: "an FSM carrying a stack" / "the complexity class TC⁰ —" / "reaches into the sampler" return zero manuscript hits; context-free, TC⁰, and additionalProperties appear in no other file (appendix A carries none of the three; the mermaid diagram's top-k/top-p/min-p node label is untouched diagram convention). Ch13 now 6,157 words (raw wc; +52, all gloss-bearing) on a base already logged over the 5,500 concept cap as content-bearing (iterations 17/39) — logged rather than trimmed, consistent with the pedagogy precedent. ch14 graded 4.17/5 — all 14 findings applied 2026-08-28 (3 [LOST] + 6 [CONFUSING] + 5 [POLISH]; the auditor independently re-derived the chapter's arithmetic — punch card, 525,000 loop, 354,200/90,450 totals, fanout 5.01M, all six chart points — and found it correct, findings teaching-only): the audit's worst gap — three meanings of "block" in one chapter (chapter 6's 16-token KV pages; Anthropic's content blocks in the 20-block lookback and the leapfrog rule; the worked example's turn blocks) — disambiguated at the lookback's first use ("blocks here are Anthropic's *content* blocks (a message or tool result), not chapter 6's 16-token pages" — content blocks being the book's own ch12 vocabulary; digests attest "20 blocks" without defining the unit, so the fix distinguishes two units the book itself has taught rather than importing a new fact) with the worked example's block labeled ("every turn's 1,000-token history block"), so a reader who internalized ch06 no longer computes 20 × 16 and lands confidently wrong; [LOST] #2 — the leapfrog rule's missing why, one sentence after the procedure ("A checkpoint left more than 20 blocks behind can no longer see any cache entry near the tail, so its write premium buys nothing; leapfrogging keeps one checkpoint always inside the window the provider will actually match"); [LOST] #3 — the mechanism sentence, the chapter's only physics and its densest prose, glossed inline (hash → "takes a fingerprint of the opening bytes, one number that matches only exactly those tokens"; resident → "a stored copy still live"; pinned → "held by your request rather than left to the clock"), with the sentence first verified digest-verbatim (cache-hit-math-agent-loops: "resident (within TTL or explicitly pinned)"); the two un-derived arithmetic islands now derived on the page — the 30,000 history-read total shows its triangular count ("re-read 0, 1, …, 24 times as turns arrive, 300 block-reads × 1,000 tokens × 0.1 = 30,000"; 0+1+…+24 = 300 re-derived before applying) and the ≈1.74 1-hour crossover carries its two-line derivation (each expiry costs the 5-minute plan a 1.25× re-write while the 1-hour entry absorbs every gap and serves reads at 0.1× — 2.0 + 0.1·N < 1.25·N once N > 2.0 ÷ 1.15 ≈ 1.74, the same inequality ch17 runs at session scale; the audit's appendix-B-card option rejected because the card carries only the 1.6 form); 6 [CONFUSING] — Opus/Sonnet/Haiku introduced as Anthropic's large/mid/small tiers at the minimums row; hidden system tokens glossed ("tokens the provider injects on your behalf, tool schemas and the like" — digest-attested "hidden system content incl. tools"); the salt row's circular "cache key" (used before any key exists for the reader) replaced by "the identity the provider fingerprints your prefix into"; the hit-rate picture re-flavored to reuse ("a form letter already on the printing plate" — the chapter's own engraver family, replacing the non-reuse quiz picture); 5 [POLISH] — the opening KV recap anchored to chapter 4's actual coinage ("the model's memory of having already read them, chapter 4's per-request notes" — documented deviation: the audit's "notebook" was never ch04's word, ch04's ELI5 is the stenographer's notes), the serializer gloss ("the code that packs your request into bytes"), the tool-stub gloss (minimal placeholder definitions — a name and a one-line description), "the miss price" → "the uncached (miss) price" at the DeepSeek first use (14.3's later "miss price" site inherits the frame), and the actuarial-insurance sentence restated in plain words ("count the expiries you expect per idle hour and compare that cost against the 0.75× extra premium — that is the whole decision"). One appendix-A ripple (the glossary mirrors the chapter table by design): the Cache salt entry re-worded to match the fixed row — its "cache key" wording carried the same circularity; the Hit rate entry carries no picture, so the quiz fix needed no glossary ripple. Documented deviation on the serializer gloss: "packs your request into bytes" rather than the audit's "packs your tool list into the bytes sent" — the flagged sentence's serializer re-serializes history (dictionary key order), not only the tool list; the gloss covers the term's actual load. Ripple greps clean: the quiz picture and "actuarial" are gone book-wide; "notebook" was never introduced (the deviation above); "cache key" survives only in ch17's own sites, whose pedagogy audit is queued (no front-running); "20 blocks" sites verified against ch06's 16-token definition and ch12's content-block vocabulary; appendix C's terse "of miss" cells left for its own queued audit (no front-running). Ch14 now 5,658 words (+238, all gloss/derivation/on-ramp-bearing), 2.9% over the 5,500 concept cap — logged rather than trimmed, consistent with the ch02 (2.9%) / ch06 (2.5%) / ch04 (5.6%) pedagogy precedent. ch15 graded 4.36/5 — all 15 findings applied 2026-08-28 (zero [LOST]; 7 [CONFUSING] + 8 [POLISH]) plus the advisory worst-gap item (worst-gaps 1 and 3 are findings 2+5 and 1+7, applied as findings): the audit's worst vocab gap — cache write/read, load-bearing in 15.2's quota box and all of 15.3's meter arithmetic but absent from the Words-before-machinery table — closed with one row ("First send of a prompt prefix (counted as fresh input) vs re-send of one the provider already holds (often uncounted at the meter)" / "The first photocopy of a document vs the second"; documented deviation: the audit's "often free" became "often uncounted at the meter" — the chapter's own 15.3 text and ch14's nothing-is-free discipline both forbid "free", which would collide with reads' 0.1× price); the opening's bare 429 glossed at first use ("the 429-class response (`too many requests`, section 15.4)"); 15.4's word collision — the fresh 529-"overloaded" picture against "deliberately overloaded in meaning" one paragraph later — resolved as "deliberately ambiguous in meaning"; the utilization notation split (fraction in 15.2, percent in 15.6) harmonized ("as utilization nears 100% (a fraction of one — chapter 5's ρ)", ch05's own notation grep-verified); the Anthropic 10M-effective claim now shows its division ("only a fifth of input is fresh at that hit rate, so the effective ceiling is 2,000,000 ÷ 0.2" — digest-attested, 2,000,000 ÷ 0.2 = 10,000,000 re-derived); K-of-N glossed at its book-first use ("accept the first K of your N results — any 70 of a 100-wide step, say"; documented deviation: the audit's "any 700 of 1,000" re-anchored to the chapter's own N=100 step example four sentences upstream); the `except` sentence de-codenamed ("both land in the same error handler (`except`, in most languages)", dropping "mid-fanout" — and the audit's premise that fanout is chapter 10's word is a reviewer false positive: grep shows ch10 never uses the term); plus the credential/key standardization (the API expansion moves to the equivalence's first use — "an API (application programming interface) key is just a credential" — then "five extra keys" and "The key is a name tag, not a bucket"), the quota box's burndown pointer ("(burndown arithmetic worked in section 15.3)"), QPS tied to the table ("QPS — the same idea as RPM"), "wire requests" glossed at first use ("actual HTTP calls that leave your machine"), the adaptive-throttle `+ 1` given its why ("only guards against dividing by zero on the first request"), "vertical asymptote" glossed ("the point where the curve goes straight up"), the character-count estimate named as a proxy ("the provider estimates your prompt's token count from its character count" — documented deviation: the audit's "the provider cannot know your token count before reading it" is not digest-attested and states an impossibility the docs do not, so the applied gloss surfaces only the character→token proxy the digest does attest), and the token-bucket row's mixed metaphor cleaned ("A tank that refills continuously; each request drains it" — the picture column owns the water). Worst-gap 2 (advisory, applied): four one-line italic recaps now close each of 15.3's provider paragraphs (OpenAI: reserve first, no forgiveness for failed requests or cached input; Anthropic: two split meters, cache reads bypass the input meter, refilling-bucket burst-trap; Gemini: carry-in only, no output meter, Pacific-midnight reset; Bedrock: output counts multiplied, reservation books up front, cache reads uncounted), giving the density wall its breathing room. Appendix A ripples (the glossary mirrors chapter tables by design): the Token bucket entry re-worded to match the fixed row, and the Cache write / Cache read entries extended with the quota sense and re-tagged (Ch. 14, 15). Ripple greps clean: "overloaded in meaning" / "mid-fanout" / "credit tank" / "K-of-N results" / "nears one" return zero manuscript hits; the later K-of-N uses (ch15's lever table and Build-it, ch16, ch18, appendices B/D) all sit after the gloss site and inherit it. Mermaid blocks byte-identical pre/post (md5-verified) — SKIP_FIGURES validation sound. Ch15 now 6,022 words (raw wc; +231, all gloss/on-ramp/recap-bearing), 9.5% over the 5,500 concept cap — logged rather than trimmed, consistent with the ch13 (12.0%) / ch09 (6.6%) logged-over precedent (beginner abandonment is release-blocking for this book's promise; the cap is not). ch16 graded 4.41/5 — all 8 findings applied 2026-08-28 (zero [LOST]; 6 [CONFUSING] + 2 [POLISH], every one a one-phrase fix): the audit's #1 gap — 16.6's inclusive-vs-exclusive teaching floor arriving field-first — fronted with the bridge sentence ("Some providers fold the discounted part into the grand total; others list it as separate line items. Read the receipt's shape before you multiply", tying the concept to the vocab table's receipt picture); the worksheet's undefined hit-rate variable introduced at its load-bearing moment ("so the hit rate, h, is measured, not assumed"; it recurs in Prove it); "affine" glossed plain ("a straight-line (affine) formula: one term you control, one you rent" — the audit's parenthetical option, keeping the technical term for the expert basement); the 16.2 strategy-name wall glossed per name ("least-busy (pick the idlest wire), latency-based-routing (pick the fastest)" — the wire ties to the switchboard ELI5); the RouteLLM benchmark wall tagged inline at book-first use ("MT Bench (chat), MMLU (multiple-choice knowledge), GSM8K (grade-school math)" — MT Bench and MMLU are book-first uses here, GSM8K's tag a spaced recall after ch09's gloss; the follow-up gloss sentence kept verbatim as emphasis per the audit's design); the hashed model_id implementation detail cut from 16.5 (zero teaching load, interrupts the granularity argument; grep-verified nowhere else — the hash detail stays in LiteLLM's docs, cited in See it); the headroom sentence made self-contained ("the small notes providers staple to every response — the x-ratelimit-remaining-* headers (chapter 15)"); and the Langfuse cliff sentence split in two with the plain restatement as the quoted rule's direct consequence ("one non-OpenAI field in the payload and the normalization no longer applies" — documented wording deviation from the audit's literal "it applies only if", which would have repeated the quote verbatim). No reviewer false positives; ripple greps clean (affine survives only in ch09's quant machinery and the new parenthetical; hashed / scrape-x-ratelimit / identified-by-a-hashed gone book-wide); mermaid blocks byte-identical — SKIP_FIGURES validation sound. Ch16 now 6,393 words (raw wc; +58, all gloss/bridge-bearing) on a base already logged ~11% over the 5,500 concept cap as content-bearing (iterations 20/42) — logged rather than trimmed, consistent with the pedagogy precedent |
| Final proof | Owed | Conditional by design: commercial upload requires a Kindle Previewer pass on the retail file, which no build script can perform |

The copyedit pass closed 2026-08-27. The gate-6 adversarial wave (claims falsifiers + cross-reference audit) landed 2026-08-28: its five P1s are applied, and its sixteen outstanding P2s are settled — twelve applied, one already fixed by the companion pass, two rejected as false positives with evidence, one closed as no-fix by judgment (full record in the table row above). The gate-6 companion attack (same day) found seven P1s and eleven P2s in the money and admission paths — all seven P1s and all ten surviving P2s (the meter-side duplicate fix resolved the eleventh) are applied, with key repros wired into the companion suites and the attack file itself held as regression evidence. The gate-6 clean-checkout build attack (same day, earlier commit) had left three P2 hygiene findings unqueued when the seal landed — the mtime guard for the cover render, a stale README status table, and a Volume-I temp-file prefix — all three applied 2026-08-28 after both queue passes, closing the last gate-6 artifact. The gate-6 queue is now closed in full; the release candidate re-seals with this pass. The pedagogy wave (beginner-simulation audits of every chapter and surface, 2026-08-28) is the current open queue: ch01 through ch16 findings are applied and the remaining files are being worked in chapter order — the candidate re-seals when that queue empties. Final proof (Kindle Previewer) is the one remaining gate before upload.

## F.2 The book as a product — categories, price, position

**The one-line tether.** This book is Volume II of a series, and the comparables say the series tether is the single most shareable sentence you will write. Announce it early and in one line, the way the AI Engineering announcement did ("builds upon [Volume I], but with a focus on [the new scope]") — for this book: *Harness Engineering taught the system around the model; Inference Engineering opens the hood.*

**Categories.** Practitioner AI titles dual-list in Amazon's "Artificial Intelligence" node (491300) and "Natural Language Processing" node (271581011); the cheaper "Generative AI" node (211759007011) is dominated by low-cost self-published titles and is not this book's shelf. Bestseller-list reality from the 2026-08-27 snapshot: trade and business AI books plus practitioner project guides dominate the top-20; technical deep-dives are the minority — which is the point of the wedge, not a defect.

**Price.** The practitioner anchors: $59.99 print list is the established level (Manning and Packt comparables), O'Reilly pushed flagships to ~$79.99, and eBook editions list $47.99–$54.99 with street prices typically 20–30% below list. Self-published AI eBooks cluster at $9.99 or below; that shelf sells volume and reviews, not depth, and pricing this series next to it would mis-signal the content. Kindle royalty mechanics and price bands are not part of this book's research corpus — verify current Kindle Direct Publishing (KDP) terms in the pricing calculator at upload time rather than trusting this page.

**Length.** Comparable practitioner successes run 368 to 534 pages; the corpus conclusion was that this volume need not exceed ~500. The manuscript weighs in north of 110,000 words; the 2026-08-27 copyedit normalized style and terminology but did not mass-trim — chapter overages against the style bands were reviewed and logged as content-bearing (mechanism and both-sides framing were the casualty of cutting), per the progress ledger.

**Sales expectations.** Niche technical books typically sustain ranks in the tens of thousands, which third-party calculators map to roughly 3–15 Kindle-equivalent sales a day — steady backlist, not spike bestsellers. Every such figure is a curve-fit; Amazon publishes no sales data, BSR updates hourly, print ranks follow a lower-velocity curve, and any single snapshot is an order-of-magnitude guide. Budget expectations accordingly: this book wins by being the only one in its lane, not by charting against celebrity titles.

## F.3 Files, cover, and the machine check

What exists and must stay true at upload:

1. **The build is one command.** `tools/build.sh` from a clean tree lints, stages, renders figures, and emits the EPUB; the structural validator must pass on the retail artifact, not a stale one. Rebuild from scratch on release day.
2. **Edition metadata is deliberate.** The build pins the identifier, the ISO publication date, and the title-page edition line; a new edition means bumping those deliberately, not inheriting them.
3. **Cover.** The retail cover ships as `figures/png/cover.jpg`; it was produced through an SVG→PNG pipeline with an ffmpeg fallback and verified opaque RGB. Inspect it once more at full size in grayscale — e-ink is this book's native habitat, and every figure in the interior was held to a no-color-only-meaning rule.
4. **Figures.** All mermaid diagrams (34 at last count) must appear as images in the EPUB with none degraded to source code. The validator checks this; do not upload on a skipped-figures build (`SKIP_FIGURES=1` is a smoke-test convenience, not a release mode).
5. **Reflow — decided 2026-08-27, fixed not ratcheted.** Every reader-facing fenced code line now fits 66 columns (the ch05 and ch07 iteration timelines were redrawn, the ch14 cost formula and the Appendix D tracer excerpt re-set, and the tracer reflowed identically in the companion so book and code still match byte-for-byte). Mermaid sources are excluded from the budget on principle, not convenience: the build replaces those fences with images, so their width never reaches a reader, and the DSL cannot wrap without changing the figure — `--check-mermaid` still measures them (76 lines, widest 158, in the source only). `tools/build.sh` runs the checker at budget 0, so a regression fails the build. Do not upload on a build that had to raise the budget.
6. **Kindle Previewer.** The one check no script performs: open the final EPUB in Kindle Previewer (phone and e-reader profiles) and page through front matter, every chapter opener, the dated boxes, the appendices, and the back matter. This is the final-proof gate the editorial system deliberately leaves conditional.
7. **The sample.** The prologue opens the book by design so the store's free sample lands on the story, not the table of contents. Read the sample exactly as a shopper receives it before setting the book live.
8. **Back matter and copyright page.** The book closes, after the appendices, with the author note, the evidence note, and the copyright page (`zz-back-matter.md`, added 2026-08-27); the copyright page dates the research snapshot and carries the numbers disclaimer. The bio lives at the back by design — the sample should spend itself on story and orientation, not on the author. On a new edition, bump the copyright year and the edition line deliberately.

## F.4 The launch sequence — what the comparables actually did

Six moves, each traced to a documented launch, none requiring luck:

1. **Announce with the tether, early.** The strongest comparable announced roughly nine months before print with nothing but the one-line series definition. The announcement is a promise, not a promotion.
2. **Ship the companion repo at launch.** Both successful O'Reilly comparables have public companion repositories that double as chapter summaries and resource hubs — one also serves as a lead-capture page with endorsement blurbs. This book's companion (the ~700-line tinyengine of Appendix D, with its offline test suite) already exists in private; making it public on launch day is a flip, not a project.
3. **Syndicate one chapter.** Pick the chapter a stranger would finish in one sitting — the arithmetic of waiting and the cache-bill chapter are the natural candidates — and place an excerpt with an established newsletter in release week.
4. **Time the tour to print week.** The comparable launch media (podcasts, newsletter interviews) clustered within weeks of the publication date; the lesson is sequencing, not volume.
5. **Respect Volume I owners.** A proven move in the corpus: the free second-edition extract for first-edition owners. The series equivalent: a clearly-scoped delta note — what Volume II assumes, what it re-teaches, where Volume I readers can skip — priced at zero.
6. **Plan the long tail.** Audiobook roughly five months post-print, translations inside a year, and a place where reader discussion is curated. None of these are launch-day work; all of them are launch-day *decisions*.

One honesty note the corpus enforces: no public pre-order or unit-sales figures exist for any comparable book. "Bestseller" claims in launch marketing are qualitative, and this book's own marketing should not reach for numbers nobody has.

## F.5 Release-day runbook

In order, on one calendar day:

1. Confirm the gate ledger (F.1) is green: Gate-2 sweep closed, copyedit closed, zero open P1s.
2. Clean-tree build; structural validator passes on the retail EPUB.
3. Kindle Previewer pass (F.3 item 6) — phone and e-reader profiles, front to back matter.
4. Reflow decision recorded (F.3 item 5).
5. Metadata: title, subtitle, author, edition line, publication date, description led by the one-line tether — and let the subtitle and series keywords (harness, agents, provider APIs) carry search: a 2026 Baseten book shares this exact title and its free companion site dominates the phrase (positioning research, retrieved 2026-08-27).
6. Categories: AI (491300) primary, NLP (271581011) secondary; stay off the self-pub Generative AI shelf.
7. Price set per F.2 after checking current KDP royalty bands.
8. Companion repository made public; README states what the code is (Appendix D's guide) and where it is referenced chapter by chapter.
9. Announcement post published with the tether sentence and one syndication target booked.
10. Errata policy posted: a public errata page, append-only, matching the project's internal discipline — every fix dated, no silent history rewrites.
11. Calendar reminders created for F.6.

## F.6 After release — the re-dating calendar

A dated book is not a sin; an undated drift is. The maintenance calendar:

| Cadence | Task | Target |
|---|---|---|
| Every pricing cycle (quarterly at most) | Re-date prices, cache multipliers, rate limits | Appendix C |
| Quarterly | Re-verify engine defaults after each major vLLM/SGLang/TensorRT-LLM upgrade | Chapters 5–9 knob values via Appendix E's engine-doc tier |
| On any provider announcement you act on | Re-date the affected matrix row immediately, not at quarter's end | Appendix C |
| Annually | Re-check claimed-vs-effective context windows (RULER-style) and same-weights provider spreads | Chapters 4, 11, Appendix C |
| On any reader-visible error | Errata entry, dated; fix in source; new edition only when accumulated change justifies a new ISBN-scale event | Errata page + progress ledger |

The rule underneath the calendar is the book's oldest one, inherited from the research method in Appendix E: every number carries the date it was true, no number outlives its date unmarked, and the honest ledger — progress, corrections, and all — is part of the product, not an embarrassment attached to it. The engine room keeps logs. So does the engine room's book.
