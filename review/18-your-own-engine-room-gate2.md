## Review — Chapter 18 "Your own engine room" (Gate 2, Technical editor)

**Correct (verified, no action needed):**
- Numbers audit: 16 numeric claims traced to dated digests in `research/`, chiefly `research/local-edge-inference.md` (GGUF quant table 4.34 GB/+0.4685, 4.78/+0.4511, 5.21/+0.1316; Ollama-on-MLX blog claim; arXiv 2511.05502 on M2 Ultra 192 GB; 70B Q4_K_M ≈ 40 GB; 400÷40 ≈ 10 tok/s; Ultra ≈ 2×; M4 Max/llmcheck community figures; H100 $2.39–2.49/hr, A100 $1.49–2.49/hr, 4090 ≈ $1.49/hr, checked 2026-08-02/April 2026; DeepInfra $0.02–$2.85 per 1M; blended $0.60/1M 2026), plus `429-529-retry-behavior.md` and `failure-ownership-by-layer.md` (SRE ~10% retry budget) and `serving-observability-metrics.md` ("no hosted provider surfaces distributions"). All pricing/rates live in three dated boxes, community figures hedged "not vendor-verified". Zero untraceable market numbers.
- Mechanics recomputed (6 derivations, all correct): 720 × $2.49 = $1,792.80 ≈ $1,793 ✓; 720 × $1.49 = $1,072.80 ≈ $1,073 ✓; $1,073 ÷ $0.60/1M = 1,788M ≈ 1,790M, ÷ 2,000 tok/s = 894,167 s ≈ 895,000 s = 34.5% ≈ 35% of a 30-day month ✓; checkpoint Q2: 500 ÷ 4.5 = 111 ≈ 110 tok/s ✓; Q3: $1,073 ÷ $60 = 17.9 ≈ 18× ✓; 10M × $0.60 = $6 ✓. Mermaid `graph LR` block is syntactically valid (no paren/quote hazards in labels; dotted labeled edges fine). All three tables well-formed; checklist numbering 1–13 continuous and its cadence line references valid line numbers (5/10/12/13 nightly; 1/2/7/11 quarterly).
- Frame: ELI5 blocks on all five concept sections (bridge, kitchen, taxi/van, checklist, elevator — all jargon-free); H2s numbered 18.1–18.6; `Where the picture stops` present with four specific breaks; all four closers present; `Words before machinery` table has 12 rows, every term used in-chapter; scope matches all six CHAPTER_MAP beats (assembly, metering, scheduler, routing, local/edge + own-vs-rent, checklist, manifesto); no vendor marketing language (the Ollama claim is attributed with "they say"); cross-references verified in-chapter (ch1 ownership test, ch16 night train/cost-per-completed-task, ch17 five layers, ch10 peak-fragility advice, ch12 client-side TTFT).

**Findings (numbered, quote → replacement → why):**

1. **[P1]** `manuscript/18-your-own-engine-room.md:97` — "> - 70B Q4_K_M on M4 Max 128 GB: roughly 20–28 tok/s — the first consumer chip community guides describe as running 70B at "real-time" speeds; ~10–12 tok/s at Q8; ~43 GB of RAM (random-access memory) in use."
   **Replacement:** "> - 70B Q4_K_M on M4 Max 128 GB: community guides claim roughly 20–28 tok/s — but note that range needs 800–1,120 GB/s by the law above, i.e., Ultra-class bandwidth; the same guides' own tables put Max-class chips nearer 12–16 tok/s. Treat the top of the range as optimistic; ~10–12 tok/s at Q8; ~43 GB of RAM (random-access memory) in use."
   **Why:** The chapter's own arithmetic (line 94: 400 GB/s ÷ 40 GB ≈ 10 tok/s; line 94–95: Ultra ≈ "roughly double the bandwidth" ≈ 800 GB/s) makes 20–28 tok/s on a Max-class chip a physical impossibility — it requires Ultra-class or higher bandwidth. Three paragraphs later line 102 even re-asserts "20–28 tok/s is a *single stream*" under "the bandwidth floor is the ceiling." The digest itself pairs community ~12 tok/s with Max-class chips and reserves ~21 tok/s for the M4 Ultra. The figure is boxed, dated, and hedged (so it passes the letter of STYLE.md), but the section teaches the reader the formula that falsifies its own headline number — the exact failure a technical gate exists to catch. One reconciling clause fixes it without touching the source.

2. **[P2]** `manuscript/18-your-own-engine-room.md:38` — "| Call tracer | Chapter 2 | ~10 | TTFT, inter-token latency, the identity e2e ≈ TTFT + (N−1) × ITL |" and `:188` — "wrap one real call with three timestamps (chapter 2, ten lines)".
   **Replacement:** "| Call tracer | Chapter 1 | ~10 | TTFT, inter-token latency, the identity e2e ≈ TTFT + (N−1) × ITL |" and "wrap one real call with three timestamps (chapter 1, ten lines)".
   **Why:** The tracer is chapter 1's Build-it ("Ten lines of code; you have just built the seed of tinyengine's call tracer", ch1:210), the code header says "Chapter 1" (`companion/tinyengine/tracer.ts:1`), Appendix D's table says "Chapter 1", chapter 8 says "tinyengine's tracer from chapter 1", and chapter 18's own closer says "the tracer (ten lines, chapter 1)" (line 213). The chapter contradicts itself and the rest of the book in two spots. (Appendix D's "D.2 The tracer (chapters 1–2)" is also acceptable wording if the identity attribution is wanted.)

3. **[P2]** `manuscript/18-your-own-engine-room.md:34` — "**tinyengine is roughly seven hundred lines of TypeScript — a tracer, a normalizer, a meter, a scheduler, a router, and a session store — …**" vs the table (lines 36–43, six rows: tracer, normalizer, **CacheLedger**, scheduler, router, store — no meter) vs the diagram (separate `Meter` and `CacheLedger` nodes) vs `:184` — "The instruments you built — tracer, normalizer, meter, ledger, scheduler, router, store" (seven).
   **Replacement:** "**tinyengine is roughly seven hundred lines of TypeScript — a tracer, a normalizer, a ledger that doubles as the money meter, a scheduler, a router, and a session store — …**" and at `:184` "— tracer, normalizer, ledger, scheduler, router, store —" (and optionally merge the diagram's Meter/CacheLedger nodes, as Appendix D's diagram already does).
   **Why:** The chapter counts six instruments but names different sets in different places: the sentence and checklist ("Money lines (meter)") include a meter the table lacks; the manifesto lists seven; the diagram draws a Meter node that maps to no code module — in the shipped companion `cache-ledger.ts` *is* "the money meter" (its own header comment, and Appendix D's diagram). In a chapter whose thesis is "each instrument owns one dial," the instrument roster must be one list.

4. **[P2]** `manuscript/18-your-own-engine-room.md:121` — "at a community-typical sustained ~2,000 tok/s aggregate for a 70B-class model (varies with batch size)" (and `:207` "4. Same A100, sustained 2,000 tok/s aggregate.").
   **Replacement:** "at a community-typical sustained ~2,000 tok/s aggregate for a 70B-class model on H100-class hardware (the digest's assumption; an A100 sustains materially less, which *raises* the breakeven share — varies with batch size)".
   **Why:** `research/local-edge-inference.md` attaches ~2,000 tok/s to the "dedicated H100 at $2.49/hr" scenario; the chapter silently transfers it to the $1.49/hr A100 used in both the section math and checkpoint 4. An 80 GB A100 serving a 70B has roughly a third of the H100's compute, so real breakeven utilization sits well above 35%. The directional conclusion (crossover lands where utilization does) survives, and the figure is hedged, so this is a one-clause honesty fix, not a rewrite.

5. **[P2]** `manuscript/18-your-own-engine-room.md:82` — "The local stack has three names that mean three different things." (followed by four definitions: llama.cpp, GGUF, Ollama, MLX).
   **Replacement:** "The local stack has three names that mean three different things — plus one latecomer."
   **Why:** Four bolded terms follow a count of three; a reader counting bold terms hits four. Smallest fix is one appositive; alternatively "four names."

6. **[P2]** `manuscript/18-your-own-engine-room.md:161` — "chapter 10's MoE peak rule keeps multiplicative budgets small" — MoE never expanded in this chapter; `:38` — "the identity e2e ≈ TTFT + (N−1) × ITL" — `e2e` never expanded before use ("end-to-end" first appears only as "end to end" at line 72, after the table).
   **Replacement:** "chapter 10's MoE (mixture-of-experts) peak rule…" and "the identity e2e (end-to-end) ≈ TTFT + (N−1) × ITL".
   **Why:** STYLE.md requires acronyms expanded at first use; both are single-token fixes. (Every other acronym in the chapter — KV, APIs, TTFT, TTL, CPU, GPU, PII, CI, SEV, TPOT, ITL, SLO, OS, RAM — is correctly expanded.)

7. **[P2]** `manuscript/18-your-own-engine-room.md:34/74/194` — "roughly seven hundred lines" vs shipped companion: tracer 17 + normalizer 153 + cache-ledger 109 + rate-scheduler 111 + router 133 + session-store 113 = **636 lines** (table tildes sum to 720).
   **Replacement:** either keep as-is (Appendix D discloses exact shipped counts and explains "the shipped totals run a little under") or change to "roughly six hundred fifty lines".
   **Why:** 636 is ~9% below the headline; "roughly" plus Appendix D's explicit reconciliation keeps this within the numbers-discipline hedge, but a reader who counts will land on 636. Report-only; no digest violation since Appendix D is the traceable source.

8. **[P2]** `manuscript/18-your-own-engine-room.md:211–229` — closers use bolded paragraphs (`**Build it.** …`) under one H2, while chapters 1–14 use `### Build it` / `### Break it` / `### Prove it` / `### See it in the wild` H3 subsections (chapters 15–17 match chapter 18's bold format).
   **Replacement:** none required in this chapter; decision owed at the copyedit gate — normalize all 18 chapters to one format (H3 subsections match the majority, chapters 1–14).
   **Why:** All four closer beats are present and substantive, so the STYLE contract's content requirement is met; this is a book-wide formatting split (Part III/IV drifted), not a chapter-18 defect. Flag for the copyedit pass recorded as owed in EDITORIAL_SYSTEM.md.

Counts: P0 = 0 · P1 = 1 · P2 = 7
Verdict: MINOR