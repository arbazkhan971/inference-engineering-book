## Review — Gate 2 (Technical editor): `manuscript/16-routing-fallbacks-money-meter.md`

### Correct (verified, with evidence)

**Numbers audit — 18 numeric claims traced to dated digests (all digests dated 2026-08-27):**
1. OpenRouter two-layer failover, `allow_fallbacks: true`, 30-second outage deprioritization, inverse-square-of-price weighting — `research/model-routing-gateways-fallbacks.md` ("Key facts" bullet 5, worked example: $1/$2/$3 → 9×).
2. ~50-minute OpenRouter gateway-database outage, Aug 2025, no uptime % published — same digest, and the chapter hedges it correctly ("evidence of the design's purpose, not its performance").
3. RouteLLM (arXiv 2406.18665): ~95% GPT-4 quality, >85% MT Bench / 45% MMLU / 35% GSM8K savings — same digest, restated with correct per-benchmark scoping of the "more than."
4. Cloudflare `cf-aig-step:0`/`:1` — routing digest + `research/circuit-breakers-llm-ops.md`.
5. LiteLLM strategies (`simple-shuffle` recommended, `least-busy`, `latency-based-routing`), `health_check_interval`, `ignore_transient_errors`, single-instance deployments never cooled, fallback skipping the cooldown check — routing digest.
6. Batch shape: OpenAI 50%/24h-only-window/50,000 requests/200 MB/separate higher limits; Anthropic 100,000/256 MB, most <1 h, 29-day retention, unbilled errored/canceled/expired, 24 h expiry; Google 50%, 24 h SLO, Flex 50% 1–15 min sheddable, Caching up to 90% — `research/batch-api-economics.md`.
7. GPT-4o $2.50/$10.00; Sonnet 4.6 $3/$15, batch $1.50/$7.50 — batch digest.
8. LiteLLM 429 → immediate 5-second-default cooldown; >50%-of-minute trip; `allowed_fails: 3`/`cooldown_time: 30` presented as examples-not-defaults; 401/404/408 non-retryable; `AllowedFailsPolicy` per-class/per-deployment; hashed `model_id` — circuit-breakers digest + routing digest.
9. Portkey `attempts: 5` on [429,500,502,503,504], `failure_threshold`/`_percentage`, `minimum_requests`, 30-second minimum cooldown, `failure_status_codes` default >500, all-open bypass, per-strategy-path, 6×-vs-5-second spread — circuit-breakers digest (the 6× is the digest's own derived note).
10. Usage-bucket conventions: OpenAI inclusive `prompt_tokens`, 0.1× reads "up to 90% off" GPT-5.6+; Gemini `promptTokenCount`/`cachedContentTokenCount`/`thoughtsTokenCount`; Anthropic/Bedrock exclusive 0.1×/1.25×/2× additive buckets; reasoning tokens at output rates — `research/cost-metering-attribution.md`.
11. LiteLLM `response_cost`, `model_prices_and_context_window.json`, gpt-3.5-turbo 1.5e-06/2e-06 USD/token, `register_model`, local-map pin — cost-metering digest.
12. Langfuse `cost_details` > inferred, exclusive normalization cliff, reasoning-token warning; OpenMeter events/subjects/meters/customers — cost-metering digest.
13. Anthropic cache-batch stacking (1.25× of batch base) and daily-reconcile/drift-as-schema-change rule — cost-metering digest.
14. "No hosted provider publishes TTFT percentiles" attributed to ch12 — accurate: ch12:134 states it ("no provider exposes TTFT distributions to customers… checked 2026-08-27"), backed by `research/goodput-dashboard-design.md` and `research/tail-latency-fanout-amplification.md`.
15. Ch14 TTL handoff ("5-minute default, refreshed on read") — `research/prompt-caching-provider-semantics.md` bullet 1.
16. "Tail at Scale" Dean & Barroso, CACM 2013 — Appendix E (ch 15, 16).

**Cross-references verified against actual chapter text:** ch14:170 ends verbatim with "chapter 16 will make you arbitrate" ✓; "chapter 16's job" is verbatim in ch15:129 ✓; ch01:168 ("not a controlled variable… Chapter 16 turns this into routing strategy") ✓; ch02:171 (affine cost formula, "chapter 16's metering will reuse exactly this arithmetic") ✓; ch13:170 (SchemaGuard ↔ ch16 routing table) ✓; ch18:42 lists Router/Chapter 16/~150 lines, matching this chapter's "Roughly 150 lines" ✓.

**Mechanics — recomputed derivations (7, all correct except one label, flagged below):**
- Worksheet interactive column: 500×$3/M×10k=$15.00, 1,500×$3/M×10k=$45.00, 400×$15/M×10k=$60.00, total **$120.00** ✓.
- Batch column: $7.50 + $22.50 + $30.00 = **$60.00** ✓; batch+hits: write 1,500×1.875e-6≈$0.0028 ("≈$0.003" ✓), reads 9,999×1,500×$0.15/M=$2.25 ✓, total **≈$39.75** ✓.
- All 12 xychart points recomputed (linear interpolation between fresh-at-base and read-rate prefix cost at each hit rate): 120→79.50 and 60→39.75 — every point matches ✓; chart uses correct dollar rates.
- Failure lines: per-request batch cost 2,000×$1.50/M+400×$7.50/M=$0.006 ✓; 5%→$3.00, full re-run $60.00 ✓.
- Checkpoint 2: (1/2²)/(1/8²)=16:1 ✓; Checkpoint 3: 5,000×$0.010=$50/$25 ✓; Checkpoint 5: 1.25×$1.50=$1.875/M ✓.
- 16.4 examples: $4.00/$2.00, $5.40/$2.70, $100–$135/night ✓.

**Frame audit:** ELI5 blocks open 16.2–16.6 ✓; `Words before machinery` table (12 terms, three columns, chapter-scoped) ✓; numbered H2s 16.1–16.8 with unnumbered special sections, matching ch14/15 pattern ✓; `Where the picture stops` covers all five analogies ✓; Checkpoint (6 Q&A) ✓; Build it/Break it/Prove it/See it in the wild ✓; dated snapshot box ("The mid-2026 batch sheet") with inline `(retrieved 2026-08-27)`/`(derived)` hedges elsewhere, consistent with ch14's precedent ✓; acronyms expanded (API, GPUs, JSONL/JSON, SLO, TTFT) ✓; no vendor marketing language ✓; scope matches CHAPTER_MAP ch16 (gateways, per-task routing, batch, breakers, cost attribution, 10k worksheet) with no drift into ch17 territory ✓; mermaid state machine and xychart are syntactically valid and grayscale-readable ✓.

### Findings

1. **[P1] Fabricated quotation of chapter 15's ending.** Location: `manuscript/16-routing-fallbacks-money-meter.md:5`. Exact text: `Chapter 15 ended on a handoff: "chapter 16's router will consume exactly this ledger"`. Evidence: the quoted phrase appears nowhere in ch15 (grep across manuscript: only ch16:5). Ch15's actual handoffs are "Chapter 16's router feeds on it" (15:165, a table row) and "chapter 16's router will read its cooldown state" (15:202, Build-it). The sibling quotes in the same sentence are verbatim-accurate ("chapter 16's job" = 15:129; ch14's "chapter 16 will make you arbitrate" = 14:170), so the fix is one clause. Replacement: `Chapter 15 ended on a handoff: "chapter 16's router will read its cooldown state" — and the `RouterRateLimitError` with no `Retry-After` is "chapter 16's job."` Why: quotation marks assert verbatim text; a quote that doesn't exist is an invented fact under EDITORIAL_SYSTEM §2 ("The writer must not invent facts").
2. **[P1] Cache multipliers mislabeled in the interactive-with-hits derivation.** Location: 16.7, "The lane beats the cache." Exact text: `(≈ $79.50 — the same worksheet with reads at 0.3× and writes at 3.75× base, derived)`. Evidence (recomputation): with interactive base $3/M, reads are 0.1× base = $0.30/M and writes 1.25× base = $3.75/M; $15.00 + $4.50 + $0.0056 + $60.00 = $79.51 ✓ — the total and the chart are right, but the stated labels are dollar-per-million prices mislabeled as multipliers. A reader applying "0.3× base" ($0.90/M) and "3.75× base" ($11.25/M) computes ≈$88.52, not $79.50, and the labels contradict the chapter's own 16.6/16.7-stated multipliers (0.1×/1.25×). Replacement: `(≈ $79.50 — the same worksheet with reads at $0.30/M and writes at $3.75/M — 0.1× and 1.25× of the $3 base, derived)`. Why: internal contradiction inside a worked derivation; one-clause fix.
3. **[P1] Batch-file fit arithmetic fails recomputation.** Location: 16.4, worked-arithmetic paragraph. Exact text: `at roughly 2 KB per request the whole matrix fits inside one OpenAI batch file (50,000-request ceiling)`. Evidence: 50 runs × 800K input tokens = 40M input tokens ≈ 160 MB at the standard ~4 bytes/token; at "2 KB per request" that is 80,000 requests — over the 50,000 ceiling. The claim holds only if requests are ~1,000 tokens ≈ 4 KB (40,000 requests, 160 MB). The digest's looser version ("~2 KB JSONL"; 50,000 requests "fit comfortably" in 200 MB; a matrix "often" one job) never ties 2 KB to this 50-run matrix. Replacement: `and at roughly 1,000 tokens (~4 KB) per request the whole matrix sits at OpenAI's batch-file ceilings (50,000 requests / 200 MB) — split it across two jobs rather than trust the fit`. Why: a derived numeric claim contradicted by its own arithmetic under the standard token↔byte heuristic; STYLE.md numbers discipline ("show the arithmetic as reusable arithmetic").
4. **[P2] RouteLLM commercial-router comparison restated with flipped base.** Location: 16.3. Exact text: `comparisons against commercial routers found those **more than 40% more expensive** at similar quality`. Evidence: digest says "commercial-router comparisons were **>40% cheaper** at similar quality" — i.e., commercial is ~>67% more expensive, not >40%; both direction and magnitude drift from the traced source. Replacement: `comparisons against commercial routers found RouteLLM **more than 40% cheaper** at similar quality`. Why: numbers must match the digest, not an inversion of it.
5. **[P2] Ambiguous tail-law cell in the worksheet table.** Location: 16.7 table, "Latency" row. Exact text: `wave-paced; waits on the 1-in-10,000 tail draw`. Evidence: with the stated 1% per-call slow-draw rate, ~100 of 10,000 children are slow, so "1-in-10,000" reads as a probability and contradicts the paragraph directly above. Replacement: `wave-paced; waits on the slowest child of the 10,000`. Why: prevents the reader from computing 1/10,000 odds.
6. **[P2] Jargon inside an ELI5 block.** Location: 16.6 ELI5. Exact text: `to "the summarizer spent $2,100, mostly on cache writes after the Tuesday deploy."` Evidence: STYLE.md — "No jargon inside the ELI5 block"; "cache writes" and "deploy" are unexplained machinery terms (cache writes are defined in ch14, not here, and not in this chapter's Words-before-machinery table). Replacement: `to "the summarizer spent $2,100 — most of it surcharges for re-sending text it had already seen."` Why: hard style rule; the other four ELI5 blocks are clean.
7. **[P2] "≈ 99.99999%" understates a ~1−10⁻⁴⁴ quantity.** Location: 16.7 intro. Exact text: `1 − 0.99^10000 ≈ 99.99999% of runs`. Evidence (recomputation): 0.99^10000 = e^(−100.5) ≈ 2.2×10⁻⁴⁴, so the probability rounds to 100% at 42 decimal places; "99.99999%" is a floor, not an approximation. House-consistent with Appendix B ("at N = 10,000, 99.99999% (derived)"), so keep the convention but tighten the operator. Replacement: `1 − 0.99^10000 ≥ 99.99999% of runs`. Why: recomputed derivation; "≥" is airtight, "≈" is false. (Appendix B should follow in copyedit — outside this chapter's diff.)
8. **[P2] `usage-based-routing` description drops RPM.** Location: 16.2 bullet list. Exact text: `` `usage-based-routing` (by cost or by tokens-per-minute consumption) ``. Evidence: digest says "by cost or TPM/RPM usage" — requests-per-minute is silently dropped. Replacement: `` `usage-based-routing` (by cost or by tokens/requests-per-minute usage) ``. Why: trace fidelity to the dated digest.
9. **[P2] Dangling "2013" anchor.** Location: 16.2. Exact text: `One more input the gateway did not have in 2013: **headroom telemetry.**` Evidence: nothing in 16.2 anchors 2013 (Tail at Scale appears only in 16.7's wild list), and LLM gateways did not exist in 2013, so the sentence reads as a false historical claim. Replacement: `One more input the resilience canon never had: **headroom telemetry.**` Why: unanchored date invites a fact-check it fails.

### Verdict

The chapter is structurally complete, fully framed per STYLE.md, and numerically disciplined: 18 claims traced to dated digests, cross-chapter handoffs verified against actual sibling text, and seven worked derivations recomputed with only one label error. All three P1s are single-clause text fixes — no restructuring, no missing sections, no untraceable headline numbers. Fix the three P1s (and ideally the P2s) in one small pass, then this chapter clears Gate 2.

Counts: P0 = 0 · P1 = 3 · P2 = 6
Verdict: MINOR