## Review — Gate 2 (Technical editor): `manuscript/appendix-d-tinyengine-companion.md`

**Verified correct (evidence):**
- Module table line counts match the shipped source (hand-counted): `tracer.ts` 20, `stream-normalizer.ts` 153, `cache-ledger.ts` 109, `rate-scheduler.ts` 111, `router.ts` 133, `session-store.ts` 114; the "roughly seven hundred lines" opener matches ch18's sentence and the "lands a little under the sum" framing (estimates sum 720, shipped 640).
- All money math checks out and traces: $0.645 = $0.375 write + 9 × $0.030 reads; $3.00 uncached; break-evens ceil(0.25/0.9) = 1 and ceil(1/0.9) = 2 — traced to `research/prompt-caching-provider-semantics.md` (retrieved 2026-08-27) and ch14 §14.3.
- Provider meter arithmetic traced to `research/rate-limit-quota-architectures.md` (retrieved 2026-08-27): OpenAI `max(max_tokens, character-estimate)`; Bedrock `input + cache-write + max_tokens` up front with burndown re-credit; Anthropic cache-read ITPM exemption. LiteLLM 5-second default → `research/circuit-breakers-llm-ops.md` (fetched 2026-08-27). Retry canon (full jitter `random(0, min(cap, base·2^attempt))`, `Retry-After` floor, 3-attempt/3× cap, ~10% retry budget, spend-cap "regain access", billing fail-fast) → `research/429-529-retry-behavior.md`. Batch "up to 24 h late" → `research/batch-api-economics.md`.
- Code excerpts in D.2/D.4 are verbatim against `tracer.ts` / `cache-ledger.ts:cost()`. All 19 named smoke cases exist in `tests/smoke.ts`; the cadence suite replays the three CLIs over fixtures; `ex-011` exists in `fixtures/golden-results.jsonl`; the three CLI invocations match `package.json` `cadence` and `dist/`; CLI+plumbing ≈ 339 lines ("about 340" ✓).
- Chapter cross-references verified: ch1 ten-line tracer; ch12 meta-skip/parse-once/unknown-finish lessons; ch14 first-class metric, TTL clock from `requestStart`, budget-gated keep-alive; ch15 four parts, zombie fleet, burst trap; ch16 daily reconcile, classified-error Break-it, all-open bypass; ch17 byte-exact test, idle taxonomy, spawn isolation; ch18 wiring order, one-dial/no-uninstrumented-crossings, kill-each-instrument; ch9 golden-set field note; ch13 tier ladder; ch6 deploy-as-cache-herd event.
- Style scan: US spellings, spaced em-dashes, "fanout" (not "fan-outs"), no `tokens/s` variants applicable, no vendor marketing language; every durable number traces to a dated digest or a chapter's dated box.

**Findings:**

1. **[P1]** `appendix-d-tinyengine-companion.md:86` — Quote: "The renderer serializes the five layers in the frozen order — template header, tools, system, static context, transcript, tail". Replacement: "The renderer emits a template header banner, then the five layers in the frozen order — tools, system, static context, transcript, tail". Why: the sentence names six items as "five," contradicting ch17 ("serializes the five layers deterministically (tools → system → static context → transcript → volatile tail)", `17-cache-aware-harness-design.md:234`), the code comment (`session-store.ts`: "five layers in ch. 17's frozen order — tools → system → static → transcript → tail"), ch18's diagram ("five layers, byte-frozen"), and this appendix's own D.1 node ("renders 5 layers").

2. **[P1]** `appendix-d-tinyengine-companion.md:95` — Quote: "Three `node:` built-ins are used (`crypto` for hashing, `assert` for tests, `fs` for fixtures)". Replacement: "Four `node:` built-ins are used (`crypto` for hashing, `assert` for tests, `fs` for fixtures, `process` for argv/exit in the CLIs)". Why: checkably false against the companion — `cadence-io.ts`, `golden-set.ts`, `cache-hit-gate.ts`, and `invoice-reconcile.ts` all import `node:process`, and `env.d.ts` shims four modules (`node:crypto`, `node:assert/strict`, `node:fs`, `node:process`). A reader counting the shims finds four.

3. **[P1]** `appendix-d-tinyengine-companion.md:13,59` — Quote: "one event grammar for four provider grammars" / "raw SSE lines from any of the four grammars (`openai-chat`, `openai-responses`, `anthropic`, `gemini`)". The appendix (correctly, per the code's `Provider` union) says four, but chapter 12 — the chapter D.3 cites — says three at four call sites: "Three grammars on one wire" (§12.2 heading, `12-the-streaming-contract.md:30`), "three grammars, one normalizer" (:60), "the three SSE grammars" (:66, a box that then lists all four including Gemini), and "any of the three grammars" in its Build it (:174). Replacement (choose one side): fix ch12's four "three" → "four" (preferred — the box already enumerates four), or bridge in D.3: "raw SSE lines from any of the four grammars (`openai-chat`, `openai-responses`, `anthropic`, `gemini` — all four listed in chapter 12's dated snapshot box)". Why: a count mismatch on the exact seam the appendix cites; the copyedit gate claims terminology uniformity.

4. **[P2]** `appendix-d-tinyengine-companion.md:78` — Quote: "the burst-trap test in the suite proves a 60-per-minute bucket refuses a 60-in-second-one launch". Replacement: "…proves a 60-per-minute bucket refuses the 61st token of a second-one burst (59 admitted instantly, the next reservation refused until the refill readmits it)". Why: off by one against the cited test — `tests/smoke.ts` uses `new TokenBucket(60, 1, 0)`, admits `tryAcquire(59, 0)`, refuses `tryAcquire(2, 0)`, readmits at t=1.1; a capacity-60 bucket admits exactly 60 unit requests in second one.

5. **[P2]** `appendix-d-tinyengine-companion.md:78` — Quote: "Anthropic splits input/output meters and exempts cache reads". Replacement: "Anthropic books the input meter with cache reads exempt (the output meter's OTPM cap is declared in `QuotaMeters` but not yet debited — add an output bucket before relying on it)". Why: the shipped `QuotaLedger` builds only `rpm` and `tpm` buckets and never reads `otpm`; output tokens are never booked, so the ledger does not encode the input/output split the sentence credits it with (ch15's Build-it has the same phrasing — same seam).

6. **[P2]** `appendix-d-tinyengine-companion.md:59` (and diagram `:29` "4 events") — Quote: "Output: the four internal events — `text_delta`, `tool_call_delta`, `usage`, `stop_reason`". Replacement: append "— plus two finish-time markers the accumulator emits (the assembled `tool_call`, and `incomplete_call` when arguments do not parse)". Why: matches ch12's "four internal event types," but the code's `Event` union has six members and the Portland test consumes the assembled `tool_call` event the appendix never names; the appendix is the code-facing artifact.

7. **[P2]** `appendix-d-tinyengine-companion.md:59` — Quote: "`ttft_seconds` on the first content delta". Replacement: "`ttftSeconds` on the first content delta". Why: backticked as code, but the code property is `ttftSeconds` (`stream-normalizer.ts`); ch12 carries the same snake_case — flag for that chapter's pass too.

8. **[P2]** `appendix-d-tinyengine-companion.md:16` — Quote: "every interface this table's chapters named exists in the code, under the name the chapter used". Replacement: "…every interface this table's chapters named exists in the code — the scheduler's four parts ship in `rate-scheduler.ts` under their own names" (or add a thin `RateScheduler` facade). Why: ch15 ("tinyengine's `RateScheduler`") and ch18 ("Add the RateScheduler") name an aggregate that exists only as the file `rate-scheduler.ts`; no `RateScheduler` symbol exists, so the absolute "under the name the chapter used" has one symbol-level miss.

9. **[P2]** `appendix-d-tinyengine-companion.md:9,31,55,59,61,97` — Acronyms unexpanded at first use (STYLE.md: "Acronyms expanded at first use"): TTFT (:9), TTL (:31 diagram, :74), ITL (:55 — "inter-token latency" is spelled in the table but the acronym is never tied), SSE (:59), WHATWG (:61), CLIs (:97). Replacement (one parenthetical each): "TTFT (time to first token)", "TTL (time to live)", "mean inter-token latency (ITL)", "SSE (server-sent events)", "WHATWG (the web-standards body behind the SSE spec)", "CLIs (command-line tools)". Why: the appendix is a reference shelf a reader may enter directly.

Counts: P0 = 0 · P1 = 3 · P2 = 6
Verdict: MINOR