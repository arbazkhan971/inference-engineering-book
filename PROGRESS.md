# PROGRESS — append-only ledger

- 2026-08-27 — repo scaffolded; architecture (GOAL/CHAPTER_MAP/STYLE/EDITORIAL_SYSTEM) locked; research fanout launched (W1: serving internals, 24 digests).
- 2026-08-27 — iteration 2 (research gap-fill): 14 new dated digests (roofline/arithmetic intensity, decode bandwidth wall, attention cost scaling, KV-bytes formula w/ per-model math, GQA/MQA/MLA, context-window claims vs effective, GPU memory hierarchy, continuous batching, PagedAttention, prefix caching/radix, chunked prefill + PD split, speculative decoding, quantization menu, sharding/MoE). research/ 10 → 24 digests; W1 wave complete; Part I (ch1-4) + Part II engine chapters (ch5-10) now fact-covered. Note: glm-5.3-flash fanout hit spawn 429s at 14-wide; retried in batches of 3. 10 of 14 researcher outputs returned text not files; recovered verbatim from run artifacts. No chapter drafted this iteration (per gap-fill rule). Next: draft ch01.
- 2026-08-27 — iteration 3 (research gap-fill, still <60 digests): 18 new dated digests via 6 sequential waves of 3 glm-5.3-flash researchers (ch01 residuals: inference-engineering-discipline, same-model-different-providers; ch11: context-parallelism-long-context, context-compaction-tradeoffs; ch13: constrained-decoding-grammars, provider-structured-output-apis, structured-output-costs-tension; ch14: prompt-caching-provider-semantics, cache-hit-math-agent-loops, cache-breakpoint-design; ch15: rate-limit-quota-architectures, 429-529-retry-behavior, client-rate-scheduling; ch12: streaming-transports-normalization; ch17: session-resumption-cache-rehydration; ch16: model-routing-gateways-fallbacks, batch-api-economics; ch18: local-edge-inference). research/ 24 → 42 digests; every chapter ch1-18 + prologue now has at least one fact-bearing digest. Operational notes: waves of 3 avoided spawn 429s entirely; 13/18 researcher outputs again returned text not files — recovered verbatim from workflow artifact dir (subagent-artifacts/outputs/0ac824c2.../<slug>.md) and copied to research/. Structure validated (H1 + researched-line + 4 sections + 5-10 sources, 837-1207 words each); spot-checks confirm dated numbers and visible hedges (community benchmarks marked approximate, derived arithmetic labeled). No chapter drafted this iteration (per gap-fill rule); 42 < 60 so iteration 4 = final gap-fill wave (deepen weakest: ch02 tokenizer edge cases, spec-decode acceptance data, MoE routing detail, goodput dashboards) unless corpus reaches 60.

- 2026-08-27 — iteration 4 (research gap-fill, final wave): 18 new dated digests via 6 sequential waves of 3 glm-5.3-flash researchers in one async workflow (42 → 60 digests; target reached). Topics: ch2 depth (tokenizer-numbers-edge-cases, decode-time-budget-arithmetic, provider-latency-snapshot-2026), engine depth (batching-size-latency-tradeoffs, preemption-recompute-swap, ttft-queueing-under-load, spec-decode-acceptance-data, moe-routing-serving-detail, quantization-quality-benchmarks), API contract (tool-call-delta-streaming, cancellation-timeout-semantics, sampling-params-provider-defaults), ops/routing (circuit-breakers-llm-ops, tail-latency-fanout-amplification, cost-metering-attribution, load-testing-llm-endpoints), harness (subagent-context-isolation-cache, goodput-dashboard-design — the latter two per iteration-3 prescription: goodput dashboards + subagent cache design). All 60 digests validate: H1 + dated researcher line + 4 sections + 5-10 sources, 828-1178 words, spot-checks show dated numbers, labeled derived arithmetic, explicit hedges where no primary source exists (e.g. provider-latency snapshot flags JS-rendered leaderboards as unextractable; spec-decode flags structured-output interference as "directionally known, not quantified"). Operational note: again 17/18 children returned text not repo files — child `write` tool redirects to the run artifact dir (~/.pi-infbook/sessions/<project>/subagent-artifacts/outputs/<runId>/<slug>.md); all recovered verbatim from there, no regeneration needed. Waves of 3: zero spawn 429s. No chapter drafted this iteration (per gap-fill rule); corpus at 60 so iteration 5 = draft ch01.

- 2026-08-27 — iteration 5 (chapter draft): research/ at 60 digests (≥60, gap-fill not triggered), so drafted ch01 → `manuscript/01-what-inference-engineering-is.md` (5,086 words). Covers all CHAPTER_MAP beats: model/serving/harness three-layer model with ownership test + failure table; full request lifecycle hop map (serialize→connect→admit→route→queue→prefill→decode→stream→settle) with mermaid hop map + layer-stack diagram; Words-before-machinery table (13 terms); book vocabulary map + durable equation; tinyengine intro. Sources: inference-engineering-discipline, lifecycle-of-an-llm-request, failure-ownership-by-layer, inference-vs-training, same-model-different-providers, serving-engines-overview, provider-api-anatomy, latency-vocabulary digests. Gate-1 self-review performed: structure checks pass (5 ELI5 blocks, Where-the-picture-stops ×5 breaks, Checkpoint teach-back, Build/Break/Prove/See closers, no forbidden language); numbers audit — every figure traced to a dated digest, derived math labeled (8B/H100 4.8 ms/token floor), dated snapshot boxes for provider spread + job postings; 6 fixes applied during review (TLS/KV/TPU/BF16/FP8 acronym expansions at first use, INT4 both-sides quality tradeoff, incident date phrasing correction). Known residual: latency vocabulary deliberately shallow here (TTFT/TPOT defined one-line) to avoid stealing ch02's scope; forward pointers added to ch2/4/14/16. tools/lint-manuscript.py referenced by EDITORIAL_SYSTEM.md does not exist yet — followed declared rules manually; lint script needs creating in a later iteration. Next: draft ch02 (The shape of a token).
- 2026-08-27 — iteration 6 (chapter draft): research/ at 60 digests (≥60, gap-fill not triggered), so drafted ch02 → `manuscript/02-the-shape-of-a-token.md` (5,424 words). Covers all CHAPTER_MAP beats: tokenization as learned per-vendor compression (BPE mechanism, dated vocab-size snapshot 50k–256k across 9 vendors, Claude's unpublished vocab + count-tokens endpoint, ~30% Claude 4.7+ tokenizer shift); token shapes at the edges (tiktoken 3-digit grouping vs single-digit families, comma place-value alignment, CJK 2–3×/char, code formatting 24.5% input reduction, strawberry/EMNLP-2025 side-channel framing); autoregressive serial decode (Leviathan "K serial runs", prefill-parallel vs decode-serial mermaid, bandwidth floor: 8B/BF16/H100 16 GB ÷ 3.35 TB/s ≈ 4.8 ms/token ≈ 208 t/s ceiling, labeled derived; Pope 29 ms/token); the four clocks (TTFT/ITL/TPOT/e2e with sourced definitions, the identity e2e ≈ TTFT + (N−1)×mean ITL with worked math, dated mid-2026 speed snapshot 365→39 t/s with derived ITLs, TTFT- vs TPOT-dominated regimes, reasoning-model first-visible-token trap with GLM-5.3 1.6/30.1/7.5 s split); the decode-time inequality (e2e ≈ TTFT + N×TPOT, xychart linearity plot, MLPerf v5.0 dated box, budget inversion N ≤ (D−TTFT)×P, max_tokens = floor((deadline−p95TTFT)/p95TPOT), agent-chain multiplication 5×500×40ms=100s, lever-to-chapter table, cost-twin symmetry). Sources: tokenizer-fundamentals, tokenizer-numbers-edge-cases, latency-vocabulary, decode-time-budget-arithmetic, provider-latency-snapshot-2026, inference-vs-training digests. Gate-1 self-review performed: structure pass (5 ELI5 blocks, Words-before-machinery ×12 rows, Where-the-picture-stops ×6 breaks, Checkpoint ×6 questions, Build/Break/Prove/See closers, 2 mermaid diagrams, 1 Field note, no forbidden language); numbers audit — every figure traced to a dated digest, derived math labeled, dated boxes for vocab sizes + speed medians + MLPerf SLOs; 8 fixes applied during review (acronym expansions: Hugging Face/EMNLP/pass@1/TPU/UX/CJK; "~200+ GB"→"hundreds of gigabytes" hedge; TTFT hop-count corrected 4→5 against ch01's nine-hop lifecycle). Known residual: bandwidth-floor arithmetic deliberately preview-only here (full roofline deferred to ch03 per chapter map); UX thresholds and CJK multipliers carry explicit approximate/community hedges (no primary sources exist). tools/lint-manuscript.py still does not exist (carried from iteration 5) — Gate-1 followed EDITORIAL_SYSTEM declared rules manually. Next: draft ch03 (The arithmetic of waiting).
- 2026-08-27 — iteration 7 (chapter draft): research/ at 60 digests (≥60, gap-fill not triggered), so drafted ch03 → `manuscript/03-the-arithmetic-of-waiting.md` (5,773 words). Covers all CHAPTER_MAP beats: compute-bound vs bandwidth-bound (kitchen ELI5, training-vs-serving MFU contrast 46.2%/41–43% vs ~0.3% derived, floor arithmetic 8B→4.8ms→208 t/s and 70B→42ms→24 t/s); arithmetic intensity as one ratio (GEMM vs GEMV shapes, batch-1 decode AI ≈ 1 FLOP/byte derived); roofline without tensors (min() formula, ridge point = peak FLOPs ÷ peak bytes, xychart of H100 roofline with knee at 295, dated chip-tilt table H100/H200/B200/MI300X with derived ridge points 295/210/292/245, 0.7-efficiency rule, single-stream levers); the batch dial (AI ≈ B, batch 64 → 214 TFLOP/s derived, vLLM 1,500–2,500 aggregate vs 50–100 single-stream, MLPerf 17.5k vs 3k vendor-quoted, KV-vs-weight crossover B×KV ≈ weights worked for Llama 3.1 70B: B≈220 @1k, B≈7 @32k, derived); why 1M tokens is not linear (dense 2PN linear vs attention N² quadratic, c·N² + c·(NM + M²/2), 8× vs 61–64× derived, Qwen3 8B 144 KiB/token → 18 GB @128k, Gemini tiered-pricing dated box as market evidence, cached reads 1/10); memory pyramid (4 tiers, ~10×/1000× steps, L2 50–60 MB, FlashAttention fusion: exact, O(N²) FLOPs unchanged, memory O(N²)→O(N), BERT-large 15%, GPT-2 3×). Harness angle: bytes/s-not-FLOPS capacity planning, lever-to-chapter table, 6pm-crawl field note (TPOT-degrades-TTFT-flat diagnosis). Sources: arithmetic-intensity-roofline, decode-bandwidth-wall, gpu-memory-hierarchy, attention-cost-scaling, inference-vs-training, kv-cache-bytes-formula (KV formula deferred to ch04 as planned) digests. Gate-1 self-review performed: structure pass (6 ELI5 blocks, Words-before-machinery ×12 rows, Where-the-picture-stops ×6 breaks, Checkpoint ×6 questions, Build/Break/Prove/See closers, 1 xychart diagram, 1 field note, no forbidden language); numbers audit — every figure traced to a dated digest, derived math labeled (ridge points, crossover batches, 0.3% utilization, 61×, 18 GB, A100 int4 floor), dated boxes for chip table + provider tiering; 12 fixes applied during review (chip table moved out of blockquote to dated-caption table for EPUB safety; unified 0.3%-of-peak figure; expanded BF16/TFLOPS/TPU/TPOT/p99/MLPerf at first use; removed one untraceable time claim; cut 3× duplicated single-stream-lever statements to 1; trimmed intro/closers; repaired 2 paragraph-break corruptions + 1 formula duplication introduced mid-edit). Known residuals: (1) 5,773 words is ~5% over the 3,000–5,500 concept-chapter target — judged content-bearing (four CHAPTER_MAP beats each requiring ELI5+mechanism+numbers), logged rather than cut; (2) xychart-beta render needs verification at build time (first used ch02, still no build); (3) tools/lint-manuscript.py still does not exist (carried from iterations 5–6) — Gate-1 followed EDITORIAL_SYSTEM declared rules manually. Next: draft ch04 (The memory that is not the model).
- 2026-08-27 — iteration 8 (chapter draft): research/ at 60 digests (≥60, gap-fill not triggered), so drafted ch04 → `manuscript/04-the-memory-that-is-not-the-model.md` (5,467 words). Covers all CHAPTER_MAP beats: what the KV cache stores (weights-as-shared-recipe vs per-session notes; K/V permanence; why decode is only cheap because of the cache); the memory formula (bytes/token = 2 × layers × KV heads × head dim × bytes, walked on Llama 3.1 8B = 128 KiB; dated per-model table 8B/70B/Qwen3-8B/gpt-oss-120b/DeepSeek-V3 at 8k/32k/128k with Qwen 40,960-native-max and gpt-oss sliding-window caveats; linear-growth xychart; OPT-13B 800 KB/token historical contrast); context windows as memory products (admission arithmetic GPT-5 400k/272k; capacity formula sessions = (usable HBM − weights − workspace) ÷ (KV/token × context) worked for 15→3 sessions at 32k→128k, FP8 doubling, gpt-oss-120b weights-bound flip; mid-2026 dated provider-window box incl. Gemini >200K price-doubling as market evidence; RULER claimed-vs-effective with dated examples + lost-in-the-middle pointer to ch11); architects' levers (MHA→MQA→GQA→MLA→sliding-window ladder with papers and per-variant cache ratios, DeepSeek MLA 576-element/~60× arithmetic, Gemma 3 5:1 windowing); overflow behavior (single-stream droop 208→165→101 tok/s at 8k/32k/128k derived from weights+KV payloads; vLLM RECOMPUTE preemption default + counter + stall symptoms; FP8 KV preview with dated 14.9%/54%/~7k break-even numbers deferred to ch9). Sources: kv-cache-bytes-formula, attention-variants-kv, context-window-claims, paged-attention-block-tables (OPT figure + 20–38% waste pointer only), decode-bandwidth-wall (crossover recall), preemption-recompute-swap, quantization-menu digests. Gate-1 self-review performed: structure pass (5 ELI5 blocks, Words-before-machinery ×12 rows, Where-the-picture-stops ×6 breaks, Checkpoint ×6 questions, Build/Break/Prove/See closers, 1 xychart, 1 field note, no forbidden language); numbers audit — every figure traced to a dated digest, all derived math labeled (33 GB opening, droop series, 3-session floor, ~60× MLA), avoided decode-bandwidth-wall's "20 GB at 32k" community figure because it conflicts with config-derived 10 GiB (Llama-3.1-70B GQA). 12 fixes applied during review (acronym expansions GPU/FP16/MTok/MXFP4/Prometheus at first use; ELI5 accuracy one-tenth→sliver and Polaroid→photograph; ladder-table gpt-oss dedup; removed reader-facing "digest" term from prose; FLOPs→arithmetic; feature-request phrasing; "a intelligence" grammar). Scope discipline: paging/fragmentation/prefix-caching detail, quant menu, cost-curve worked example, and position/compaction design all pointed to ch6/9/11/14/17 rather than stolen. Known residuals: (1) xychart-beta render needs build-time verification (carried since ch02); (2) tools/lint-manuscript.py still does not exist (carried since iteration 5) — Gate-1 followed EDITORIAL_SYSTEM declared rules manually. Next: draft ch05 (Batches: the engine's trick).
- 2026-08-27 — iteration 9 (chapter draft): research/ at 60 digests (≥60, gap-fill not triggered), so drafted ch05 → `manuscript/05-batches-the-engines-trick.md` (4,996 words). Covers all CHAPTER_MAP beats: static → dynamic → continuous batching (charter-bus → shuttle-departure → city-bus-line ELI5 ladder; padding + straggler wastes with 80% illustrative and Orca's measured padded-slot finding; dynamic batching fixes admission only); iteration-level scheduling (ORCA OSDI 2022 retire-EOS/admit/refill loop as mermaid + ASCII static-vs-continuous timeline, 36.9× vs FasterTransformer at equal latency, selective batching = batch the linear layers, attention per-sequence; TRT-LLM "in-flight batching" naming); the two scheduler knobs (max_num_seqs V1 default 128, max_num_batched_tokens, worked token-budget example 8,192/64-decodes/8,128-prefill-chunk labeled illustrative, chunked-prefill decodes-first with pointer to ch7, batch_wait_max_tokens_ratio as deliberate queue-latency-for-utilization trade); what batching does to your latency (tokens/s/request ≈ 1/iteration_time; dated TRT-LLM Llama-3.3-70B tuning box 64/512/2048 → 1,944/2,467/2,044 tok/s with flat ~14.5ms ITL, untuned 1,564 tok/s @ 31.3 ms → tuned 2,474 @ 14.7 ms = 58.2%/53.1%); when batching helps (straggler-tax removal, price subsidy, warm engine — both-sides rule); saturation queueing (TTFT = queue wait + prefill with vLLM metric split; M/G/1 E[W]=λE[S²]/2(1−ρ); 1/(1−ρ) multiplier table 2×/5×/10×/20×/100× labeled classical; 0.8→0.95 ≈ 4× derived; E[S²] variability → max_tokens clipping as queueing instrument; prefill linear in batch Llama-2-7B/A100; memory caps concurrency at 49 req @1,280 tok on one A100 → ch4 preemption pointer); goodput (DistServe definition with bounds-in-name, 10-vs-3 req/s example, vLLM --goodput CLI, "throughput optimized by everyone, experienced by no one", schematic xychart labeled not-measured, Sarathi-Serve 2.6–5.6× one-line preview to ch7); harness controls (client concurrency as admission controller at measured plateau per Red Hat 2026-03-03, TPOT-keyed timeouts = expected_output_tokens × p99 TPOT + margin, p99-TTFT first-chunk timeout + one backoff budget, multiplicative shedding, 20-way subagent fanout warning, concurrency-doubling field note). Sources: continuous-batching, batching-size-latency-tradeoffs, ttft-queueing-under-load, preemption-recompute-swap (recall only), serving-engines-overview, goodput-and-slos digests. Gate-1 self-review performed: structure pass (5 ELI5 blocks, Words-before-machinery ×12 rows, Where-the-picture-stops ×5 breaks, Checkpoint ×6 questions, Build/Break/Prove/See closers, 1 mermaid graph + 1 labeled-schematic xychart + 1 ASCII timeline, 1 Field note, no forbidden language); numbers audit — every figure traced to a dated digest, derived math labeled (1.21× sweet-spot ratio, 4× queue multiplier, iteration-time identity), dated boxes for TRT case study + engine knobs; promissory notes from ch02 (batching bends TPOT for others' throughput — fulfilled in 5.6) and ch03 (scheduler machinery that turns the dial — fulfilled in 5.4) discharged; scope discipline: vLLM 24×/PagedAttention/radix left for ch06, DistServe/Sarathi disaggregation + chunked-prefill depth left for ch07, 429/backoff machinery left for ch15. 9 fixes applied during review (promissory-note phrasing, TGI/IFB/ITL/CLI/429/A100/GPU expansions at first use, ITL spelled out in dated box). Known residuals: (1) xychart-beta render still needs build-time verification (carried since ch02); (2) tools/lint-manuscript.py still does not exist (carried since iteration 5) — Gate-1 followed EDITORIAL_SYSTEM declared rules manually. Next: draft ch06 (Paging the brain).
- 2026-08-27 — iteration 10 (chapter draft): research/ at 60 digests (≥60, gap-fill not triggered), so drafted ch06 → `manuscript/06-paging-the-brain.md` (5,354 words). Covers all CHAPTER_MAP beats: PagedAttention and block tables (hotel-unbroken-row ELI5 → front-desk ledger ELI5; contiguous max-length allocation as rational-but-wasteful; 4-step mechanism with logical blocks/physical slots/block table/gather; mermaid two-request diagram showing non-contiguity + shared prefix slots; copy-on-write with refcounts, beam search −37.6–55.2% Alpaca / −44.3–66.3% ShareGPT peak KV); fragmentation (three wastes: internal reserved-tail 75% of 1.6 GB, external holes, duplication of shared system prompts; measured 20.4–38.2% useful = 60–80% waste, vLLM paper 2023; waste-as-missing-concurrency framing tied to ch05's 49-request memory cap); prefix caching and radix trees (determinism license "won't change model outputs"; vLLM hash chain H(parent, tokens, LoRA ID, mm hash, cache_salt) with inline chain illustration, SHA-256 default since v0.11, refcount protection, V1 block pool O(1) LRU with reverse-order requeue; SGLang RadixAttention leaf-first eviction preserving hot trunk; vLLM-doc worked example block-size-4, 14-token prompt vs 10-token share → 8 tokens skipped; only-full-blocks + positional-invalidation lessons; semantic-vs-exact-match distinction with waiter ELI5, GPTCache labeled application-layer risk); same-prompt-twice economics (three regimes: self-hosted free-but-capacity-priced, OpenAI automatic 50% best-effort, Anthropic explicit 1.25×/2× writes + 0.1× reads, 4 breakpoints, 512–4,096 min cacheable, 5-min TTL from request start with stream-time burn — mid-2026 dated box, ch14 owns worksheet; providers-price-position rule with 10× derived spread; frozen-first/hot-last assembly discipline; deploys-as-cache-herd-events mechanism-derived; request_id-at-top field note). Harness controls (freeze-order assembly, nondeterminism stripping, hit-metric watching, tradeoff budgeting; lever table → ch10/11/14/17/18). Sources: paged-attention-block-tables, prefix-caching-radix-trees digests + recalls from kv-cache-bytes-formula/ch04 (800 KB/token OPT-13B, 128 KiB Llama 3.1 8B), continuous-batching/ch05 (49-request cap, arXiv:2407.05347), serving-engines-overview (SGLang engine context). Gate-1 self-review performed: structure pass (5 ELI5 blocks, Words-before-machinery ×14 rows, Where-the-picture-stops ×6 breaks, Checkpoint ×6 questions, Build/Break/Prove/See closers, 1 mermaid, 1 dated pricing box, 1 Field note, no forbidden language); numbers audit — every figure traced to a dated digest, derived math labeled (12.5 MB block waste, 62-of-62.5 block alignment, 10× position spread, 4× anecdote ratio), paper benchmarks hedged as authors'-own-2023-comparisons, 480→110 ms anecdote flagged single-cluster/directional. 10 fixes applied during review (mamba-style cache precision; SHA-256 gloss; 6.5 regime-header restructure with empty body filled; 5th ELI5 for semantic caching; API/prefill/GPU/p50 expansions at first use; block-alignment sentence corrected to token-zero anchoring with 62-of-62.5 arithmetic; GPU expansion dedup). Promissory notes discharged: ch03 "chapter 6 shows how PagedAttention made the KV rack cheaper to fill" and ch04 "the 20–38% waste number chapter 6 will turn into a whole chapter" both opened the chapter explicitly. Scope discipline: provider cache economics/TTL-while-streaming/breakpoint design left to ch14 (box labeled "chapter 14 owns the full worksheet"), sharded-KV to ch10, compaction-vs-invalidation to ch11/ch17, usage-field parsing to ch12. Known residuals: (1) xychart/mermaid render still needs build-time verification (carried since ch02 — first mermaid graph TD/LR-heavy diagram with subgraphs; validate at build pass); (2) tools/lint-manuscript.py still does not exist (carried since iteration 5) — Gate-1 followed EDITORIAL_SYSTEM declared rules manually. Next: draft ch07 (Prefill, decode, and the great divorce).
- 2026-08-27 — iteration 11 (chapter draft): research/ at 60 digests (≥60, gap-fill not triggered), so drafted ch07 → `manuscript/07-prefill-decode-great-divorce.md` (4,958 words). Covers all CHAPTER_MAP beats: two phases two bottlenecks (food-truck ELI5; recall ch03 asymmetry with derived 2×params×prompt-length vs one-weight-read argument, Splitwise phase-economics quote; prefill-bubble mechanism with ASCII iteration timeline showing arrival-timed TPOT freeze); chunked prefill (catering-trays ELI5; Sarathi-Serve piggybacking; formulas iterations ≈ prompt_tokens/chunk_size and iteration time ≈ chunk+decodes; vLLM V1 default + decodes-first + 2,048-vs-8,192 budget directions; 2–4×/~6× capacity hedged as authors' own comparisons; 2.6×/3.7×/5.6× series from ch05 discharged; schematic xychart TPOT spike vs plateau; DistServe insufficiency argument as the bridge); disaggregated inference (commissary+window+courier ELI5; DistServe/Splitwise/Mooncake; walk-in-freezer ELI5 for the KV tier; mermaid P/D flow diagram; dated box with colocation tax 1.6 vs 5.6/10 islands, 2:1 → 3.3 req/s/GPU ≈ 2.1× derived, 7.4×/12.6×, 4.48×/41×, Mooncake 525% simulated + 75% Kimi production, 0.2 s / 250 wpm chat SLOs; both-sides cost list: KV-transfer hop, duplicated plumbing, pool imbalance, early-rejection contract change); what PD separation means for TTFT under load (airport-lanes ELI5; TTFT = queue + prefill with vLLM metric names; three-step colocation feedback loop incl. linear prefill-batch inflation measured on LLaMA-2-7B/A100 and preemption-restarts-prefill recompute; per-pool 1/(1−ρ) walls sized against per-SLO capacity; symptom fingerprints interference/queueing/admission; field note: nightly 90k-token digest agent hitching chat streams, fixed by poor-operator's P/D separation; harness controls with caching-as-absent-prefill punchline). Sources: chunked-prefill-pd-split, goodput-and-slos, ttft-queueing-under-load, batching-size-latency-tradeoffs digests + recalls from ch03/04/05. Gate-1 self-review performed: structure pass (5 ELI5 blocks, Words-before-machinery ×13 rows, Where-the-picture-stops ×5 breaks, Checkpoint ×6 questions, Build/Break/Prove/See closers, 1 mermaid graph + 1 schematic xychart + 1 ASCII timeline, 1 dated box, 1 Field note, no forbidden language); numbers audit — every figure traced to a dated digest, derived math labeled (512× prefill/decode FLOP ratio, 2.1×, ~10 iterations, 8-iteration checkpoint answer), illustrative constants flagged, no universal spike-multiplier or transfer-crossover claimed (explicit hedges). 5 fixes applied during review (5th ELI5 added for Mooncake KV tier; Build-it spike claim softened from asserted 10–100× to chosen-threshold detector with hedge; GPU expansion moved to true first use at 7.3 and deduped; duplicate ITL expansion removed; CLI expanded in Prove it). Promissory notes discharged: ch03 (asymmetry "chapter 7 dedicates itself to it" + "chunks with resources of its own"), ch04 (lever row "whose desk long prefills land on"), ch05 (Sarathi 2.6×–5.6× "chapter 7 will unpack how" + "rationing to its architectural conclusion" + max_num_batched_tokens lever). Scope discipline: spec decode → ch08, per-phase parallelism vocabulary → ch10, long-context cost curves → ch11, cache economics/TTL → ch14, 429/retry machinery → ch15. Known residuals: (1) xychart/mermaid render still needs build-time verification (carried since ch02); (2) tools/lint-manuscript.py still does not exist (carried since iteration 5) — Gate-1 followed EDITORIAL_SYSTEM declared rules manually. Next: draft ch08 (Guessing at the speed of light).
- 2026-08-27 — iteration 12 (chapter draft): research/ at 60 digests (≥60, gap-fill not triggered), so drafted ch08 → `manuscript/08-guessing-at-the-speed-of-light.md` (5,296 words). Covers all CHAPTER_MAP beats: draft-and-verify (Sudoku checker ELI5; recall of ch03 roofline — batch-1 decode AI≈1 vs H100 ridge ≈295 explains why verification is nearly free; 4-step mechanism: draft γ → one verify pass shaped like mini-prefill → rejection sampler with corrected distribution norm(max(0, p − q)) and distribution-identical guarantee (Leviathan ICML 2023 / Chen 2023) → bonus token; mermaid loop diagram; E[progress] = (1−α^(γ+1))/(1−α) with worked α=0.8/γ=4 ≈ 3.36, break-even overhead ratio c, derived xychart of progress vs α framing the convex bet); Eagle/Medusa + drafter family (intern/author's-hands/photocopied-brief ELI5; draft-model 2–3× T5-XXL/Chinchilla; Medusa-1/2 >2.2×/2.2–3.6×; EAGLE feature-level 2.7–3.5× → EAGLE-2 dynamic trees 3.05–4.26× → EAGLE-3 4.1–6.5× with temp-0 means; n-gram prompt lookup 2–4× input-grounded as the agent species; vLLM/SGLang/TRT-LLM flag menus; dated box of published figures); acceptance rates (τ 5.84–6.62 temp-0 EAGLE-3; four forces: temperature measured precisely — temp 0→1 drops with derived 15–25% loss, copying approximate 1–2.5 tokens vs near-full, domain directional, cold-prefix sign-known-only); when speculation hurts (derived α-vs-net-multiplier table at illustrative 20% overhead showing sub-0.5 dead zone; structured-output grammar masks starve the drafter with engines disabling/degrading + TRT-LLM overlap workflow, sign-known magnitude-ours hedge; high-batch compute-bound verify-width tax with vendor framing quotes + MagicDec long-context exception 2.51× at batch 32–256, S* crossover, GQA raising it, ~90% acceptance self-spec 4k–100k, EAGLE-3 1.38× @batch 64 vs EAGLE-1 negative @24; TTFT blind spot pointed at ch6/7/14). Harness: field note (n-gram gain vanishing under JSON schema, caught by acceptance canary, flagged single-deployment directional); self-host per-workload toggle policy + acceptance-as-drift-canary + TRT-LLM max_draft_len contract quirk; provider-side invisibility → budget un-speculated, vLLM docs caveat + issue #9565; lever table → ch5/6/9/10/11/13/14. Sources: speculative-decoding-engines, spec-decode-acceptance-data digests + constrained-decoding-grammars (TRT-LLM guided+spec workflow). Gate-1 self-review performed: structure pass (5 ELI5 blocks, Words-before-machinery ×11 rows, Where-the-picture-stops ×5 breaks, Checkpoint ×6 questions incl. α=0.7/γ=4/25%-overhead worked answer 2.2×, Build/Break/Prove/See closers, 1 mermaid graph + 1 derived xychart, 1 dated box, 1 Field note, no forbidden language); numbers audit — every figure traced to a dated digest, all derived/illustrative math labeled (progress table, xychart, overhead constants), hedges kept where digests hedge (n-gram community acceptance, structured-output magnitude, cold-prefix). 4 fixes applied during review (invented intro 20→60 t/s replaced with published-range grounding; τ range 5.88→5.84–6.62 to include distill; MagicDec ~90% acceptance correctly re-attributed to the same paper w/ 70B self-spec detail; GPU expansion dedup). Promissory notes discharged: ch02 "chapter 8 runs a draft model ahead and verifies — the only mainstream escape from the serial tax" (opened the chapter), ch02 picture-stops "accepted tokens not wall-clock work" (τ framing), ch03 lever-row "more tokens per weight pass with a verification twist" (8.2 batching-with-your-own-future), ch07 closer "paying a checker instead of a writer" (proofreading motif + intro). Scope discipline: quantization → ch9, parallelism/MoE → ch10, long-context cost curves → ch11 (MagicDec given one paragraph + pointer), grammar machinery → ch13, cache economics/cold-prefix design → ch14, retry/429 → ch15. Known residuals: (1) xychart/mermaid render still needs build-time verification (carried since ch02); (2) tools/lint-manuscript.py still does not exist (carried since iteration 5) — Gate-1 followed EDITORIAL_SYSTEM declared rules manually. Next: draft ch09 (Smaller numbers, faster engines).
- 2026-08-27 — iteration 13 (chapter draft): research/ at 60 digests (≥60, gap-fill not triggered), so drafted ch09 → `manuscript/09-smaller-numbers-faster-engines.md` (5,760 words). Covers all CHAPTER_MAP beats: quantization menu (affine quant math q=round(w/s)+z, s=(max−min)/(2^b−1) with worked INT4 example s≈0.0107/error≈0.0053, per-channel/g128 scales; three-dials framing weights/activations/KV with W4A16/W8A8 notation; method zoo SmoothQuant→FP8-W8A8 lineage, GPTQ Hessian compensation, AWQ salient-1% w/ ~512 calibration samples, MXFP4-as-intended-precision via gpt-oss; derived floor ladder 8B 16GB→208 / FP8 8GB→415 / INT4 4GB→830 t/s ceilings extending ch03); quality/throughput tradeoffs (dated speedup snapshot box: SmoothQuant 1.56×/2×, GPTQ 3.25×/4.5×, AWQ >3×, Baseten FP8 8.5%/33%/31%/24%, SemiAnalysis 18%/23%; COLM 2025 delivery order — 8-bit ±1pt near-lossless but method-matters (SmoothQuant −4.43 @1.5B vs best-method −0.41), 4-bit −0.82..−3.27 avg with AIME concentration (70B AWQ −6.67/GPTQ −11.67), math/code-first mechanism, per-model variance, OOD-calibration sensitivity hedged community; KV quant deep dive: exact halving recall from ch04, vLLM 2026-04-22 measurements 14.9%/4.8%/−13% runtime, 54% decode-cost slope, ~7k break-even (was ~25k) with constant-toll-vs-length-scaled-savings mechanism, derived-schematic xychart, FA3 bug 91→13→89 cautionary tale, uncalibrated-scales Kimi guidance, vLLM dtype menu incl. per-token-head/nvfp4/turboquant); reading variant lists as quant menus (same-weights 8.3× Scout spread w/ FP8 tags, AIMultiple INT4 2.7×/−8 HumanEval, fast/mini/lite = quant+sparsity undisclosed, OpenRouter quantizations filter + price-ordered default routing as safety-rail argument, mermaid decision graph, three-questions decoding, variant pinning + quarterly re-eval, field note: router silently buying int4 caught by nightly golden-set canary). Sources: quantization-menu, quantization-quality-benchmarks digests + recalls from same-model-different-providers, kv-cache-bytes-formula, local-edge-inference, ch03/ch04 constants. Gate-1 self-review performed: structure pass (5 ELI5 blocks, Words-before-machinery ×15 rows, Where-the-picture-stops ×5 breaks, Checkpoint ×6 questions, Build/Break/Prove/See closers, 1 mermaid graph + 1 derived-schematic xychart, 1 dated snapshot box, 1 Field note, no forbidden language); numbers audit — every figure traced to a dated digest, all derived math labeled (floor ladder, xychart overhead term illustrative, 3% span), 10 fixes applied during review (real correction: rounding-error figure 6.6%→~3% of channel span; removed invented outlier-channel magnitude "10–100×" and re-grounded in SmoothQuant paper; acronym expansions FP8/GPU/AIME/COLM/GSM8K/MRCR/AUC at first use; float-precision gloss softened to avoid overstating BF16 mantissa; FlatQuant cold-naming replaced with best-method phrasing; sandwich ELI5 phrasing). Promissory notes discharged: ch03 two-lever close ("move fewer bytes — chapter 9"), ch03 lever-table row, ch04 "chapter 9 owns the full menu" quoted in opening + preview numbers (14.9%/54%/~7k) expanded into full 9.5, ch08 lever row ("the other bandwidth lever"). Scope discipline: MoE/active-params → ch10, long-context cost curves + MagicDec regime → ch11, structured-output interference → ch13, cache economics → ch14, gateway/routing mechanics → ch16, GGUF ladder detail → ch18. Known residuals: (1) 5,760 words is ~4.7% over the 3,000–5,500 concept target — judged content-bearing (three CHAPTER_MAP beats each requiring mechanism+numbers), logged rather than cut, consistent with ch03 precedent; (2) xychart/mermaid render still needs build-time verification (carried since ch02); (3) tools/lint-manuscript.py still does not exist (carried since iteration 5) — Gate-1 followed EDITORIAL_SYSTEM declared rules manually. Next: draft ch10 (One model, many chips).
- 2026-08-27 — iteration 14 (chapter draft): research/ at 60 digests (≥60, gap-fill not triggered), so drafted ch10 → `manuscript/10-one-model-many-chips.md` (5,433 words). Covers all CHAPTER_MAP beats: TP/PP/DP/EP/CP in plain words (library ELI5 for capacity-vs-speed split; five-axis kitchen ELI5; TP column/head slices + two collectives per layer per token + NVLink-sized-node rule; PP stages/microbatches/bubble + DualPipe bidirectional; DP as the only zero-collective axis; CP sequence sharding with Ring-Attention-exact sketch, Megatron 8K+ positioning, 77 s/1M/405B/128-H100/93% + 3.8 s/128K with ch11 pointer; EP one-liner → full mechanics in 10.5; mermaid anatomy diagram TP/PP/EP/DP; Megatron MoE sizing dated box: TP 1–2, EP primary, PP 8–16, expert-TP rare); MoE serving and expert routing (hospital ELI5; router = linear projection + nonlinearity → top-k → gating weights → dispatch + shared expert; dated snapshot box: Mixtral 46.7B/~12.9B top-2-of-8, DeepSeek-V3 671B/37B 256+1 shared top-8 first-three-dense, gpt-oss-120b 117B/5.1B top-4-of-128, gpt-oss-20b 21B/3.6B top-4-of-32 16 GB, HF 45B/14B framing caveat; headline derived arithmetic 140 GB vs 74 GB/token at ~10× capacity + touch fractions 5.5%/4.4%; all-experts-residency price 1,342 GB derived + MXFP4 single-GPU composition with ch09; parcel-dispatch ELI5 for EP forward: router → all-to-all dispatch → grouped GEMM → all-to-all combine × gating, DeepEP FP8/low-SM; imbalance failure mode with training-side fixes (GShard/Switch aux losses; DeepSeek aux-loss-free bias-not-gating γ=0.001/14.3T/500B + α=0.0001 seq loss) and serving-side fixes; capacity-factor formula + CF 1.0–1.25 + silent residual-path drop with seating ELI5; DeepSeek production topology: prefill 4-node/32-GPU ~40 experts/GPU 32 redundant, decode 40-node/160-GPU, 20 SMs + 10+10 warp-specialized kernels, node-limited M=4 + ~13-expert authors'-derived-ceiling hedged); why bigger can be faster (low-batch floor math above + Mistral 6× hedged as vendor claim + two-line spec reading: VRAM on total, tokens/s on active). Sources: parallelism-sharding-moe, moe-routing-serving-detail, context-parallelism-long-context digests + recalls from ch02/03/04/05/07/09. Gate-1 self-review performed: structure pass (5 ELI5 blocks, Words-before-machinery ×14 rows, Where-the-picture-stops ×5 breaks, Checkpoint ×6 incl. worked capacity math 640-vs-512 and TP=2 floor 21 ms/48 t/s, Build/Break/Prove/See closers, 1 mermaid anatomy diagram, 2 dated boxes, 1 Field note, no forbidden language); numbers audit — every figure traced to a dated digest, derived math labeled (TP=2 floor, 1,342 GB residency, touch fractions, ceiling answers), vendor 6× claim hedged, authors'-derived 13-expert ceiling flagged as not-shipped. 8 fixes applied during review (acronym expansions API/SLO/NVLink/MXFP4/RULER/VRAM at first use; real correction: DeepSeek-V3 residency "hundreds of gigabytes" → 1,342 GB derived arithmetic; diagram lead-in "sixteen GPUs TP=2×PP=2×EP=2×DP=2" corrected to unlabeled anatomy — the drawing shows 12 chips, not 16). Promissory notes discharged: ch02 ("tensor parallelism splits the weight stream so the bandwidths add — chapter 10"), ch03 ("move bytes faster lever — chapter 10"), ch04 ("real engines shard across GPUs — chapter 10 divides KV too" via CP), ch07 ("the parallelism vocabulary is chapter 10's" + per-phase plans made concrete with DeepSeek prefill/decode topologies), ch09 ("Chapter 10 owns the second lever" + "sparsity, chapter 10's half of the story" for fast/mini/lite). Scope discipline: long-context cost curves/compaction → ch11 (77 s figure given one paragraph + pointer), grammar interference → ch13, cache economics → ch14, 429/retry machinery → ch15, gateway/routing → ch16, local MoE detail → ch18. Known residuals: (1) mermaid nested-subgraph render needs build-time verification (first subgraph-within-subgraph diagram in the manuscript; carried xychart/mermaid verification from ch02); (2) tools/lint-manuscript.py still does not exist (carried since iteration 5) — Gate-1 followed EDITORIAL_SYSTEM declared rules manually. Next: draft ch11 (Long context is a memory product).
- 2026-08-27 — iteration 14 postscript: the rebase that unblocked the ch10 push brought in another author's `tools/lint-manuscript.py` (Vol. II frame) — the blocker carried since iteration 5 is now resolved upstream. Ran it after the chapter landed: MANUSCRIPT OK, 10/18 chapters, 12 files, 53,390 words — ch10 passes structural lint (ELI5, picture-stops, closers, vocab tables, numbering). Known residual (2) from the ch10 entry above is closed; mermaid/xychart render verification remains the only standing residual (build-time).
- 2026-08-27 — iteration 15 (chapter draft): research/ at 62 digests (≥60, gap-fill not triggered), so drafted ch11 → `manuscript/11-long-context-is-a-memory-product.md` (5,042 words). Covers all CHAPTER_MAP beats: cost curves vs context length (paralegal ELI5; ISL/KVSL vocabulary from NVIDIA blog; quadratic prefill vs linear-but-heavy decode with c·(N²+NM+M²/2) recall, 64×-vs-8× decomposition, 144 KiB→18 GB Qwen3 recall, eviction-of-neighbors mechanism, MagicDec long-KVSL speculation recall; dated mid-2026 window/tier box GPT-5 400K/272K, Claude 1M + ≤200K tiers, Gemini 1,048,576/$2/$12→$4/$18, Scout 10M, Qwen 1M; per-prompt tiering worked example $1.20 vs $0.40; pricing-cliff xychart); context parallelism (circle-of-clerks ELI5; Ring Attention N−1 hops exact vs Ulysses all-to-all-by-head, USP per-layer choice, pass-KV/pass-Q; 77 s/93%/63% FLOPS utilization/128 H100/16 nodes/3.8 s@128K; work-conservation punchline "divides wall-clock, not the work"; Mooncake KV-pool 75% recall; vLLM YaRN/bucketed-scheduling self-host menu; Scout iRoPE + hedged ~15.6% @128K third-party check as bridge to effective context); claimed vs effective vs positional (concert-hall ELI5; RULER recall one-line not re-tabled; attention-entropy mechanism — aggregation dies before retrieval, NIAH flatters; lost-in-the-middle with hot–cold–hot layout rule; ten-prompt own-battery budgeting at min(quality, tier, KV) ceilings); the compaction tradeoff no provider documents (meeting-minutes ELI5; Claude Code auto-compact trigger arithmetic + 13K buffer hedged unofficial; Anthropic Compaction API; LangGraph SummarizationNode; three layers: exact-prefix invalidation w/ 150K→30K derived amortization, lost-in-compaction 73%→40%→7% xychart, side-constraints 17%/>90% extractor; TokenPilot "prefix mismatches and cache invalidation" up-to-87%; MemGPT/Letta core-memory <80%; compaction-decision mermaid; log-triage field note); harness controls (truck-packing ELI5; 8 controls + lever table). Sources: context-parallelism-long-context, attention-cost-scaling, context-window-claims, context-compaction-tradeoffs digests + recalls from cache-hit-math-agent-loops (multipliers only), kv-cache-bytes-formula, ch03/04/05/06/07/08/10. Gate-1 self-review performed: structure pass (5 ELI5 blocks, Words-before-machinery ×13 rows, Where-the-picture-stops ×5 breaks, Checkpoint ×6 questions incl. worked 250K-vs-199K pricing and 150K→30K amortization, Build/Break/Prove/See closers, 2 xychart + 1 decision mermaid, 1 dated box, 1 Field note, no forbidden language); tools/lint-manuscript.py run: MANUSCRIPT OK, 11/18 chapters, 13 files, 58,334 words — ch11 passes structural lint. Numbers audit: every figure traced to a dated digest, derived math labeled ($1.20/$0.398 cliff chart, amortization example), Scout third-party figure hedged blog-grade, per-prompt-tiering claim scoped to Gemini explicitly after review (Anthropic's tiering mechanism documented, its per-prompt repricing not — softened in picture-stops). 6 fixes applied during review (MTok/DRAM/SSD/DOI expansions at first use, YaRN rotary-position-embedding gloss, "digest-sourced" removed from reader-facing prose per ch04 lesson). Promissory notes discharged: ch03 ("chapter 11's thesis — context is a memory product — rooted in the cost curve" — quoted in intro), ch04 (×3: position design as ch11's beat → 11.4; cap-and-trim lever → 11.6; mid-context attendance → 11.4), ch06 (paraphrase trap = compaction as cache decision → 11.5 layer three, named explicitly), ch08 ("chapter 11 owns the long-context half" of speculation-follows-bandwidth → 11.2 MagicDec paragraph), ch10 ("chapter 11 owns what long context costs at the contract layer" → intro + 11.2/11.3). Scope discipline: provider cache semantics/breakpoints/TTL worksheet → ch14 (multipliers recalled only), session resumption/rehydration + subagent isolation → ch17 (pointed twice), gateway/routing → ch16, local MoE detail → ch18, KV quant menu → ch9. Known residuals: (1) xychart/mermaid render verification still build-time-only (carried since ch02); (2) two xycharts in one chapter is a first — both use plain `line` plots with explicit axis ranges, but EPUB render check owed at build pass. Next: draft ch12 (The streaming contract) — first Part III chapter.
- 2026-08-27 — iteration 16 (chapter draft): research/ at 62 digests (≥60, gap-fill not triggered), so drafted ch12 → `manuscript/12-the-streaming-contract.md` (5,319 words; first Part III chapter). Covers all CHAPTER_MAP beats: SSE/WebSocket/event shapes (WHATWG envelope incl. spec-legal meta-only events + openai-python #2722 JSONDecodeError lesson; OpenAI Chat chunks + [DONE] sentinel vs Responses typed events; Anthropic ordered event log with ping/error anywhere + stop_reason-in-message_delta, no sentinel; Gemini streamGenerateContent?alt=sse where the query param IS the stream switch + finishReason-on-last-chunk; LiteLLM map_finish_reason + GLM "network_error" mid-stream Pydantic crash → unknown-value fallback rule; Realtime/Live off-SSE: OpenAI WS/WebRTC, Gemini Live WebSocket-only PCM 16 kHz in / 24 kHz out / ~10-min socket reset; mermaid three-grammars→one-normalizer diagram; dated mid-2026 grammars box); tool-call deltas (parcels ELI5; index/item_id/block-index/toolu_-id/step.start keying per provider; Portland 3-fragment worked example; parse-exactly-once-at-finish; never-parse-per-chunk with escape-splitting rationale; ""→{} coercion; parallel calls; vLLM ToolParser docs warning malformed/schema-violating streamed args as expected failure → retry-with-error tool result not crash; Gemini args-as-object vs strings + MALFORMED_FUNCTION_CALL); provider differences/normalization (mailroom ELI5; four internal events text_delta/tool_call_delta/usage/stop_reason; stop-reason state machine incl. pause_turn resumption + unknown fallback; LiteLLM/Vercel middleware + DeepSeek dual-format endpoints; usage parsing discharging ch06+ch11 promissory notes: OpenAI prompt_tokens/cached_tokens/cache_write_tokens, Responses input/output rename, Anthropic cache ledger + identity total=read+creation+fresh, Gemini usageMetadata incl. promptTokenCount-contains-cached; drift warnings — Claude 4.7+ tokenizer ~30%, assert usage invariants in CI); TTFT as a product metric (elevator ELI5; client-side TTFT from first content delta because no hosted provider surfaces TTFT distributions — checked 2026-08-27; anchor-swap xychart streaming-flat vs non-streaming-climbing, illustrative constants from ch02 worked example, labeled; levers: caching shrinks TTFT not decode (Anthropic quote, no published ms figure), long prompts dominate TTFT, provider choice as latency knob (6× gpt-oss-120b spread recall); reasoning-model trap: GLM-5.3 1.6/30.1/7.5 split recalled, first-visible-token decision made explicit, percentile-goodput alerting). Also: timeouts/cancellation/capture-resume (10-min SDK defaults dated box, ~30-min logical hang derived, vLLM step-granular abort + issue #10087, billing-follows-generation hedged community reports, ping-vs-byte-idle stall detection with event-aware clocks per ch05's two-budget shape, Anthropic capture-and-resume ≤4.5 assistant / ≥4.6 user continuation). Sources: streaming-transports-normalization, tool-call-delta-streaming, provider-api-anatomy, cancellation-timeout-semantics, serving-observability-metrics, provider-latency-snapshot-2026 (recalls), sampling-params digests not needed. Gate-1 self-review performed: structure pass (5 ELI5 blocks, Words-before-machinery ×12 rows, Where-the-picture-stops ×3 breaks incl. state-machine/bursty/realtime-state, Checkpoint ×6 questions with answers, Build/Break/Prove/See closers, 1 mermaid graph + 1 illustrative xychart, 2 dated boxes, 1 Field note, no forbidden language); tools/lint-manuscript.py run: MANUSCRIPT OK, 12/18 chapters, 14 files, 63,526 words — ch12 passes structural lint. Numbers audit: every figure traced to a dated digest, derived math labeled (~30-min hang, xychart lines, 6× spread), community billing reports hedged, TTFT-cache magnitude explicitly noted as unpublished. 8 fixes applied during review (HTTP/JSON/SDK/API/KV/CI acronym expansions at first use; delta.role "exactly once" softened to digest's "omitted or null"; Claude ≤4.5/≥4.6 version numbers added to capture-resume; untraced "2009-vintage transport" date removed — no digest backs it). Known residuals: (1) xychart/mermaid render verification still build-time-only (carried since ch02); (2) Build-it's "roughly 150 lines" is a design estimate, not a verified companion artifact — companion code itself is owed in the Part IV build chapters/Appendix D per plan. Next: draft ch13 (Structured output is not a prompt trick).
- 2026-08-27 — iteration 17 (chapter draft): research/ at 62 digests (≥60, gap-fill not triggered), so drafted ch13 → `manuscript/13-structured-output-is-not-a-prompt-trick.md` (5,863 words). Covers all CHAPTER_MAP beats: constrained decoding + grammars + xgrammar/outlines (proctor-keyboard ELI5; mask = set illegal logits to −∞ before sampling, by construction, 100% correct both backends in vLLM PR #10785; compile-over-characters ∩ tokenizer-vocabulary via token trie/toktrie; regex→FSM vs nested-JSON→pushdown-automaton-with-stack; sampler-pipeline mermaid with mask placed after penalties, before temperature/truncation per vLLM source; Outlines at scale → 0.22 req/s, 113 tok/s, mean TTFT ≈38.5 s from per-request FSM compile; XGrammar Nov 2024 adaptive token mask + context-independent precompute + persistent stack + CPU/GPU overlap, authors' claims up to 100× per-token grammar latency and 80× end-to-end H100; vLLM default Dec 2024: 0.94 req/s / 480 tok/s / TTFT ≈4.55 s, derived 4.3×/4.2×/8×, blog 2025-01-14 up to 5× TPOT under load + schedule-level masks mixing free/guided batches; llguidance ~50 μs @128k vocab powering guidance/llama.cpp/SGLang/vLLM/mistral.rs/onnxruntime-genai/Chromium; dated engine-backend box incl. TRT-LLM xgrammar+outlines fallback+guided-spec workflow); JSON mode + provider surfaces (four-couriers ELI5; guarantee gradient worked example {name,age}: OpenAI strict = full schema at generation with all-required + null-union optionals + additionalProperties:false + root-object + unsupported-keywords-error, key-order-is-emission-order documented + required-order folklore hedge, 100%-vs-<40% flagged vendor-2024 eval, dated limits box 5,000 props/10 levels/120k chars/1,000 enums/fine-tune rejections; Anthropic = strict:true tools + output_config.format ex-beta + tool_choice auto/any/tool/none as forced-extractor pattern; Gemini = responseSchema OpenAPI-3.0 subset that *silently ignores* unsupported properties (mirror of OpenAI's erroring), propertyOrdering 2.0-era→2.5+, enum-only text/plain mode; DeepSeek = json_object syntax-only with json-in-prompt + example + max_tokens else documented unending-whitespace stream, Responses text.format third-party hedged; gradient table + no-guarantee-for-truncation row); token overhead of schemas (rulebook-luggage ELI5; three taxes: compile toll with flat-correctness-spiking-TTFT signature, per-step toll linear-in-vocab per PSC + TSC "intractably high overhead for more complex CFGs", prompt/output token toll with ≈389-token measured tool schema + Field(description) serialized regardless + derived 40-step 15,560-token loop arithmetic + JSON structural-token inflation hedged unverified; both-sides: schema as most-cacheable content → frozen-schema discipline, ch14/17 pointers; field note: 6→31-field schema bloat, quality flat, dev-docs migration); grammar fights the model (verdict-first-judge ELI5; GAD different-distribution mechanism with expensive reweighting fix; evidence ladder: 38.15-pt Last Letter gap at 0.148% parse errors behavioral-not-parsing, JSON < format-restricted < NL-to-format < free-text ordering, 100% answer-before-reason GPT-3.5 = schema suppressing CoT, GPT-4o-mini NL wins 2-of-3, schema-removal helps GSM8K; CRANE 29-vs-38 GSM-Symbolic ≈5–9-pt raw tax + TC⁰ unreachable-in-principle + both-sides pass@3 ~4× tokens; Hidden Cost base-benefit/instruct-lose 11 models; Capacity-Not-Format spare-capacity framing; repetition trap; strict-regex 43.7%-vs-75.5% grader twin = 31.8 pts destroyed by validation strictness; both-sides close: extraction/classification mostly untaxed). Harness: fittings ELI5 + 8 rules (guarantee-tier config w/ validator+retry below full enforcement, constrain-edges/free-middle with reason-before-answer field ordering, tolerant parsing, minimal hot-path schemas, frozen schema bytes, max_tokens above worst-case, TTFT+quality canaries, no spec-decode budget on schema endpoints) + lever table → ch2/5/8/12/14/15/16/17. Sources: constrained-decoding-grammars, provider-structured-output-apis, structured-output-costs-tension digests + sampler-order facts from sampling-params-provider-defaults + recalls from ch02/05/06/12. Gate-1 self-review performed: structure pass (5 ELI5 blocks, Words-before-machinery ×13 rows, Where-the-picture-stops ×4 breaks, Checkpoint ×6 questions with answers incl. worked backend-switch and repetition-trap diagnoses, Build/Break/Prove/See closers, 1 mermaid sampler-pipeline graph, 2 dated boxes, 1 Field note, no forbidden language); tools/lint-manuscript.py run: MANUSCRIPT OK, 13/18 chapters, 15 files, 69,389 words — ch13 passes structural lint. Numbers audit: every figure traced to a dated digest (audit done during draft + re-verified), derived math labeled (4.3×/4.2×/8× ratios, 15,560-token loop), vendor/engine-author claims flagged as own benchmarks, single-source 1-in-10 prompt-failure estimate hedged, JSON token-multiplier study hedged unverified, DeepSeek Responses text.format hedge kept. 14 fixes applied during review (acronym expansions at first use: TTFT/PR/TGI/API/CPU/GPU; API-expansion phrasing reworked once; folklore hedge tightened; 4 trim passes totaling ~120 words: intro paragraph merge, closers compression, mechanism-paragraph tightening; removed speculative "most-shipped formal-language technology of the decade" flourish from See-it). Promissory notes discharged: ch08 ("Grammar-constrained decoding (chapter 13's machinery)…" → 13.2 explains the mask fully + ch08 recall paragraph closes the loop; "do not budget speculative gains" rule restated as harness rule 8), ch01 Part III route mention fulfilled. Scope discipline: cache pricing multipliers → ch14 (named, not priced), retry/backoff machinery → ch15, guarantee-tier routing → ch16, frozen-prefix assembly → ch17, tool-call fragment reassembly → ch12 (recalled only). Known residuals: (1) 5,863 words is ~6.6% over the 3,000–5,500 concept target — judged content-bearing (four CHAPTER_MAP beats: engine mechanics+lineage, four-provider menu, three taxes, quality tension, each requiring ELI5+mechanism+dated numbers; consistent with ch03/ch09 overage precedent), logged rather than cut; (2) mermaid render verification remains build-time-only (carried since ch02 — first sampler-pipeline-style graph LR with a cycle edge; validate at build pass); (3) Build-it's "roughly 120 lines" is a design estimate, not a verified companion artifact (companion code owed in Part IV/Appendix D per plan). Next: draft ch14 (The cache that pays your bill).
- 2026-08-27 — iteration 18 (chapter draft): research/ at 66 digests (≥60, gap-fill not triggered), so drafted ch14 → `manuscript/14-the-cache-that-pays-your-bill.md` (5,294 words). Covers all CHAPTER_MAP beats: prompt caching per provider (warehouse-club ELI5; one mechanism — byte-exact KV-prefix reuse from ch06 — under four contracts: Anthropic explicit breakpoints w/ 4-max + 20-block lookback + per-model minimums 512–4,096 + 1.25×/2×/0.1× + TTL-from-request-start w/ free refresh + 1h-precedes-5min; OpenAI automatic/best-effort w/ 1,024-visible minimum (5.6+; 2,048 older; hidden tokens excluded), 5.6+ mirroring Anthropic economics incl. cache_write_tokens vs older flat discounts (GPT-4o 50% at Oct-2024 launch, no write charge), ≥30-min TTL, ~15-req/min overflow routing + prompt_cache_key; Gemini implicit (2.5+, 2,048/4,096 thresholds, 10% of input, "no cost-saving guarantee") + explicit storage-priced caches.create (default 1h TTL, $4.50/1M tok/hr Pro-class; ~3-requests payback hedge flagged unconfirmed); DeepSeek automatic disk cache ~0.1× (deepseek-chat $0.014/$0.14 exact; newest rows ≈2–3%); dated mid-2026 six-column semantics/pricing box); cache-hit math for agent loops (punch-card ELI5; $3/1M × 100k-prefix × 10-turn worked example → $0.645 vs $3.00 ≈ 79%; base-input units; break-even N ≥ (w−1)/(1−r) → 0.28/1.11 with Anthropic docs' verbatim one-read/two-reads statement; 25-turn loop 525,000 → 354,200 (~32.5%, prefix-only) → ≈55,000 (~90%, incremental) with three-habit explanation of the 32-vs-90 gap; derived per-turn-cost xychart; TTL expiry 12.5× penalty + 1.6-rewrite crossover; 10k-fanout 50M → 5.01M ≈ 90%; four-term cost formula from token-pricing-anatomy); designing prefixes that hit (letterhead ELI5; render order tools→system→messages w/ invalidation-cascade mermaid; named cache-breakers list; system-reminder tail-delivery + Claude Code four-layer stack; 4-breakpoint allocation w/ leapfrogging past 20 blocks + checkpoint-on-change; defer_loading/tool stubs; multi-tenant cache_salt as engine-side isolation mechanism turned money decision; keep-alive under TTL w/ rate-budget both-sides; field note: 60-way OpenAI fanout sagging cached share past the ~15-rpm line, fixed by prompt_cache_key + paced waves, qualitative only); harness controls (fuel-gauge ELI5; ch12 usage-field instruments + identity; hit-rate-as-SEV culture w/ no-published-percentages caveat; deploys as priced cache-herd events; 9-row lever table). Sources: prompt-caching-provider-semantics, cache-hit-math-agent-loops, cache-breakpoint-design, token-pricing-anatomy digests + recalls from ch06 (hash chain, salt, herd events, 10× position spread), ch11 (compaction cliff), ch12 (usage fields), ch13 (frozen schema bytes). Gate-1 self-review performed: structure pass (5 ELI5 blocks, Words-before-machinery ×13 rows, Where-the-picture-stops ×5 breaks, Checkpoint ×6 questions with worked answers, Build/Break/Prove/See closers, 1 invalidation-cascade mermaid + 1 derived xychart, 1 dated box, 1 Field note, no forbidden language); tools/lint-manuscript.py run: MANUSCRIPT OK, 14/18 chapters, 16 files, 74,463 words — ch14 passes structural lint. Numbers audit: every figure traced to a dated digest (all four cache digests retrieved 2026-08-27), derived math labeled ($0.645, 525,000/354,200/55,000 series, 12.5×, 1.6 crossover, 5.01M, xychart lines), vendor claims hedged (OpenAI TTFT-up-to-80% vendor-reported; Gemini ~3-request folklore flagged unconfirmed; DeepSeek newest-row 2–3% spread across digest snapshots noted as dated), no hit guarantee claimed where providers say best-effort. 11 fixes applied during review (4,098→4,096 Gemini 3.x threshold typo ×2; cache_salt re-attributed from "providers mix salt" to engine-side hash chain per ch06; "identity holds everywhere" softened to ch12's Anthropic-documented identity; "0.1 on three of four providers" corrected to all-four-newest w/ older-row variance; $3 prose price given dated-snapshot hedge; DeepSeek news-post undated year "2024" removed; API/SEV acronym expansions at first use; "eval platform"→"evaluation platform"; 5th ELI5 added for TTL expiry). Promissory notes discharged: ch06 "chapter 14 owns the full worksheet" (dated box + 14.2/14.3), ch06 "14 for multi-tenant economics" (salt paragraph), ch03 "asymmetry into harness design" (14.4 layout discipline), ch07 "biggest line item on your bill" (intro), ch11 "pricing worksheet in ch14" (multipliers + tiering arithmetic), ch13 "chapter 14 prices the multipliers" (14.2/14.3), ch01 timestamp promise (intro + 14.4 cache-breakers). Scope discipline: retry/429 machinery → ch15 (borrowed pacing pointer only), routing/gateways/batch-stacking detail → ch16 (per-model-cache tension flagged, not solved), stable-prefix full discipline + session rehydration + subagent isolation → ch17, tinyengine CacheLedger integration → ch18/Appendix D. Known residuals: (1) xychart/mermaid render verification remains build-time-only (carried since ch02); (2) Build-it's "roughly 130 lines" is a design estimate, not a verified companion artifact (companion code owed in Part IV/Appendix D per plan, consistent with ch12/ch13 residuals); (3) dated pricing box necessarily duplicates a subset of the ch06 two-bullet box — kept intentionally (worksheet role) with the ch06 box now pointing here as "owns the full worksheet". Next: draft ch15 (Rate limits are physics).
- 2026-08-27 — iteration 19 (chapter draft): research/ at 66 digests (≥60, gap-fill not triggered), so drafted ch15 → `manuscript/15-rate-limits-are-physics.md` (5,734 words). Covers all CHAPTER_MAP beats: quota architectures (water-main/tower ELI5; limits as capacity statements — ch03/05/07 physics recalled backward, org/project-level not per-key with documented 3.5M shared-family pool, physics-floor/business-decoration rule; four-provider meter taxonomy: OpenAI RPM/TPM/RPD/IPM/audio-minutes with max(max_tokens, char-estimate) reservation + unsuccessful-requests-count + cached-tokens-not-carved, Anthropic RPM/ITPM/OTPM split with token-bucket continuous refill "60 RPM might be 1 rps" + cache reads exempt (2M ITPM → ~10M effective at 80% hit, provider's own arithmetic) + OTPM-on-actual-not-max_tokens, Gemini per-project RPM/input-TPM/RPD midnight-Pacific + spend-based limits with no output meter, Bedrock burndown multipliers 15×/10×/5×/10×/1:1 + input+max_tokens up-front deduction with 36,000→9,000 documented worked example + quota-vs-billing divergence; dated mid-2026 quota-sheet box; per-provider-ledger harness rule; max_tokens as quota lever cross-tied to ch13); 429/529 behavior (restaurant-door ELI5; Anthropic 429 rate_limit_error vs 529 overloaded_error with spend-cap 429 carrying no Retry-After + resume-date message, OpenAI's overloaded 429 with insufficient_quota code/type + July 2026 enforced monthly spend limits + Retry-After/x-ratelimit-* headers; classify-before-retry mermaid decision graph; SDK defaults dated box openai 2/anthropic 0.5–8s/google 4 retries ~1-2-4-8s all straggler-tuned; 3-attempt cap → 3× amplification bound; zombie-fleet field note on the spend-cap retry loop, qualitative only); queues/jitter/backoff (traffic-light ELI5; Full Jitter formula random(0, min(cap, base·2^attempt)) with AWS 2015 simulation 1,000-clients/100-tokens pedigree, Retry-After-as-floor; SRE retry-storm math 10,000 QPS vs 100-QPS overload → +100 retry QPS/round, 2× at 50% failure; ~10% retry budget rejected locally; adaptive throttling formula with K=1.1 and worked 1,000/600 → 34%, Beam floor + AWS SDK adaptive AIMD; LiteLLM cooldowns + enforce_model_rate_limit + RouterRateLimitError-no-Retry-After hazard issue #27823); client-side scheduling (airport ELI5; queueing recall 2×@50%/~10×@90%/~100×@99% with 70–80% knee + classical-law xychart; 900k TPM ÷ 60 ÷ 500 → ~30 req/s derived with 70–80% → ~21–24 derived; Little's Law semaphore (24 rps × 4 s ≈ 96 in flight, labeled illustrative); client token bucket as ch14's keep-alive gate; 10k-fanout wave-pacer recipe discharging ch14's 60-way field note; tail law 1−(1−p)^N with 63% @ p=1%/N=100 and 99.99999% @ N=10k derived, step budgets + K-of-N reduce, OpenAI latency-guide make-fewer-requests + 50%-output→~50%/50%-prompt→1–5%). Harness lever table (11 rows). Sources: rate-limit-quota-architectures, 429-529-retry-behavior, client-rate-scheduling digests + tail-latency-fanout-amplification for 15.6's tail law + recalls from ch02/05/07/12/13/14. Gate-1 self-review performed: structure pass (5 ELI5 blocks, Words-before-machinery ×14 rows, Where-the-picture-stops ×4 breaks, Checkpoint ×6 questions with worked answers incl. Bedrock 18,500→10,500 quota math, jitter random(0,4) compute, throttler 17.5% probability, Build/Break/Prove/See closers, 1 mermaid classifier + 1 classical-law xychart, 2 dated boxes, 1 Field note, no forbidden language); tools/lint-manuscript.py run: MANUSCRIPT OK, 15/18 chapters, 17 files, 80,007 words — ch15 passes structural lint. Numbers audit: every figure traced to a dated digest, derived math labeled (30 req/s, 21–24, 96 in flight, xychart 1/(1−ρ) values, 63%/99.99999%, 17.5%, 18,500/10,500), Gemini paid-tier 150–300 RPM kept third-party-hedged, concurrency-cap dimension hedged "not all providers publish this". 10 fixes applied during review (real correction: Bedrock "effective TPM a fifth of nominal" → output-token-draws-quintuple phrasing per digest's far-lower-than-nominal framing; M/M/1 removed from xychart title for classical-law phrasing; "5xx" removed from mermaid node; HTTP/SDK/API/CACM/QPS/AIMD/SRE expanded at first use; illustrative constants labeled; muddled token-bucket parenthetical rewritten; "fan-out"→"fanout" consistency; ~200 words trimmed across two passes). Promissory notes discharged: ch03 ("chapter 15 adds fanout amplification" → 15.6 tail law), ch05 ×3 ("owns the retry-and-backoff machinery" + "formalizes the backoff" → 15.5; "stay inside your quota; chapter 15 first" → 15.6), ch07 ×3 (admission-control "chapter 15 owns the response" quoted in intro; early-rejection retry discipline quoted in 15.5; polite-restaurant echoed by door ELI5), ch10 (retry-budget machinery → 15.5 cites ch10's fleet rule), ch13 (validator retry-path lever → 15.5 + lever table), ch14 ×3 (client-side scheduling pointer → 15.6; field-note wave pacing → 15.6 recipe; keep-alive rate-budget interface stub → 15.6 token-bucket acquire + Build it RateScheduler). Scope discipline: circuit breakers/cooldown routing/gateway mechanics/hedged requests → ch16 (named, not built), batch APIs/cost attribution → ch16, session rehydration → ch17, tinyengine assembly → ch18/Appendix D. Known residuals: (1) 5,734 words is ~4.2% over the 3,000–5,500 concept target — judged content-bearing (five CHAPTER_MAP beats: quota taxonomy, 429/529, jitter/backoff, queueing, client scheduling; consistent with ch09 4.7%/ch13 6.6% overage precedent), logged rather than cut; (2) xychart/mermaid render verification remains build-time-only (carried since ch02; first xychart whose x-axis is a percent scale — validate at build pass); (3) Build-it's "roughly 120 lines" is a design estimate, not a verified companion artifact (companion code owed in Part IV/Appendix D per plan, consistent with ch12–14 residuals); (4) the Field note is a qualitative operator anecdote with no numbers, mirroring the documented spend-cap 429 semantics rather than any digest-backed incident. Next: draft ch16 (Routing, fallbacks, and the money meter).
- 2026-08-27 — iteration 20 (chapter draft): research/ at 66 digests (≥60, gap-fill not triggered), so drafted ch16 → `manuscript/16-routing-fallbacks-money-meter.md` (6,110 words; last Part III chapter). Covers all CHAPTER_MAP beats: gateways (switchboard ELI5; alias-not-literal rule; LiteLLM moves — weighted routing/simple-shuffle-recommended/least-busy/latency-based/usage-based, health-check-driven removal w/ ignore_transient_errors, model-group fallbacks w/ all-cooling-skips-check; OpenRouter two-layer failover: allow_fallbacks default, 30-s outage deprioritization, inverse-square-of-price weighting w/ $1/$2/$3 → 9× derived, Aug-2025 ~50-min DB outage hedged no-uptime-published, classified-errors-only fallback rule (malformed 400/garbage 200 don't fire); Cloudflare cf-aig-step header; ch14 arbitration discharged as route-at-session-start-not-per-call w/ fallback-breaks-recorded-as-cache-events; headroom telemetry = client-side TTFT + x-ratelimit-remaining gauges as router input); model routing per task (triage-nurse ELI5; RouteLLM arXiv 2406.18665: ~95% quality, >85% MT-Bench/45% MMLU/35% GSM8K cost cuts, commercial routers >40% costlier, savings-collapse-as-workload-hardens lesson; guarantee-tier routing table from ch13; pin-and-rebenchmark from ch01; both-sides: misroute = silent quality failure no breaker sees); batch APIs (night-train ELI5; uniform 50%/24h across all three; dated box: OpenAI 50k/200MB/24h-only-window, Anthropic 100k/256MB/most-<1h/29-day-retention/unbilled-errors, Google 50%/24h-SLO + Flex 50%/1-15min/sheddable middle lane; worked $4.00→$2.00 GPT-4o and $5.40→$2.70 Sonnet 4.6 1M-token suite, 50-run matrix $100-135/night; "if the harness would retry rather than time out, it can batch"; OpenAI-only separate-pool claim scoped honestly; meter warnings: usage-lands-late + cache-stacking-documented-on-one-provider, intra-batch hits measured-not-designed); circuit breakers (fuse-box ELI5; canonical CLOSED/OPEN/HALF_OPEN FSM w/ mermaid, Fowler/Azure/Resilience4j count-vs-time windows; under-retries-beside-fallbacks doctrine w/ Portkey attempts:5-on-[429,500,502,503,504] layering; error-class trips: LiteLLM 429→5s default, >50%-of-minute, 401/404/408 non-retryable, AllowedFailsPolicy; per-deployment granularity w/ hashed model_id + single-instance-never-cools; Portkey count-OR-rate w/ minimum_requests, 30s min cooldown, >500 default, all-open bypass; 6×-spread rule; brownout blindness + field note: breaker that never tripped, caught by cost-per-completed-task); cost attribution (submeter ELI5; cost = Σ bucket×rate×modifier w/ modifier-set-recorded; inclusive-vs-exclusive: OpenAI/Gemini total+subset vs Anthropic/Bedrock additive three-bucket; four-exclusive-buckets-at-edge normalization; reasoning tokens billed-output-invisible-in-text; LiteLLM response_cost/community price map/local pin, gpt-3.5-turbo 1.5e-06/2e-06; Langfuse exclusive-multiply + inclusive-normalized-only-if-pure-OpenAI-schema cliff + reasoning-inference warning; OpenMeter events/subject/meters; attribution-at-emission-time; drift taxonomy ×4 + daily-invoice-reconciliation-as-schema-change); the 10k-fanout cost worksheet (arithmetic artifact: 10,000×(2,000 in incl. 1,500 frozen prefix + 400 out) on dated Sonnet 4.6 rates → interactive $120.00 / batch $60.00 / batch+prefix-hits ≈$39.75, all derived-labeled; lane-beats-the-cache punchline: interactive-at-perfect-hits $79.50 > batch-no-cache $60.00; two-series derived xychart cost-vs-hit-rate; failure lines: ~$0.006/retry, 5%→$3.00, unbilled-errors vs re-run asymmetry, 24h-expiry; tail-law latency row from ch15; generalizable skeleton w/ config-dated price table). Sources: model-routing-gateways-fallbacks, batch-api-economics, cost-metering-attribution, circuit-breakers-llm-ops digests + tail-latency-fanout-amplification (latency row) + recalls from ch01/02/05/12/13/14/15. Gate-1 self-review performed: structure pass (5 ELI5 blocks, Words-before-machinery ×12 rows, Where-the-picture-stops ×5 breaks, Checkpoint ×6 questions with worked answers incl. 16:1 inverse-square odds, $50→$25 batch math, $1.875/M stacked write, Build/Break/Prove/See closers, 1 breaker-FSM mermaid + 1 two-series derived xychart, 1 dated batch sheet box, 1 Field note, no forbidden language); tools/lint-manuscript.py run: MANUSCRIPT OK, 16/18 chapters, 18 files, 86,117 words — ch16 passes structural lint. Numbers audit: every figure traced to a dated digest, all worksheet/xychart/checkpoint math re-derived and labeled, OpenAI batch-pool claim scoped to its source, single-provider cache-stacking claim scoped, Aug-2025 incident hedged as purpose-not-performance. 11 fixes applied during review (added the missing Field note fixing a dangling in-text reference; 2 reader-facing "digest" wordings removed per ch04/ch11 lesson; TTFT/GPU/API/JSON expansions at first use; batch rate-limit-pool claim re-scoped to OpenAI; p99.99 table cell glossed as 1-in-10,000 tail draw; ~150 words of trims across 3 passes; also caught + reverted an accidental PROGRESS.md line edit before commit). Promissory notes discharged: ch01 ("chapter 16 turns this into routing strategy" + pin-and-rebenchmark → 16.2/16.3), ch02 ("chapter 16's metering will reuse exactly this arithmetic" → 16.6 opening), ch05 (batch subsidy → 16.4 backfill economics), ch13 (guarantee-tier routing lever → 16.3 table), ch14 ×3 (model-hopping arbitration → 16.2; "chapter 16 will make you arbitrate" quoted in intro; CacheLedger as "the money meter chapter 16 will read" → Build it + worksheet skeleton), ch15 ×5 (quota-ledger consumption → intro + Build it; shed-load escalation → 16.5 doctrine; RouterRateLimitError scheduling → Build it Break-it; cooldown-state reading → Build it; wave-pacer/tail law → 16.7 latency row). Scope discipline: tinyengine assembly → ch18 (Router ≈150 lines estimate, consistent with ch12–15 residuals); session resumption/rehydration → ch17 (session-pinning rule names ch17's territory without stealing); load-testing/hedged-requests machinery → named in See it only. Known residuals: (1) 6,110 words is ~11% over the 3,000–5,500 concept target — exceeds the ch03/ch09/ch13 content-bearing precedent (4.7–6.6%); judged content-bearing (six CHAPTER_MAP beats — most of any Part III chapter — each requiring ELI5+mechanism+dated numbers; three trim passes applied, further cuts would cut mechanism or both-sides, against depth-beats-breadth), logged honestly rather than cut; (2) xychart/mermaid render verification remains build-time-only (carried since ch02; first two-series xychart and first FSM-style cycle graph in the manuscript — validate at build pass); (3) Build-it's "roughly 150 lines" is a design estimate, not a verified companion artifact (companion code owed in Part IV/Appendix D per plan, consistent with ch12–15 residuals); (4) the Field note is a qualitative operator anecdote with no numbers, mirroring the documented breaker semantics (status-class trips cannot see slow-but-200) rather than any digest-backed incident. Next: draft ch17 (Cache-aware harness design) — first Part IV chapter.
- 2026-08-27 — architect pass (parent session): mermaid pipeline live (21/21 diagrams rendered, 08-m1 label fix); lint-manuscript.py gate wired into build; first clean EPUB build (2.3M, integrity OK); book-landscape research complete (12 digests — verified wedge: no book-length serving-layer title for agent/harness engineers; nearest neighbors documented in books-positioning-wedge.md); ch01 Gate-2 review: MINOR (P0=0, P1=3, P2=2) — findings in review/ch01-gate2.md for next editorial pass. KNOWN Gate-4 finding: reflow check fails 54 lines over 66 cols in 10 files (worst: 12-the-streaming-contract.md L140) — fix or ratchet with --budget 54 before release.
- 2026-08-27 — iteration 21 (chapter draft): research/ at 66 digests (≥60, gap-fill not triggered), so drafted ch17 → `manuscript/17-cache-aware-harness-design.md` (6,570 words; first Part IV chapter). Covers all CHAPTER_MAP beats: stable prefixes (courier-office ELI5; thesis "a session is a byte-exact asset"; the five-layer contract L0 model/engine pinned per ch16 → L1 frozen tools w/ ch13 frozen-schema + defer_loading → L2 byte-frozen system prompt w/ Claude Code stack recall + cwd/platform/git embedding → L3 static context w/ checkpoint-on-change → L4 append-only transcript → L5 volatile tail; serializer nondeterminism as named cache-breaker; deploy playbook discharging ch06's promise: version/pin/boundary-rolls/hit-rate gate); compaction vs cache invalidation (shredder ELI5; ch11's three layers recalled not re-taught; the new timing dimension — warm compaction (cache-warm summary call, 150K→30K derived), cold compaction at resume (summary re-reads history uncached), pre-idle compaction (compact before the gap: converts a 150K cold resume into a 30K one; Claude Code "resume from a summary"); breakeven t = 2.5 turns derived + xychart; 40K→10K under-four-turns with shrinking stakes; decision mermaid; external-memory escape as ch11's full pattern); session resumption and cache rehydration (keycard ELI5; TTL clock from request start w/ 4-min-stream→1-min-left; dated resume-cost box derived from published multipliers — 200K prefix warm $0.10/$0.04, 5m-cold $1.25/$0.50 = 12.5×, 1h-cold $2.00/$0.80 on Opus-5/Sonnet-5-class list prices; third-party ~$1.25/resume + 30–60% daily-cost hedged; #42338 400–500K re-writes + #71659 Pro-window exhaustion; N≥2 derived 1h-TTL breakeven; Claude Code TTL buckets + promptCacheTtl v2.1.242+; idle taxonomy interactive/think-time/overnight w/ ch15 keep-alive budgets + ch09/ch11 regime clause; byte-exact replay rule + 20-block-lookback trap + ch14 leapfrog as lifecycle rule; hedged latency 8–10 s @100K/70B, vLLM 78% 4.3→0.97 s, 85–95% prefill-share community estimate; ch12 capture-resume generation-vs-session distinction; session-lifecycle state mermaid; serializer field note); subagent context isolation as cache design (specialists'-offices ELI5; child = cache-key view — shares at most system/tools layer, never transcript; spawn keeps parent cache intact; isolated-child vs shared-preamble-fleet patterns with 1.25 + 0.1(N−1) vs N arithmetic (10 children 2.15× vs 10×, 4.7×) from OpenAI docs formula; stagger-behind-write + ~15 req/min prompt_cache_key caveats; compact-summary-return rule; fork = byte-exact replay → 0.1× + branch, OpenAI fork guidance w/ >90% illustrative flag; sibling scoping one machine+directory; cross-provider Gemini 2,048/4,096 minimums + DeepSeek ambient; spawn-flow mermaid); session-ledger arithmetic artifact (shipping-company ELI5; 11 lifecycle-event rows priced as cache events wired to ch12 usage fields/ch14 CacheLedger/ch15 scheduler/ch16 meter; ch14 salt decision at session level; generalizes like ch16's worksheet). Sources: session-resumption-cache-rehydration, subagent-context-isolation-cache, cache-breakpoint-design, context-compaction-tradeoffs digests + recalls from ch04/06/09/11/12/13/14/15/16. Gate-1 self-review performed: structure pass (5 ELI5 blocks, Words-before-machinery ×14 rows, Where-the-picture-stops ×5 breaks, Checkpoint ×6 questions with worked answers incl. $0.50-vs-$0.04 resume, 2-vs-3 gap breakeven, 105K-vs-325K fleet math, 25-block lookback miss, Build/Break/Prove/See closers, 1 xychart + 2 mermaid graphs, 1 dated box, 1 Field note, no forbidden language); tools/lint-manuscript.py run: MANUSCRIPT OK, 17/18 chapters, 19 files, 92,687 words — ch17 passes structural lint. Numbers audit: every figure traced to a dated digest, all derived math labeled (resume costs, 12.5×, N≥2, t=2.5, xychart series, 105K/325K/3.1×, 4.7×), third-party estimates hedged (per-resume $, 30–60%, 8–10 s, 85–95%), OpenAI >90% flagged illustrative by provider. Fixes applied during review (12 substantive across 5 trim passes): real correction — 40K→10K compaction breakeven restated from wrong "9 turns" to derived "under four turns" with shrinking-stakes framing; removed reader-facing "digest" wording; 5th ELI5 added to 17.6; acronym expansions at first use (KV, TTFT, JSON, API, HTTP, CI); $/M glossed in the dated box; duplicate fork-ledger row removed; ~520 words trimmed to land near the Part IV cap. Promissory notes discharged: ch06 ("chapter 17 adds the harness-side playbook" for deploys-as-herd-events → 17.2 playbook), ch09 ("the ones whose chapter 17 sessions run for hours" → overnight bullet), ch11 ×3 (session rehydration → 17.4; "full harness pattern" external memory → 17.3 close + five-layer contract; lever-table rows), ch13 ×2 ("chapter 17 assembles the prefix" + frozen-prefix assembly lever → 17.2 L1/L2), ch14 ("returns to this at the session level" salt → 17.6 last row), ch15 (keep-alive rate-budget interface → 17.4 taxonomy + Build it), ch16 (session-pinning rule → 17.2 L0; CacheLedger read → Build it). Scope discipline: tinyengine full assembly → ch18 (SessionStore ≈160-line estimate, consistent with ch12–16 residuals); llama.cpp/MLX local regime → ch18. Known residuals: (1) 6,570 words is 1.1% over the 3,500–6,500 Part IV cap — five trim passes applied, further cuts would cut mechanism or both-sides framing; logged rather than cut (well inside the ch16 11% precedent); (2) xychart/mermaid render verification remains build-time-only (carried since ch02; first mermaid state-graph with self-loop edge `W -->|each hit| W` — validate at build pass); (3) Build-it's "roughly 160 lines" is a design estimate, not a verified companion artifact (companion code owed in ch18/Appendix D per plan, consistent with ch12–16 residuals); (4) the Field note is a qualitative operator anecdote with no numbers, mirroring the documented nondeterministic-key-ordering cache-breaker semantics rather than any digest-backed incident. Next: draft ch18 (Your own engine room) — final numbered chapter.
- 2026-08-27 — iteration 21 postscript: ch17 residual (2) closed — rendered all four ch17 diagrams (correction to the entry above: 1 xychart + 3 mermaid graphs, not 2) via mermaid-cli with a `--no-sandbox` puppeteer config (`build/puppeteer-config.json`); PNGs committed to figures/png/mermaid/ per pipeline convention; self-loop state graph (m3) and xychart-beta (m1) both verified in SVG output. Environment note: bare `tools/render-mermaid.sh` fails here — Chromium's AppArmor sandbox blocks launch; mmdc needs `-p build/puppeteer-config.json` until the script grows an optional-config hook. Carried residuals for ch17 now: word-count 1.1% over cap, ≈160-line Build-it estimate (ch18/Appendix D).
- 2026-08-27 — iteration 22 (chapter draft): research/ at 72 digests (≥60, gap-fill not triggered), so drafted ch18 → `manuscript/18-your-own-engine-room.md` (5,661 words; final numbered chapter — manuscript 18/18 complete). Covers all CHAPTER_MAP beats: tinyengine assembled (ship-bridge ELI5; six-instrument table recalling tracer ~10 lines ch01/02, StreamNormalizer ~150 ch12, CacheLedger ~130 ch14, RateScheduler ~120 ch15, Router ~150 ch16, SessionStore ~160 ch17 ≈ roughly seven hundred lines; request-path mermaid assembler→scheduler→router→hosted/local endpoints→normalizer→meter→ledgers with three feedback edges; three composition properties: one dial per instrument = ch01 ownership test as code layout, no uninstrumented crossing, policy-in-config-not-code; "not a framework" both-sides), local/edge inference (home-kitchen ELI5; llama.cpp/GGUF/Ollama/MLX role split w/ Ollama-on-MLX dated; GGUF quant ladder dated 2026-08-27 incl. llama.cpp's own Llama-3-8B table Q4_0 4.34 GB +0.4685 ppl / Q4_1 4.78 +0.4511 / Q5_0 5.21 +0.1316, Q4_K_M default; bandwidth law restated: 70B Q4_K_M ~40 GB ÷ 400 GB/s ≈ 10 tok/s matching community ~12; hedged Apple-Silicon community box: 20–28 tok/s M4 Max 70B, ~10–12 at Q8, ~43 GB RAM, 12–21 tok/s aggregate tables, MLX ~15–30% faster + ~10% memory approximate, arXiv 2511.05502 M2 Ultra 192 GB first peer-quality head-to-head; single-stream = batch-size-one floor; self-hosted prefix cache = free-but-capacity-priced recall; "local engine is inference engineering with a different landlord"), when to own the engine (taxi-vs-van ELI5; dated rental/per-token box: H100 $2.39–2.49/hr, A100 marketplace $1.49–2.49, 4090 ~$1.49, DeepInfra $0.02–$2.85/1M 2026; digest's worked crossover restated: 10M tok/mo $6 vs H100 24/7 $1,793; 1,000M $600 vs A100 $1,073; derived breakeven ≈ 1,790M tok/mo ≈ ~35% of every hour at ~2,000 tok/s, labeled derived w/ varies-with-batch hedge; four honest reasons to own — utilization/privacy/edge/data gravity — and honest costs — you become the provider, no SLA but physics; policy-not-vibes routing rule w/ quarterly recompute; H100-idled-at-4% field note, qualitative), ship checklist (pre-flight ELI5; 13 numbered lines grouped by owning instrument across contract/cache/limit/money/latency-quality, each traced to its chapter; ship/night/quarter cadence), closing manifesto (electricity-meter ELI5; ch01 durable equation restated and closed; every-dial-has-a-price spine; instrument-everything; laws-outlive-architectures; Monday-morning three-instrument starter). Where-the-picture-stops ×4 breaks (engine room not a product, home cooking not free, van arithmetic assumes prices hold, checklist cannot see misroute/compaction), Checkpoint ×6 with worked answers (Q4_K_M-on-64 GB, 500÷4.5≈110 tok/s derived, 100M/$60-vs-$1,073 = 18× derived, ~35% breakeven derived, sensitivity-pins-local, bill-jump triage order). Sources: local-edge-inference digest + recalls from ch01/02/04/05/06/09/10/11/12/13/14/15/16/17 (all line estimates and component interfaces re-verified against the manuscript before writing). Gate-1 self-review performed: structure pass (5 ELI5 blocks, Words-before-machinery ×12 rows, Where-the-picture-stops section, Checkpoint ×6, Build/Break/Prove/See closers, 1 mermaid + 3 dated boxes, 1 Field note, no forbidden language); tools/lint-manuscript.py run: MANUSCRIPT OK, 18/18 chapters, 20 files, 98,199 words — ch18 passes structural lint. 6 fixes applied during review (tracer ~20→~10 lines corrected to ch01's ten-lines promise; API expansion at first use; GGUF expansion added; GPU expansion dedup; retry-budget attribution corrected to ch15's ~10% rule w/ ch10 MoE note; varies-with-batch hedge added to 2,000 tok/s assumption). Known residuals: (1) Field note is a qualitative operator anecdote (one number, 4% utilization) mirroring the crossover arithmetic, not a digest-backed incident — consistent with ch15/16/17 precedent; (2) Appendix D companion guide still owes the ~700-line tinyengine code the book estimates. ALSO THIS ITERATION (build-pipeline repairs while confirming the 18/18 manuscript builds): ch18 mermaid rendered + verified via mmdc w/ build/puppeteer-config.json; rendered the 7 previously-unrendered ch13–16 diagrams (33/33 mermaid → images in EPUB, 0 degraded); tools/render-figures.sh: added ffmpeg fallback for the KDP cover JPEG (ImageMagick/sips absent on this host; cover.png verified opaque RGB); tools/prepare-kindle-epub.py: accessibility block now injected when Pandoc <3.x omits it (was a hard failure on Pandoc 3.1.3); tools/validate-epub.py: fixed two Vol. I leftovers (EPUB filename, expected title). tools/build.sh now exits 0 one-command clean: EPUB OK 3.1M, validate-epub.py passes. Pre-existing known findings unchanged: reflow-check needs --budget 87 to freeze (54-line Gate-4 item, release-time), verify.sh still references Vol. I companion/tinyharness (inapplicable here). Next: appendices A–F per CHAPTER_MAP (start with A/B), then EDITORIAL_SYSTEM pass log updates.
- 2026-08-27 — iteration 23 (appendices): research/ at 72 files / 71 dated digests (≥60, gap-fill not triggered); manuscript was complete at 18/18 so moved to the appendix phase per AGENTS.md, starting with A/B as planned in iteration 22. Drafted `manuscript/appendix-a-plain-words.md` (3,453 words) — Inference engineering in plain words: ~200 terms harvested from all 18 chapters' Words-before-machinery tables and chapter prose, de-duplicated (TTFT/TPOT, gateway, keep-alive, TTL, cache salt, compaction, context parallelism each merged with cross-refs), grouped by the book's four parts, every entry carrying a chapter pointer so the glossary doubles as a reverse index; disambiguation entry for engine-side prefix caching (ch06) vs provider-billed prompt caching (ch14). Drafted `manuscript/appendix-b-arithmetic-cheatsheet.md` (2,477 words) — The arithmetic cheat-sheet: 27 formula cards organized by operator question (how long / how much memory / what a turn costs / what the fleet costs / when do I trip limits / when does guessing pay / when does owning beat renting), each card carrying statement, symbols, the chapter's own smallest worked example, and a "when it lies" caveat (the cheat-sheet analog of Where-the-picture-stops); provider constants confined to one dated mid-2026 box (cache 1.25×/0.1×/2×, batch 50%/24h, Sonnet 4.6 rates, H100/A100 rental, $0.60/M blended) pointing forward to Appendix C's full matrix. Gate-1 self-review performed: numbers audit re-verified every reused figure against its source chapter (decode identity 5.4 s, roofline 214 TFLOP/s @batch 64, 70B-FP8 floor ≈80 t/s, c·(N²+NM+M²/2) 8×-vs-~61–64×, 144 KiB→18 GB KV, break-even 0.28/1.11, $0.645-vs-$3.00 ≈79%, 12.5×/1.6, t=2.5, 2.15×-vs-10×, $120/$60/$39.75/$79.50 lane worksheet, 30 rps/21–24, 2×/10×/100× queueing, Little 96-in-flight, tail 63%/99.99999%, E[progress] 3.36→2.8×, crossover 1,790M ≈35%). Two real corrections applied during review: resume-card price pair re-attributed Opus-5-class ($1.25/$0.10, was mislabeled Sonnet-5-class, whose pair is $0.50/$0.04); Anthropic usage identity aligned to ch12's exact wording (total input = cache reads + cache writes + fresh input). Also 11 acronym-expansion fixes at first use per file (TTFT/ITL/TPOT/FLOPs/KV/MoE/TTL/429/EBNF/JSON). tools/lint-manuscript.py: MANUSCRIPT OK, 18/18 chapters, 22 files, 103,781 words — appendix letters A<B in reading order, appendices exempt from chapter frame per linter policy. EPUB verified: SKIP_FIGURES=1 tools/build.sh exits 0 (EPUB OK 3.1M, 33/33 mermaid intact), spine places Appendix A/B after ch18, nav TOC carries both titles. Known residuals: (1) Appendix B's constants box forward-references Appendix C (provider matrix) — discharge when C lands; (2) Appendix A intentionally omits the Everyday-picture column (pictures stay in each chapter's table; adding ~200 analogy repeats would pad, against shorter-is-better); (3) no ELI5/closers in appendices — intentional, per linter exemption and reference-shelf role; (4) appendices C–F remain. Next: appendices C (provider matrix, dated snapshot tables) + D (tinyengine companion guide, owes the ~700 lines ch12–18 estimate).
- 2026-08-27 — iteration 24 (appendices C + D, companion code delivered): research/ at 72 files / 71 dated digests (≥60, gap-fill not triggered); manuscript 18/18 + A/B so continued the appendix phase per plan. Drafted `manuscript/appendix-c-provider-matrix.md` (2,126 words) — the provider matrix: C.1 read-me/re-date discipline; C.2 prices (four dated tables: OpenAI gpt-5.6-sol/terra/luna + 5.5/5.x/legacy w/ long-context columns, Anthropic Opus/Sonnet/Haiku/Fable/Mythos + fast 2×/geo 1.1×/tokenizer ~30% note, Gemini 2.5/3.x w/ cached columns + >200K tiers, DeepSeek v4 flash/pro w/ peak-off-peak + the two-snapshot hit-price divergence flagged); C.3 cache semantics matrix (opt-in/min-prefix/write/read/TTL/traps × 4 providers, break-even 1-vs-2 reads); C.4 rate-limit meters (reservation semantics, cache-read exemption, Bedrock burndown 15×/10×/5×/1:1, worked 36,000→9,000, spend caps); C.5 structured-output guarantee tiers (strict/strict-tool/subset-ignoring/json-only w/ limits row + folklore hedge); C.6 streaming surfaces (5 rows incl. realtime/Live PCM + ~10-min reset); C.7 batch + flex/fast/priority lanes; C.8 windows + long-context tiers (GPT-5 400K/272K, Gemini 1,048,576/65,536, RULER 4×–100×+ gaps); C.9 same-weights spreads (Scout 8.3×/$0.12–0.33, R1 6.1×, gpt-oss-120b 6×, FP8 near-lossless + 18–23%, INT4 2.7×/−8 HumanEval) + rental constants (H100 $2.39–2.49/hr checked 2026-08-02, A100 $1.49–2.49, blended $0.60/M) closing ch18's crossover; C.10 re-dating rule. Every number traced to the 2026-08-27 digests (token-pricing-anatomy, prompt-caching-provider-semantics, rate-limit-quota-architectures, provider-structured-output-apis, streaming-transports-normalization, batch-api-economics, context-window-claims, same-model-different-providers, provider-latency-snapshot-2026, local-edge-inference); Appendix B's constants-box forward reference discharged (all five constant groups now in C). DRAFTED `manuscript/appendix-d-tinyengine-companion.md` (1,609 words) — the companion guide — AND shipped the actual code to `companion/tinyengine/` (STYLE.md forbids promising code that never appears; ch18 estimates now exist as files): tracer.ts 20 lines (ch01's ten-line promise + identity gap), stream-normalizer.ts 153 (4 grammars → 4 events, ToolCallAccumulator keyed per-provider-agnostic id, parse-exactly-once-at-finish, ""→{} coercion, incomplete_call marker, unknown-finish fallback), cache-ledger.ts 109 (four-term cost from dated config, breakEvenReads = ceil((w−1)/(1−r)), TTL-from-request-start, budget-gated keep-alive, sha256 deploy hook), rate-scheduler.ts 111 (per-provider quota ledger w/ OpenAI max(max_tokens,est)/Anthropic split+read-exempt/Bedrock book-then-recredit burndown, continuous token bucket, classify-before-retry incl. spend_cap park, full jitter w/ Retry-After floor, 10% retry budget, wave pacer + K-of-N), router.ts 133 (weighted table w/ guarantee tier/lane, session pins whose break is priced as a cache event, error-class breakers w/ half-open probes, classified-error-only fallback, garbage-200 → validator path, loud all-open bypass), session-store.ts 113 (five-layer renderer w/ stableStringify sorted keys, sorted tool order, append-only content-hashed log, 4-max leapfrog breakpoints, idle taxonomy → TTL class, staggered shared-preamble spawn). Total 639 module lines + 15-line env.d.ts node shim (bare-tsc, zero npm deps; delete with @types/node) — "roughly seven hundred" per ch18; Appendix D carries an honest estimated-vs-shipped table. Offline test suite tests/smoke.ts replays the chapters' Break-it/Prove-it list: Portland 3-fragment split-mid-escape assembly, meta-only SSE skip, ""→{} + unknown network_error finish, Anthropic toolu_ keyed assembly, Gemini object-args, usage identity (fresh+cached=prompt, reads≤input), $0.645-vs-$3.00 worked example (exact to 1e-9), break-evens 1 and 2, TTL-expiry event, budget-gated keep-alive, jitter bounds + Retry-After floor, burst trap on the bucket, zombie-fleet spend-cap classifier, K-of-N, dead-primary failover, garbage-200 no-fallback, all-open bypass, priced pin break, byte-exact render hash incl. reordered-tools defense, leapfrog, idle taxonomy, child isolation. Gate-1 self-review performed: numbers audit re-verified every Appendix C figure against its digest (one divergence handled by publishing both DeepSeek hit-price snapshots); Appendix D excerpts checked verbatim against shipped code; fixed during review: ch18 Build-it "tracer (twenty lines, chapter 2)" → "(ten lines, chapter 1)" (inconsistent with ch01's ten-line promise, ch18's own ~10 table, and the shipped 20-line file — caught while cross-checking the companion table). Infrastructure: tools/render-mermaid.sh grew the optional-puppeteer-config hook iteration 21 called for (auto-uses build/puppeteer-config.json when present; bare invocation now works on this host); rendered appendix-d-m1 (34/34 mermaid, 0 degraded). VALIDATION: tsc 5.9.3 clean; smoke suite green ×5 runs (jitter paths stable); tools/lint-manuscript.py MANUSCRIPT OK 18/18 chapters, 24 files, 107,267 words; tools/build.sh one-command clean → EPUB OK 3.4M; validate-epub.py passes; appendix-d diagram SVG verified to carry all 10 node labels. Known residuals: (1) ch12–16 Build-it "roughly N lines" estimates vs shipped counts differ by −16% to +100% per module (honest table in D; no interface dropped — ToolCallAccumulator/StreamNormalizer/CacheLedger/RateScheduler/Router/SessionStore all exist under the chapters' names); (2) Tester-role cadence scripts (nightly golden set / invoice reconciliation) are described in D.8 but not automated — release-time item for Appendix F; (3) reflow-check --budget 87 item unchanged (release-time); (4) appendices E (source notes/bibliography) + F (KDP/release checklist) remain — next: E, then F.
- 2026-08-27 — iteration 25 (appendices E + F — appendix phase COMPLETE): research/ at 72 files / 71 dated digests (≥60, gap-fill not triggered); manuscript 18/18 + A–D so drafted the final two appendices per plan. Drafted `manuscript/appendix-e-sources-bibliography.md` (2,644 words) — source notes and bibliography: E.1 the dated-digest method (71 digests all researched 2026-08-27, >570 distinct source URLs, 65 distinct arXiv papers — counts computed from the corpus itself; three source-discipline rules: never invent / label derived math / flag authors'-own benchmarks); E.2 trust ladder (six tiers from peer-reviewed systems papers to community calculators, mapped to the book's hedge vocabulary; provider-docs-are-the-contract-but-perishable and issues-as-primary-sources rules); E.3 curated bibliography organized by the book's four parts + reference shelf (~60 annotated entries — papers by the names the chapters use, provider pages by role, engine docs/issues by what they prove — every entry carrying chapter pointers; curation rule stated: load-bearing sources only, background reading excluded, stated so it can be audited); E.4 volatility map (six decay speeds from arithmetic-laws-years to prices-weeks, tied to the dated-box/Appendix-C re-date discipline). Drafted `manuscript/appendix-f-kdp-release-checklist.md` (1,665 words) — KDP and release checklist: F.1 honest gate ledger (Architecture pass; Gate-1 pass per chapter; Gate-2 partial — ch01 done P0=0/P1=3/P2=2, ch02–18 owed; code test pass; build test pass; copyedit owed; final proof conditional on Kindle Previewer); F.2 product setup (one-line series tether per the AI Engineering announce precedent; dual-list AI node 491300 + NLP 271581011, stay off the KDP self-pub Generative AI node 211759007011; practitioner price anchors $59.99/$79.99 list, $47.99–$54.99 eBook, not the ≤$9.99 shelf; 368–534 pp comparables; sales-reality expectations 3–15/day at tens-of-thousands BSR flagged curve-fit-only; KDP royalty mechanics explicitly flagged outside the corpus — verify at upload); F.3 files/cover/machine check (one-command rebuild, edition metadata, cover.jpg grayscale re-check, 34/34 mermaid, reflow 54-line/budget-87 fix-or-ratchet decision, Kindle Previewer phone+e-reader pass, prologue-first sample); F.4 launch sequence (six playbook moves traced to the comparables: tether announce ~9 months early, companion repo already exists privately, one-chapter excerpt syndication, tour timed to print week, free Vol. I delta extract per the Ousterhout 2nd-edition precedent, long-tail audiobook/translations; no-public-sales-figures honesty rule); F.5 release-day runbook (11 ordered steps); F.6 re-dating calendar (quarterly Appendix C re-date, engine-default re-check per upgrade, immediate re-date on acted-on announcements, annual window/spread re-check, dated-errata policy). Gate-1 self-review performed: numbers audit re-verified every figure against the books-kdp-market-data + books-launch-playbooks digests and repo state (word count 111,448 → "north of 110,000" true; 3.4M EPUB; 34 mermaid; 54/10-files reflow; P1=3/P2=2; node IDs; price bands; 9-month announce; ~700-line companion); 8 fixes applied during review, including three real corrections: removed invented paper title for arXiv:2305.13707 (digest names no title), removed untraceable GShard/Switch author attributions (digest names none — merged to one authorless entry) and the "Tam et al." attribution (untraceable), and removed the untraceable "errata intake" claim about comparable repos (digests say chapter summaries + one lead-capture page); also "the project's research corpus"→author-facing phrasing, Pope pointer ch1,3→ch1–3, Llama-3 config pointer ch4,9→ch4, editorializing "most-admired"→"a proven move". Lint: MANUSCRIPT OK, 18/18 chapters, 26 files, 111,457 words; appendix letters A<B<C<D<E<F strictly increasing. Full clean build (figures on): EPUB OK 3.4M, 34/34 mermaid, 0 degraded; validate-epub.py passes; nav TOC carries Appendix E + F titles. EDITORIAL_SYSTEM pass log updated with a Volume II pass record (8 passes, honest partial/owed statuses mirroring Appendix F's ledger). Known residuals: (1) Appendix E bibliography is curated not exhaustive (~60 of >570 URLs; curation rule published in E.1 — load-bearing sources only); (2) papers named by digest gloss where digests give no title (PSC, USP-adjacent 2508.01989, 2608.06557) are cited by ID with digest phrasing, never invented titles; (3) release-time items now owned by Appendix F: Gate-2 sweep ch02–18, copyedit, reflow decision, Kindle Previewer, QUALITY_REPORT.md does not yet exist (owed by the technical-edit sweep); (4) verify.sh still references Vol. I companion paths (inapplicable, release-time cleanup). Definition-of-done state: all 18 chapters + prologue + appendices A–F drafted; Gate-1 logged per chapter; one-command EPUB clean; PROGRESS tells the story. NEXT: this was the last planned manuscript artifact — remaining work is the release-gate list in Appendix F (Gate-2 sweep, copyedit, reflow decision, previewer), which per AGENTS.md loop semantics means future iterations pick from that list rather than CHAPTER_MAP.
- 2026-08-27 — architect: Gate-2 technical-editor reviews complete for ch01-04, 06-15 (review/*-gate2.md; ch05/16/17/18 in flight). Verdicts: 12 MINOR, 1 MAJOR (ch04 — three localized factual corrections, one writer fix-pass). NEXT FOR DRIVER after appendices: work through review/*-gate2.md in chapter order applying every P0/P1 (P2 at your judgment), one chapter per iteration, commit per chapter; then reflow fixes (54 wide lines, worst 12-the-streaming-contract.md L140) or ratchet; then final build + EDITORIAL_SYSTEM pass log.
- 2026-08-27 — architect: Gate-2 COMPLETE for all 18 chapters (review/ has the full set incl. 05/16/17/18). Driver fix-pass queue confirmed: ch04 MAJOR first, then P1s in chapter order.
- 2026-08-27 — iteration 28 (Gate-2 fix pass, ch04 — the MAJOR): research/ at 71 dated digests (≥60, gap-fill not triggered); manuscript 18/18 + A–F complete, so worked the confirmed post-appendiceal queue: ch04's MAJOR Gate-2 verdict first, then P1s in chapter order in later iterations. Applied all 3 P1 + 5 P2 findings from review/04-the-memory-that-is-not-the-model-gate2.md (P2 #9 moot — chapter is under the 5,500 cap): (P1-1) removed the fabricated ch03 quotation at the promissory-note sentence — replaced with an accurate paraphrase of what ch03 actually wrote (floor divides bandwidth by weight bytes, KV deferred); (P1-2 ×2) fixed the inverted 20–38% figure in picture-stops and See-it — it is the useful fraction, waste is 62–80%, per digest and ch06's own body; (P1-3) OPT-13B "half the size" → "only 60% larger" (13B vs 8B); (P2) "a intelligence"→"an intelligence"; B≈7 crossover re-qualified with ch03's FP8 condition (+ BF16 ≈13 derived); "FlashAttention-3"→"FlashAttention" (digest attests no version); untraceable "hypothetical ~40+ GiB" replaced with derived counterfactuals (~30 GiB GQA-8 = 244 KiB/token × 131,072; ~512 GiB MHA = 4 MiB/token × 131,072 — reviewer suggested ~32/~490, recomputed exactly); gpt-4-1106 → gpt-4-1106-preview ×2 per RULER digest. All digest citations re-verified against research/ before applying (paged-attention-block-tables, context-window-claims, kv-cache-bytes-formula, quantization-quality-benchmarks, ch03 lines 101/117/212). Lint: MANUSCRIPT OK, 18/18 chapters, 26 files; EPUB rebuilds clean (3.4M, validates). Ch04 now 5,528 words — 0.5% over the concept cap from correction-bearing additions; logged rather than trimmed (well inside precedent). Gate-2 status: ch04 findings applied 9/9; queue next: P1 sweep in chapter order (ch02, ch03, …). 
- 2026-08-27 — iteration 29 (Gate-2 fix pass, ch02): research/ at 71 dated digests (≥60, gap-fill not triggered); manuscript 18/18 + A–F complete, so continued the confirmed post-appendiceal queue (ch04 MAJOR done in iteration 28, then P1s in chapter order). First verified ch01's status before queueing: the post-review "ch01: post-draft polish" commit did apply all 3 P1 + 2 P2 from review/ch01-gate2.md (P1-1 "resamples the noise" at line 49, P1-2 FP8/INT4 numbers now in a dated box at line 170, P1-3 acronym expansions, P2-5 package-delivery ELI5 at 1.6) — ch01 confirmed done, queue advanced to ch02. Applied all 2 P1 + 3 P2 findings from review/02-the-shape-of-a-token-gate2.md (verdict MINOR, P0=0): (P1-1) Where-the-picture-stops TTFT hop count corrected four→five hops with routing restored to the list, resolving the self-contradiction with 2.5's "five of the nine hops"; (P1-2) two STYLE hard-rule violations fixed — DeepSeek $0.007/$0.22 pricing and MLPerf v5.0 SLO numbers moved out of the durable-prose spine into dated snapshot boxes (the gate-1 record had overclaimed the MLPerf boxing; now true); (P2-3) DeepSeek miss price qualified "off-peak" with peak-doubles-both-rates note per digest's $0.22/$0.44 split; (P2-4) book-first p99/p95 expanded at first use (99th/95th-percentile), SLO expanded at line 224, "NVIDIA NIM" → "NVIDIA" (NIM never expanded anywhere); (P2-5) AA speed-snapshot box now carries the JS-rendered provenance hedge (re-verify before each print run), resolving the same-day contradiction with provider-latency-snapshot-2026.md's unextractable-leaderboard note. One judgment deviation from the reviewer's literal text: the suggested MLPerf box title used "SLOs", which would have become the book's first unexpanded SLO use ahead of the same review's line-224 expansion point — retitled "interactive-latency targets" so the expansion stays the true first use. All digest citations re-verified against research/ before applying (tokenizer-fundamentals L11 prices, decode-time-budget-arithmetic L7-8 MLPerf, provider-latency-snapshot-2026 L9 JS note). Lint: MANUSCRIPT OK, 18/18 chapters, 26 files, 111,671 words. EPUB rebuilds clean (3.4M, validate-epub.py passes). Ch02 now 5,502 words — 0.4% over the concept cap from box conversions; logged rather than trimmed (inside ch04's 0.5% precedent). Gate-2 status: ch01 + ch04 + ch02 findings applied; queue next: ch03, then P1 sweep in chapter order (ch05+).
- 2026-08-27 — iteration 30 (Gate-2 fix pass, ch03): research/ at 71 dated digests (≥60, gap-fill not triggered); manuscript 18/18 + A–F complete, so continued the confirmed post-appendiceal queue (ch01/ch04/ch02 done in iterations 29/prior). Applied all 2 P1 + 7 P2 findings from review/03-the-arithmetic-of-waiting-gate2.md (verdict MINOR, P0=0): (P1-1) inserted the missing blank line before `## 3.3` — pandoc's blank_before_header would have folded the heading into the preceding paragraph and silently dropped §3.3 from the EPUB body and TOC; (P1-2) fixed the real arithmetic defect in the B200 footnote — the ridge is 2,250 ÷ 7.7 ≈ 292 or 2,250 ÷ 8.0 ≈ 281, not "near 292 either way", now stated with both divisions so the footnote teaches the chapter's own method (re-verified against arithmetic-intensity-roofline.md, which supports 292 at 7.7 TB/s only); (P2-3) vLLM 1,500–2,500 aggregate + MLPerf v6.0 17,500-vs-3,000 benchmark sentences moved from the durable-prose spine into a `Dated snapshot (throughput benchmarks; decay with engine versions)` blockquote per STYLE's hard rule, with the "gap *is* the batch dial" analysis sentence kept outside the box; (P2-4) Gemini cached-read price pair ($0.15 vs $1.50) clarified as a 2026 Flash-class generation with the box-top $0.075/$0.15 flagged as the archived-2025 tier, resolving the apparent same-box contradiction (both trace to attention-cost-scaling.md:9,:11 but from different snapshot dates); (P2-5) "eight eightk prompts" → "eight 128k prompts … sixty-four of those 128k attention problems" (8×128k=1M; the old text parsed as 8×8k≠1M); (P2-6) "resources of its own" → "their own"; (P2-7) "chattest" → "chattiest"; (P2-8) both reader-facing research-corpus filename leaks removed ("GPU-memory-hierarchy digest" → "GPU vendor datasheets and hardware teardowns, retrieved 2026-08-27", verified fair against the digest's actual sources; "decode-bandwidth-wall analysis" → "decode bandwidth-wall analysis, retrieved 2026-08-27"), per the ch04/ch11/ch16 lesson; (P2-9) FP16 expanded at true first use (16-bit floating point, glossed in the chip-table caption directly above the header cell), SRAM now expanded in the 3.1 table row (its actual first use) with the later 3.7 parenthetical de-duplicated to PCIe/NVMe only, p95 glossed as 95th-percentile. One self-caught mid-pass correction: my first P2-5 edit dropped the sentence's leading clause and was immediately restored to the reviewer's exact replacement; my first FP16 gloss was circular ("the format vendor FP16/BF16 headline rates share") and was simplified to the plain expansion. All digest citations re-verified against research/ before applying (arithmetic-intensity-roofline, attention-cost-scaling, gpu-memory-hierarchy sources). Lint: MANUSCRIPT OK, 18/18 chapters, 26 files, 111,727 words. EPUB rebuilds clean (SKIP_FIGURES=1: 3.4M, 34/34 mermaid staged, validate-epub.py passes; the cover.jpg image-size WARNING is the known benign probe warning). Ch03 now 5,825 words (+52, all correction-bearing: box header, clarifier, expansions) on a base already logged ~5% over the 5,500 concept cap in iteration 7 as content-bearing — logged rather than trimmed, consistent with ch04/ch02 fix-pass precedent. Gate-2 status: ch01 + ch04 + ch02 + ch03 findings applied; queue next: ch05, then chapter order through ch18.
- 2026-08-27 — iteration 31 (Gate-2 fix pass, ch05): research/ at 71 dated digests (≥60, gap-fill not triggered); manuscript 18/18 + A–F complete, so continued the post-appendiceal Gate-2 queue (ch01/ch04/ch02/ch03 done in iterations 29–30/prior). Applied all 1 P1 + 3 P2 findings from review/05-batches-the-engines-trick-gate2.md (verdict MINOR, P0=0): (P1-1) static-batching ASCII figure annotation corrected — the old text said "B pads ~24 wasted steps", but bar recount shows B is the straggler (32 filled / 0 empty — it pads nothing); the padding victims are C (~26) and D (~14), so the annotation now reads "C and D pad ~26 and ~14 wasted steps" with the slot-C comma upgraded to a semicolon — the mislabel had inverted the chapter's own straggler-vs-padding ownership distinction; (P2-2) TRT-LLM sweet-spot claim "~20% more throughput than either extreme" split into its two true sides — ~20% vs 2048 (÷2,044.26 ≈ 1.21) and ~27% vs 64 (÷1,944.26 ≈ 1.27), both divisions now displayed — the single-derived-both-sides error was inherited from batching-size-latency-tradeoffs.md, verified; (P2-3) budget example's 4,000-token prompt → 12,000-token prompt — with 8,128 tokens of leftover budget a 4,000 prompt fits one iteration (nothing forces "two"), while 12,000 = 8,128 + 3,872 genuinely spans two; grep confirmed no cross-chapter references to the old constant, and the separate 4,000-vs-4×1,000 E[S²] illustration at the queueing section is a different mechanism, left untouched; (P2-4) queueing table header scoped to "Mean system time multiplier 1/(1−ρ), M/M/1 form" — the 1/(1−ρ) system-time scaling is the exponential-service M/M/1 result, while the chapter's M/G/1 framing covers only the queue-wait term (E[W] = λE[S²]/2(1−ρ), stated correctly two sentences above). All digest citations re-verified before applying (batching-size-latency-tradeoffs sweep numbers + the digest's own one-sided derivation; continuous-batching budget semantics; goodput-and-slos classical-M/M/1 attribution; bar counts recomputed mechanically, not eyeballed). Lint: MANUSCRIPT OK, 18/18 chapters, 26 files, 111,747 words. EPUB rebuilds clean (SKIP_FIGURES=1: 3.4M, 34/34 mermaid staged, validate passes; cover.jpg image-size WARNING remains the known benign probe warning). Ch05 now 5,011 words — inside the 3,000–5,500 band. Gate-2 status: ch01 + ch04 + ch02 + ch03 + ch05 findings applied; queue next: ch06, then chapter order through ch18.
- 2026-08-27 — iteration 32 (Gate-2 fix pass, ch06): research/ at 71 dated digests (≥60, gap-fill not triggered); manuscript 18/18 + A–F complete, so continued the post-appendiceal Gate-2 queue (ch01/ch04/ch02/ch03/ch05 done in iterations 29–31/prior). Applied all 1 P1 + 4 P2 findings from review/06-paging-the-brain-gate2.md (verdict MINOR, P0=0): (P1) the dated pricing box's OpenAI bullet said a flat "50% discount" — stale against the book's own digest; replaced with the generation-split pricing per research/prompt-caching-provider-semantics.md (GPT-5.6-class: 0.1× reads + 1.25× writes; older models model-dependent "up to 90%", 50% only launch-era Oct 2024), and the Prove-it closer's "reads at 0.1× or 50%-off inputs, writes at 1.25× where explicit" amended to "reads at 0.1× on current models (older: model-dependent discount), writes at 1.25× where charged" — verified consistent with ch14's own framing (line 39) and its provider table; (P2) TTL expanded at true first use (5-minute TTL — time to live) in the Anthropic bullet; (P2) O(1) glossed as constant-time eviction (one queue pop however full the pool) per the ELI5-ladder reader; (P2) RAM (random-access memory) and HTTP (hypertext transfer protocol) expanded at first use, matching the chapter's own GPU/KV/API/LLM/HBM standard; (P2) bundled citation split — the 16–128/16–32 sweep attributed to arXiv:2309.06180 while the shipped-16 default + multiples-of-8 mamba constraint attributed to vLLM docs only (the 2023 paper predates mamba support; per-source attribution keeps the claim ledger clean per paged-attention-block-tables.md L18). All digest citations re-verified against research/ before applying. Lint: MANUSCRIPT OK, 18/18 chapters, 26 files, 111,801 words. EPUB rebuilds clean (SKIP_FIGURES=1: 3.4M, 34/34 mermaid staged, validate-epub.py passes; cover.jpg image-size WARNING remains the known benign probe warning). Ch06 now 5,411 words — inside the 3,000–5,500 band. Gate-2 status: ch01 + ch04 + ch02 + ch03 + ch05 + ch06 findings applied; queue next: ch07, then chapter order through ch18.
- 2026-08-27 — iteration 33 (Gate-2 fix pass, ch07): research/ at 71 dated digests (≥60, gap-fill not triggered); manuscript 18/18 + A–F complete, so continued the post-appendiceal Gate-2 queue (ch01/ch04/ch02/ch03/ch05/ch06 done in iterations 29–32/prior). Applied all 2 P1 + 3 P2 findings from review/07-prefill-decode-great-divorce-gate2.md (verdict MINOR, P0=0): (P1-1) DistServe box allocation direction corrected — "2:1 decode-heavy" → "2:1 prefill-to-decode allocation — two prefill GPUs feeding one decode GPU"; the box's own figures (prefill island ≈5.6 < decode island ≈10 req/s/GPU) make only the 2-prefill:1-decode split reach ≈10 total ≈3.3/GPU, so the old label contradicted the box, the digest (goodput-and-slos.md L11), and Checkpoint Q5, which now asks about a possible allocation; the separate §7.4 "decode-heavy *workload*" phrase (a workload shape, line 111) correctly left untouched; (P1-2) Sarathi-Serve measured figures (2–4× strict / ≈6× relaxed / ch05's 2.6×/3.7×/5.6× series) moved from the durable-prose spine into a new `Dated snapshot (Sarathi-Serve capacity vs prior systems — ShareGPT-style traces on A100s; arXiv:2403.02310, OSDI 2024)` box with the authors'-own-comparisons hedge inside, per STYLE's hard rule (identical pattern was the ch01 gate-2 P1); the "under tail-latency constraints, which is the point" analysis sentence kept outside the box as durable prose; (P2-3) §7.1 Goodput sentence no longer points to a nonexistent table row ("treat that table row as read" → "it needs no second entrance ramp"); (P2-4) §7.5 queueing item's untraced "15% load increase" replaced with the book's own tabulated example "moving from 0.8 to 0.95 utilization — under a fifth more load — can multiply queue wait about 4×" (matches ttft-queueing-under-load.md L6's 0.2/0.05 derivation and ch05; 0.95/0.8 = 1.1875 ≈ under a fifth); (P2-5) DistServe box header widened to "disaggregation results, 2024 — DistServe: … arXiv:2401.09670; Mooncake: arXiv:2407.00079" since the box carries Mooncake's 525%/75% figures, preventing misattribution to the DistServe experiment. All digest citations re-verified against research/ before applying (goodput-and-slos.md L11/L15, ttft-queueing-under-load.md L6, chunked-prefill-pd-split.md). Lint: MANUSCRIPT OK, 18/18 chapters, 26 files, 111,833 words. EPUB rebuilds clean (SKIP_FIGURES=1: 3.4M, 34/34 mermaid staged, validate-epub.py passes; cover.jpg image-size WARNING remains the known benign probe warning). Ch07 now 4,991 words — inside the 3,000–5,500 band (box conversion added a few words). Gate-2 status: ch01 + ch04 + ch02 + ch03 + ch05 + ch06 + ch07 findings applied; queue next: ch08, then chapter order through ch18.
- 2026-08-27 — iteration 34 (Gate-2 fix pass, ch08 + figure-pipeline label fix): research/ at 72 files / 71 dated digests (≥60, gap-fill not triggered); manuscript 18/18 + A–F complete, so continued the post-appendiceal Gate-2 queue (ch01/ch04/ch02/ch03/ch05/ch06/ch07 done in iterations 29–33/prior). Applied ch08 findings from review/08-guessing-at-the-speed-of-light-gate2.md (verdict MINOR, P0=0): (P1-1) mermaid loop-diagram node corrected "forward progress is τ + 1 tokens" → "τ tokens" — τ already includes the guaranteed token per the chapter's own formula (geometric sum 1 + α + … line), 8.1 glossary, and the EAGLE-3 τ/speedup pairing; (P1-2) 8.4 τ definition re-aligned from "mean accepted tokens per verify step" to "mean tokens of forward progress per verify step (accepted guesses plus the guaranteed token)", making the τ 5.84–6.62 → 4×–5.5× pairing derivable; (P1-3) "15–25% speedup loss" corrected to "15–25% on three of the four models — the 70B drops only ~4%" (recomputed: 15.6/22.3/4.1/15.4%); (P2) intro's unsourced 20 tok/s hedged as illustrative against ch03's derived ~24 t/s 70B ceiling; untraced "2023 methods" re-anchored to EAGLE-1's measured τ ≈ 3.5–4.5 (spec-decode-acceptance-data.md, verified before applying); MTP/MLP expanded at first use; P2 "speculative samples" typo found MOOT — file already reads "speculative sampling" (grep 0 hits; reviewer artifact); Medusa-2 lower bound 2.2×–3.6× → 2.3×–3.6× after verifying the primary source (arXiv:2401.10774 abstract v3, fetched this iteration: "Medusa-2 further improves the speedup to 2.3-3.6x") — the two digests genuinely diverge (speculative-decoding-engines.md says 2.2×–3.6×, spec-decode-acceptance-data.md 2.3×–3.6×); research/ left untouched per the never-edit rule, manuscript now matches the primary source, Appendix E's Medusa entry carries no numbers so needed no change. ALSO THIS ITERATION (discovered while re-rendering the corrected ch08 diagram): the mermaid PNG pipeline was silently dropping ALL flowchart labels — mermaid v11 emits node/edge labels as <foreignObject> HTML, and librsvg (rsvg-convert, the old SVG→PNG step) renders foreignObject as nothing, so every one of the 20 flowchart-family PNGs committed since iteration 22 showed boxes and arrows with no text (verified pixel-level: 0 dark pixels inside node regions; stale-vs-fixed label test rendered byte-identical, the tell). htmlLabels:false does not fix it — mermaid v11's flowchart renderer ignores the setting via both -c config and inline %%{init}%% directives (verified empirically). Fix: tools/render-mermaid.sh now rasterizes PNGs directly from mermaid-cli (Chromium, -s 4 ≈ old 2400px width), which renders HTML labels natively; SVG outputs still generated for browser-viewable inspection. All 34 diagrams re-rendered and pixel-verified (flowcharts: dark text pixels present incl. the corrected τ label at 11,965 dark pixels in node-I; xycharts: ink present — their labels were always SVG <text> and unaffected, re-rendered anyway for one unified path). CORRECTION TO PRIOR LOGS: iteration 21's "verified in SVG output" and iteration 22's "34/34 mermaid → images, 0 degraded" were SVG/staging-level checks — the label loss was not caught until now; EPUBs built before this iteration carry label-less flowchart diagrams. Validation: lint MANUSCRIPT OK 18/18 chapters, 26 files, 111,873 words (+40, all correction-bearing hedges/expansions); EPUB rebuilds clean (6.0M, larger because labeled rasters carry real pixels; 34/34 mermaid staged, validate-epub.py passes; cover.jpg image-size WARNING remains the known benign probe warning). Gate-2 status: ch01 + ch04 + ch02 + ch03 + ch05 + ch06 + ch07 + ch08 findings applied; queue next: ch09, then chapter order through ch18.
- 2026-08-27 — iteration 35 (Gate-2 fix pass, ch09): research/ at 72 files / 71 dated digests (≥60, gap-fill not triggered); manuscript 18/18 + A–F complete, so continued the post-appendiceal Gate-2 queue (ch01/ch04/ch02/ch03/ch05/ch06/ch07/ch08 done in iterations 29–34/prior). Applied all 2 P1 + 5 P2 findings from review/09-smaller-numbers-faster-engines-gate2.md (verdict MINOR, P0=0): (P1-1) FP8 formats relabeled — "(e4m3/e5m2 — 4-or-5-bit mantissa variants)" → "4-or-5-bit exponent, 3-or-2-bit mantissa variants"; in E4M3/E5M2 the first digit counts exponent bits, the second mantissa bits, so the durable spine had credited exponent bits to the mantissa; (P1-2) §9.6's volatile provider market numbers (Scout 53.5–446.7 tok/s / 8.3×, AIMultiple Qwen3-32B FP8/INT4, SemiAnalysis 18%/18%/23%) moved out of the durable-prose spine into a "Provider snapshot (retrieved 2026-08-27)" box per STYLE's hard rule — identical pattern to the ch01/ch03/ch07 gate-2 P1s — with durable prose keeping only "~8× spread; part is precision" and pointing economics to the 9.3 box; (P2-3) MRCR expansion corrected "multi-round coreference retrieval" → "multi-round co-reference resolution" — verified this iteration against the benchmark's source lineage (Michelangelo, arXiv:2409.12640 §2.2, "Multi-Round Co-reference Resolution (MRCR)"; neither digest expands it, exactly as the reviewer noted; vLLM blog itself never expands it, confirmed by fetch); (P2-4) GSM8K 0.00 datum re-attributed to its true regime with an em-dash qualifier "— a W4A4 run, weights and activations both quantized —" per quantization-quality-benchmarks.md L7 ("GSM8K drops 0.00 in one 32B W4A4 case"), so weight-only 4-bit is no longer quietly credited with a datum from the both-sides regime; (P2-5) intro's stitched non-verbatim ch4 "quotation" replaced with an accurate paraphrase (ch4 L163: "halves every row of the 4.3 table exactly, with measured quality caveats that are workload-dependent", "chapter 9 owns the full menu" in the citation parenthetical — the old quote dropped "the 4.3" and spliced non-adjacent fragments); (P2-6) GGUF expanded at first use as "(GPT-Generated Unified Format — llama.cpp's single-file model container)", matching ch18's own expansion "GGUF — GPT-Generated Unified Format" rather than the reviewer's role-gloss, for book-wide consistency; (P2-7) Hopper glossed at first occurrence as "(NVIDIA's H100-generation chips)". Two documented deviations from reviewer literal text: (a) P2-7 uses "chips" not "GPUs" — GPU letters' true first use + expansion is four lines later in the AWQ paragraph, so the reviewer's literal gloss would have introduced the unexpanded acronym before its first-use expansion, violating the rule the fix serves (same class of deviation as iteration 29's SLO-retitled box); (b) the suggested §9.6 box re-included the SemiAnalysis line already boxed in §9.3 — dropped the duplicate, prose points to the 9.3 box instead, keeping one re-dating site per Appendix C discipline. Reviewer notes (not findings) handled: 18%-cheaper kept digest-verbatim (no prices quoted beside the percentage in prose, so the reconcile-only-if-quoted condition isn't met); W8A8-FP8 kept over the paper's W8A8-FP (book vocabulary); xychart grayscale contrast remains the visual-proof/release-gate item. Lint: MANUSCRIPT OK, 18/18 chapters, 26 files, 111,883 words. EPUB rebuilds clean (SKIP_FIGURES=1: 6.0M, 34/34 mermaid staged, validate passes; cover.jpg image-size WARNING remains the known benign probe warning). Ch09 now 5,772 words (+12 net: box conversion + qualifiers − trimmed prose) on a base already logged ~4.7% over the 5,500 concept cap in iteration 13 as content-bearing — logged rather than trimmed, consistent with ch03/ch13/ch15 precedent. Gate-2 status: ch01 + ch04 + ch02 + ch03 + ch05 + ch06 + ch07 + ch08 + ch09 findings applied; queue next: ch10, then chapter order through ch18, then reflow decision (54 wide lines) per Appendix F.
- 2026-08-27 — iteration 36 (Gate-2 fix pass, ch10): research/ at 72 files / 71 dated digests (≥60, gap-fill not triggered); manuscript 18/18 + A–F complete, so continued the post-appendiceal Gate-2 queue (ch01–ch09 done in iterations 29–35/prior). Applied all 1 P1 + 3 P2 findings from review/10-one-model-many-chips-gate2.md (verdict MINOR, P0=0): (P1-1) wrong cross-chapter attribution fixed — §10.3's TP paragraph credited "chapter 2's promissory note" for a quote that lives in chapter 3 (03:107 "splitting the weight stream across chips so bandwidths add — chapter 10" + 03's §3.8 lever-table row "Splits the weight stream; bandwidths add"), contradicting ch10's own intro which already credits chapter 3; sentence now quotes ch03's lever-table row verbatim; (P2-2) CP-paragraph benchmark receipt de-duplicated with ch11 — the 77 s / 93% / 3.8 s scaling numbers (arXiv:2411.01783) are ch11's "published headline" (11 presents them in full and opens "Chapter 10 named the axis"), so ch10 keeps a one-line receipt and points onward per CHAPTER_MAP's split (ch10 owns the axes in plain words; ch11 owns context parallelism's cost story); (P2-3) two spliced non-verbatim sibling quotes repaired — ch07's real internal quote is "each phase gets its own plan" (07:101, full sentence "Each pool gets hardware, replica count, and a parallelism plan…" now paraphrased outside the quote marks) and ch04's real sentence is "Real engines shard across GPUs (chapter 10 divides KV too)" (04:184, quoted verbatim now); (P2-4) STYLE hard-rule fix — the five-axes kitchen ELI5 carried bare acronym tags "(TP)/(PP)/(DP)/(CP)/(EP)", the only ELI5 in the manuscript with jargon inside the block; tags removed and the mapping moved one line below the analogy ("Those five ways, in the order just listed: TP, PP, DP, CP, EP — the table in 10.1."), keeping first-use discipline intact since §10.1's vocab table defines the quintet. One documented deviation from reviewer literal text: the P1 replacement cited "3.6's lever table" but ch03's lever table verifiably lives in §3.8 ("What this arithmetic buys you"; §3.6 is "Why one token is cheap and a million are not linear") — used 3.8 (same class of correction-as-applied as iteration 29's SLO-retitled box). Residual parenthesized-acronym uses at lines 96/100 verified as legitimate post-machinery glosses (diagram caption + Megatron sizing box), not ELI5 content. All citations re-verified against manuscript/03, 04, 07, 11 and CHAPTER_MAP before applying; grep confirms no stray references to the removed 93%/3.8 s figures. Lint: MANUSCRIPT OK, 18/18 chapters, 26 files, 111,889 words. EPUB rebuilds clean (SKIP_FIGURES=1: 6.0M, 34/34 mermaid staged, validate passes; cover.jpg image-size WARNING remains the known benign probe warning; no figures changed this iteration so SKIP_FIGURES is sound). Ch10 now 5,443 words (+10 net) — inside the 3,000–5,500 concept band. Gate-2 status: ch01 + ch04 + ch02 + ch03 + ch05 + ch06 + ch07 + ch08 + ch09 + ch10 findings applied; queue next: ch11, then chapter order through ch18, then reflow decision (54 wide lines) per Appendix F.
- 2026-08-27 — iteration 37 (Gate-2 fix pass, ch11): research/ at 72 files / 71 dated digests (≥60, gap-fill not triggered); manuscript 18/18 + A–F complete, so continued the post-appendiceal Gate-2 queue (ch01–ch10 done in iterations 29–36/prior). Applied all 2 P1 + 4 P2 findings from review/11-long-context-is-a-memory-product-gate2.md (verdict MINOR, P0=0): (P1-1) the mid-2026 snapshot box's Anthropic bullet had priced a long-context surcharge onto the current generation — replaced with the generation split per research/token-pricing-anatomy.md L48–49 ("Claude 4.6+ ships the full 1M-token window at standard pricing — no long-context surcharge"; older sheets tier, e.g. Sonnet 4.5 ≤200K vs separate >200K, matching ch04:114's correctly versioned table); the old text made ch11 contradict both the digest and the book's own Appendix C:31 ("Claude 4.6+ ships the full 1M-token window at standard price — positioning, not physics (chapter 11)") — the fix brings chapter, digest, and matrix into agreement; (P1-2) Where-the-picture-stops "price tiers sit at 200K tokens at two of the three major providers" corrected to "two of the three major providers price a long-context tier — Google at 200K, OpenAI at ≥272K — while Anthropic's current generation ships 1M at standard price" per the pricing digest (OpenAI's long tier is defined as ≥272K-token requests, L13/L17; at most one provider tiers at 200K); (P2-3) ch04 droop-series label fixed "at 8K → 32K → 128K" → "from short context → 32K → 128K" — ch04:159 derives 208 tok/s at short context (16 GB weights-only payload; reviewer's ~197-at-8K counterfactual recomputed and confirmed: 128 KiB/token × 8K = 1 GiB → 208×16/17 ≈ 196); (P2-4) undefined ring-size N (colliding with §11.2's prompt-length N in c·(N²+NM+M²/2)) re-symbolized "after N−1 hops" → "after d−1 hops, with d chips in the ring" per the digest's ring-size meaning; (P2-5) MagicDec ~90% acceptance re-attributed to its true setup — "on a 70B drafter at batch 1" — per spec-decode-acceptance-data.md L13 (the 2.51× headline and the ~90% belong to different experiments: 8B/batch 32–256 vs 70B self-draft/batch 1); (P2-6) YaRN expanded at its only manuscript use as "(a RoPE-scaling method)". One documented deviation from reviewer literal text: P2-6 uses "a RoPE-scaling method" not "an interpolated RoPE-scaling method" — both digests describe YaRN only as RoPE-scaling/position-scaling, so "interpolated" would be a mechanism claim outside the corpus (same class of discipline as iteration 25's attribution removals). All digest citations re-verified against research/ before applying (token-pricing-anatomy, spec-decode-acceptance-data, context-parallelism-long-context, context-window-claims, ch04:114/159). Ripple check: grep confirmed the remaining "N−1" hits (ch17 fleet arithmetic, ch18 latency identity) are unrelated; ch11:192's "measure TTFT at 8K, 32K, 128K, 256K" is a fresh probe recipe, not the mislabeled droop series, left intact; no other chapter or appendix carried the corrected claims. Lint: MANUSCRIPT OK, 18/18 chapters, 26 files, 111,930 words (+41, all correction-bearing). EPUB rebuilds clean (SKIP_FIGURES=1: 6.0M, 34/34 mermaid staged, validate-epub.py passes; cover.jpg image-size WARNING remains the known benign probe warning; no figures changed this iteration so SKIP_FIGURES is sound). Ch11 now 5,083 words — inside the 3,000–5,500 concept band. Gate-2 status: ch01 + ch04 + ch02 + ch03 + ch05 + ch06 + ch07 + ch08 + ch09 + ch10 + ch11 findings applied; queue next: ch12, then chapter order through ch18, then reflow decision (54 wide lines) per Appendix F.
- 2026-08-27 — iteration 38 (Gate-2 fix pass, ch12): research/ at 72 files / 71 dated digests (≥60, gap-fill not triggered); manuscript 18/18 + A–F complete, so continued the post-appendiceal Gate-2 queue (ch01–ch11 done in iterations 29–37/prior). Applied all 2 P1 + 5 P2 findings from review/12-the-streaming-contract-gate2.md (verdict MINOR, P0=0): (P1-1) Field-note arithmetic contradiction fixed — "hang 40 minutes … ten-minute line — three times over" broke the chapter's own derived 3 × 10 min ≈ ~30 minutes (dated box + checkpoint 5 both teach it); anecdote now reads "hang ~30 minutes", aligning the operator observation with the SDK arithmetic it illustrates; (P1-2) gpt-oss-120b ~3,000-vs-~500 tokens/s Cerebras/Groq benchmark numbers moved out of the durable-prose spine into a new "Dated snapshot — same weights, different stacks (2026-08-27)" box per STYLE's hard rule (same pattern as the ch01/ch03/ch07/ch09 gate-2 P1s), prose keeps the six-fold spread claim + pointer; (P2-3) GLM-5.3 1.6/30.1/7.5 s split attribution reworked to "(mid-2026 Artificial-Analysis snapshot, quoted in full in chapter 2)" — book-wide boxing convention verified first: ch02:121 carries the identical numbers in dated-attribution prose, so both chapters now match (callback, not divergence); (P2-4) "no provider exposes TTFT distributions" scoped to "hosted providers … checked Anthropic and OpenAI docs" — the digest verifies the negative for two providers only, and self-hosted vLLM does publish server-side TTFT histograms (the chapter's own See-it says so); (P2-5) "no provider publishes a milliseconds-per-hit figure" → "neither OpenAI nor Anthropic publishes … (checked 2026-08-27)" per digest scope; (P2-6) acronym expansions at true first use — TTFT at the intro (time to first token), JSON in the 12.1 table row (JavaScript object notation, with the §12.2 duplicate expansion de-duplicated to plain "JSON"), LLM in See-it (large language model; grep confirmed line 184 is the chapter's only LLM use); (P2-7) unverifiable vendor-motive claim hedged — "the SDK defaults are ten minutes long precisely because" → "OpenAI's and Anthropic's SDK defaults … largely because" (digests give no vendor motive, and the 10-min default belongs to those two SDKs only, not Google's). All digest citations re-verified against research/ before applying (provider-latency-snapshot-2026.md L6-8/L18/L25 for the box wording incl. Cerebras-docs/Groq-catalog attribution and the derived 6×; cancellation-timeout-semantics.md for the 3 × 10-min arithmetic; serving-observability-metrics.md for the two-provider TTFT negative; grep confirmed no ripples — "40 minutes" appears nowhere else in manuscript/). Lint: MANUSCRIPT OK, 18/18 chapters, 26 files, 111,987 words. EPUB rebuilds clean (SKIP_FIGURES=1: 6.0M, 34/34 mermaid staged, validate passes; cover.jpg image-size WARNING remains the known benign probe warning; no figures changed this iteration so SKIP_FIGURES is sound). Ch12 now 5,377 words (+58 net: box conversion + expansions − de-duplicated §12.2 expansion) — inside the 3,000–5,500 concept band, resolving the reviewer's word-count note (by-inspection "at or slightly above" was under, exact figure 5,377). Gate-2 status: ch01 + ch02 + ch03 + ch04 + ch05 + ch06 + ch07 + ch08 + ch09 + ch10 + ch11 + ch12 findings applied; queue next: ch13, then chapter order through ch18, then reflow decision (54 wide lines) per Appendix F.
- 2026-08-27 — iteration 39 (Gate-2 fix pass, ch13): research/ at 72 files / 71 dated digests (≥60, gap-fill not triggered); manuscript 18/18 + A–F complete, so continued the post-appendiceal Gate-2 queue (ch01–ch12 done in iterations 29–38/prior). Applied all 1 P1 + 4 P2 findings from review/13-structured-output-is-not-a-prompt-trick-gate2.md (verdict MINOR, P0=0): (P1-1 ×2) dangling sampler-knob cross-references fixed — §13.2 step 3 said "all the chapter-5 sampler knobs" and the lever table row credited "Chapter 2, chapter 5", but grep-verified that top-p/min-p/sampler-knob content exists nowhere outside ch13 in the entire manuscript; step 3 now reads "the usual sampler knobs" and the lever row now reads "Not owned elsewhere — glossed in §13.2", so neither pointer sends readers to background that does not exist; (P2-2) derived TTFT ratio "≈8× faster first token" → "≈8.5×" (38.5 ÷ 4.552 = 8.46, matching the two-significant-figure rounding of its sibling ≈4.3×/≈4.2× ratios; figures re-verified against constrained-decoding-grammars.md L11); (P2-3) "half a minute building a per-request FSM" → "over half a minute" — 38.5 s exceeds 30 s by ~28% and the understatement softened the compile-tax argument the exact figure was making; (P2-4) CRANE model variant corrected "Qwen2.5-Math-7B" → "Qwen2.5-Math-7B-Instruct" per structured-output-costs-tension.md L8 (the base 7B is a different model from the one that produced the 29%/38% numbers); (P2-5) grader instrument corrected "a tolerant parser" → "an LLM-based parser" in the 43.7%-vs-75.5% sentence — the digest attributes 75.5% specifically to an LLM parser, "tolerant" was the chapter's generalization. Judgment calls documented: See-it-in-the-wild's separate "run your strictest validator and a tolerant parser" (line 178) left untouched — that is operator-practice advice the digest itself recommends ("grade/validate with a tolerant parser, never a tight regex", L27), not an attribution of the measured number; ch08's "rejection sampler"/"the sampler" mentions verified unrelated (speculative-decoding content, no chapter-5 pointers). Ripple checks: no manuscript-wide references to Qwen2.5-Math outside ch13; no other chapter references ch5 sampler knobs. All digest citations re-verified against research/ before applying. Lint: MANUSCRIPT OK, 18/18 chapters, 26 files, 111,991 words. EPUB rebuilds clean (SKIP_FIGURES=1: 6.0M, 34/34 mermaid staged, validate passes; cover.jpg image-size WARNING remains the known benign probe warning; no figures changed this iteration so SKIP_FIGURES is sound). Ch13 now ≈5,762 prose words (+~5, all correction-bearing) on a base logged at 5,863 draft words / over the 5,500 concept cap in iteration 17 as content-bearing — logged rather than trimmed, consistent with ch03/ch04/ch09/ch12 fix-pass precedent. Gate-2 status: ch01–ch13 findings applied; queue next: ch14, then chapter order through ch18, then reflow decision (54 wide lines) per Appendix F.
