# Canonical retail-builder versions. tools/release-verify.sh fails closed when
# any of these drift, because Pandoc and converter changes can alter bytes or
# rendering even when the manuscript is unchanged.
PANDOC_VERSION=3.10.2
EPUBCHECK_VERSION=5.3.0
ACE_VERSION=1.4.6
KINDLE_PREVIEWER_VERSION=3.106
